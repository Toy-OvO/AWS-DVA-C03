# AWS Certified Data Engineer – Associate (DEA-C01) 判断ポイント集

抽出元：模擬試験5セット（実質170問分の設問＋正解）を論点単位に再編成したもの。
「どのサービスか」ではなく「**どの条件のときにどれを選ぶか**」を言語化することを目的とする。

---

## 0. 全論点に効く「キーワード → 反応」早見表

DEA-C01 は選択肢が全部それなりに動く。優劣を決めているのは**問題文の末尾の修飾語**であることが大半なので、まずここを機械的に拾う訓練をする。

| 問題文のキーワード | 反応すべき方向 |
|---|---|
| 最小限の運用負荷 / 運用オーバーヘッド / 管理の手間 | マネージド・サーバーレス優先。自前スクリプト／EC2／Lambda自作ロジックは切る |
| 最小限の開発作業 / コーディング作業 / コードを書かずに | ノーコード系（DataBrew、Glue Studio、組み込み変換）優先 |
| 最小限のリファクタリング / 既存資産をそのまま | OSS互換のマネージド（MWAA、MSK、EMR）。書き換えを伴う移行は切る |
| 最もコスト効率よく | 「常時稼働」を切る。サーバーレス／Flex／スポット／S3への外出し／ファイル形式最適化 |
| ほぼリアルタイム / near real-time / 数分以内 | Firehose、Redshiftストリーミング取り込み、DMS CDC |
| リアルタイム / 低レイテンシー / 秒未満 | Kinesis Data Streams + Managed Service for Apache Flink |
| 週に数回 / 月に数回 / アドホック | Athena（サーバーレス）優先。専用クラスタ常時稼働は切る |
| 継続的に大量・高頻度クエリ | Redshift（またはRedshift Serverless） |
| 既存の◯◯（BIツール/Airflow/Kafka/Hive）はそのまま使う | 互換性のあるマネージド版に寄せる |
| きめ細かい / 行レベル / 列レベルのアクセス制御 | Lake Formation（S3データレイク）／Redshift RBAC・動的マスキング（DWH内） |
| データの重複を作らない | ビュー／マスキングポリシー／データ共有。コピーを作る選択肢は切る |
| 変更分だけ / 増分 | Glueジョブブックマーク、DMS CDC、Iceberg/Delta のMERGE |
| ◯つ選択 | 「同じ層の対策を2つ」ではなく「別の層の対策を1つずつ」が正解になりやすい |

**メタ的なひっかけ**：問題文に「すでに◯◯は設定済み」と書いてある場合、その◯◯を再設定する選択肢は必ず誤答。原因は**明記されていない別レイヤー**にある。

---

## 1. 取り込み・転送サービスの選定（DataSync / DMS / Transfer Family / AppFlow / Data Exchange）

- **問われ方**: 「オンプレのXをAWSへ」「サードパーティのYを取り込みたい」「SaaSのZのデータを分析したい」。末尾は必ず運用負荷かコスト。

- **正解を選ぶ判断基準**:
  - **ソースがファイル／ファイルサーバー／NAS/NFS/SMB/HDFS** → DataSync
  - **ソースがデータベース**（かつ継続的な変更取り込みが要る） → DMS（CDC）
  - **ソースが異種DB**（Oracle→Redshift、Teradata→Redshift など） → **SCT でスキーマ変換 + DMS でデータ移行**のセット
  - **相手がSFTP/FTPS/FTPクライアントを使い続けたい** → Transfer Family
  - **ソースがSaaS**（Salesforce、HubSpot、Zendesk、Google Analytics 等） → AppFlow
  - **ソースが第三者の商用データセット**（市況、統計、人口動態など購入するデータ） → **AWS Data Exchange**
  - **専用線・インターネットを経由しない** → Direct Connect（+ 認証情報は Secrets Manager）

- **紛らわしい選択肢との切り分け**:

  | サービス | 得意 | 選ばない条件 |
  |---|---|---|
  | DataSync | ファイル/オブジェクトの一括＋差分同期、スケジュール実行、転送検証つき | DBのトランザクションログを追いたい／SaaS API |
  | DMS | DB→DB / DB→S3、フルロード＋CDC、ダウンタイム最小化 | ファイル転送、SaaS |
  | Transfer Family | 既存SFTPクライアント資産の維持 | 転送の自動スケジュール・差分検出が主目的 |
  | AppFlow | SaaS ↔ S3/Redshift、双方向、スケジュール/イベント | 自社DB、ファイルサーバー |
  | Data Exchange | 「購読する」外部データセット | 自社データ、自社SaaS |
  | S3 Transfer Acceleration | 単純に**遠距離の転送速度**を上げる | 差分同期・スケジュール・整合性検証が要件のとき |
  | Snowball/Snowmobile | 回線が細い＋大容量＋一回きり | 日次の継続同期 |

- **ひっかけパターン**:
  - 「オンプレ→S3、毎日変更分だけ、スケジュール実行」に **Transfer Acceleration** を混ぜる（速度は上がるが差分同期もスケジュールも持たない）。
  - 「毎月DBを丸ごとコピーしていてコストが増えた」→ 全量再送を続ける選択肢ではなく **DMSのCDCで差分だけ**が正解。
  - SaaS取り込みに Glue の JDBC 接続を提案する（SaaS はJDBCで繋がらないことが多い）。
  - サードパーティ提供データに Glue Data Catalog を提案する（カタログは調達手段ではない）。

- **派生して問われうる点**: DMS のレプリケーションインスタンスのサイジング、DMS のターゲットとして S3 を選び Parquet で書く設定、Transfer Family のセキュリティポリシー（TLS最低バージョン）、DataSync の転送検証オプション、Direct Connect と VPN の使い分け。

---

## 2. ストリーミング取り込みサービスの選定（KDS / Firehose / MSK / Flink）

- **問われ方**: 「毎秒N件のイベントを…」「リアルタイムに可視化したい」「ストリームを複数チームで読みたい」。

- **正解を選ぶ判断基準**:
  - **配信先がS3/Redshift/OpenSearchで、変換は軽く、バッファ遅延（数十秒〜数分）を許容** → **Amazon Data Firehose**。変換が要るなら Firehose に Lambda 変換を組み込む（別途Lambdaを自前で並べない）。
  - **秒未満のレイテンシ／複数コンシューマ／再処理／順序保証** → **Kinesis Data Streams**
  - **ウィンドウ集計・異常検知・ストリーム同士の結合など「複雑なストリーム処理」** → **Managed Service for Apache Flink**
  - **既存のKafkaをそのまま移す（リプラットフォーム）** → **MSK**、スループットが読めない/運用を減らすなら **MSK Serverless**
  - **リアルタイム時系列を大画面ダッシュボードで見る** → Flink → Timestream → Grafana（Managed Grafana）の並び

- **紛らわしい選択肢との切り分け**:

  | | Kinesis Data Streams | Data Firehose | MSK |
  |---|---|---|---|
  | 位置づけ | 保持つきストリームバッファ | 配信（ETLロード）サービス | Kafka互換ブローカー |
  | 保持・再読み込み | できる（既定24h〜最大365日） | できない（バッファのみ） | できる（保持期間設定） |
  | コンシューマ複数 | できる（拡張ファンアウトで専有帯域） | 1配信先＋バックアップ | コンシューマグループ |
  | スケール | シャード管理（またはオンデマンド） | 自動 | ブローカー/パーティション管理（Serverlessなら自動） |
  | 選ぶ合図 | 「再処理」「複数チーム」「秒未満」 | 「S3/Redshift/OpenSearchへ入れたいだけ」 | 「既存Kafka」「Kafka APIのまま」 |

- **ひっかけパターン**:
  - 「リアルタイムに集約・フィルタリングして異常検知」に **Firehose→S3→Athena** を混ぜる。Athena はバッチであり "リアルタイム" 要件を満たさない。
  - トレース収集に Kinesis や MSK を提案する（これらは分散トレーシングの機能を持たない。トレースは OpenTelemetry / X-Ray）。
  - 「運用負荷最小でOpenSearchへ変換して入れる」に、Lambda を自前で並べる構成を混ぜる（Firehose の変換で足りる）。

- **派生して問われうる点**: Firehose の動的パーティショニング、Firehose のバックアップ先S3、KDS のオンデマンドモード、MSK Connect、Flink のチェックポイント/セーブポイント。

---

## 3. Kinesis のシャード設計・ホットシャード・スロットリング

- **問われ方**: 「`WriteThroughputExceeded` / `ProvisionedThroughputExceededException` が出る」「一部のシャードだけ負荷が高く、他はアイドル」。

- **正解を選ぶ判断基準**:
  - **「一部のシャードに偏っている」と明記 → 原因はパーティションキーの偏り。答えは「パーティションキーを高カーディナリティ／ランダム（UUID等）に変える」**。
  - **偏りの記述がなく、単に全体が足りない → シャード数を増やす（またはオンデマンド）**。
  - **コンシューマ側で `ReadProvisionedThroughputExceeded` / 読み取り競合 → 拡張ファンアウト**。

- **紛らわしい選択肢との切り分け**:

  | 症状 | 誤りやすい対処 | 正しい対処 |
  |---|---|---|
  | ホットシャード（偏り） | シャード数を増やす | パーティションキーの見直し |
  | 全体的なスループット不足 | パーティションキー変更 | シャード追加／オンデマンド |
  | コンシューマが増えて読み取りが遅い | シャード追加 | 拡張ファンアウト |
  | レコードが大きすぎる | シャード追加 | 集約（KPL）／レコード分割 |

- **ひっかけパターン**:
  - 「シャードを増やす」は**もっともらしいが偏りは解消しない**という点をほぼ毎回突いてくる。偏りの記述があるかどうかだけで判定する。
  - 「レコードサイズを小さくする」も偏り問題には無効。
  - 順序保証が要件に書かれている場合はランダムキーが使えないので、その時は選択肢の文言をよく読む（順序要件があるならキー設計の再検討＋シャード追加になる）。**要確認**：この分岐が出題される頻度は低い。

- **派生して問われうる点**: 拡張ファンアウトのコンシューマ数上限、PutRecord と PutRecords の違い（バッチ、部分失敗の扱い）、KPL/KCL、リシャーディング。

---

## 4. ストリームの再処理・重複排除・exactly-once

- **問われ方**: 「変換ロジックの不具合が判明したので過去N日分を作り直したい」「ネットワーク障害があるので正確に1回の配信にしたい」。

- **正解を選ぶ判断基準**:
  - **Kinesis / Kafka は at-least-once。「exactly-once を実現したい」と言われたら、サービス設定ではなく "アプリケーション側で冪等にする"** が答え。具体的には **レコードにユニークIDを持たせて重複排除**。
  - **過去分の流し直しが要件 → 保持期間の延長**（KDSは最長365日まで延長可）。
  - **リアルタイム処理を止めずに再処理 → 再処理用のコンシューマを別に登録（拡張ファンアウト）**。
  - **二重集計を防ぐ → 処理済み位置/IDを DynamoDB 等に記録した冪等コンシューマ**。

- **紛らわしい選択肢との切り分け**:

  | 要件 | 対応レイヤー |
  |---|---|
  | 過去に遡れる | ストリームの保持期間 |
  | 本番を止めずに並行再処理 | コンシューマの分離（拡張ファンアウト） |
  | 二重計上しない | アプリの冪等性（ユニークID／進捗記録） |

- **ひっかけパターン**: 「Kinesis の設定で exactly-once を有効にする」風の選択肢。そんな設定は存在しない。同様に「Firehose で重複排除する」も存在しない。

- **派生して問われうる点**: DynamoDB を進捗ストアにする設計、Flink のチェックポイントによる状態復元、SQS FIFO の重複排除ID（こちらは実在する機能なので混同しない）。

---

## 5. ETL 実行エンジンの選定（Glue Spark / Glue Python shell / DataBrew / EMR / Lambda）

- **問われ方**: 「ETLジョブを構築したい」＋「最小の開発作業」or「最もコスト効率よく」。

- **正解を選ぶ判断基準**:
  - **GUIで前処理・クレンジング・列操作、コードを書かない、アナリストが操作する** → **AWS Glue DataBrew**（または Glue Studio のデータ準備レシピ変換）
  - **数百MB以下の小さなファイルを pandas 程度で処理、Sparkは過剰** → **Glue Python shell ジョブ**（コスト効率の合図）
  - **分散処理が必要な規模のSpark ETL、サーバーレス** → **Glue Spark ジョブ**
  - **Hadoopエコシステム（Pig / Oozie / HBase / Flink / Hive など）を含む既存資産をそのまま動かす** → **Amazon EMR**
  - **数分未満の軽量な単発処理、イベント駆動** → Lambda

- **紛らわしい選択肢との切り分け**:

  | | DataBrew | Glue Studio | Glue Spark job | Glue Python shell | EMR |
  |---|---|---|---|---|---|
  | コード | 不要（レシピ） | ほぼ不要（ビジュアル） | 要（PySpark/Scala） | 要（Python） | 要 |
  | 対象規模 | 中規模まで | 大 | 大 | 小 | 特大 |
  | 主な使い手 | アナリスト | データエンジニア | データエンジニア | データエンジニア | データエンジニア |
  | 選ぶ合図 | 「コードを書かずに」「プロファイリング」「PII検出」 | 「ビジュアルETL」 | 「Spark」「大規模変換」 | 「小さいファイル」「pandas」「コスト効率」 | 「Pig/HBase/Oozie」「既存Hadoop」 |

- **ひっかけパターン**:
  - 小さなCSVを日次で数十件処理するだけの要件に **Glue Spark ジョブ**や **EMR** を混ぜる（動くがコスト効率で負ける）。
  - 「コードを書かずに」と明記されているのに **Lambda + pandas** を混ぜる。
  - 「PigやHBaseを含む」と書いてあるのに Glue を選ばせようとする（Glue は Spark ベースであり Hadoop エコシステム全部は動かない）。

- **派生して問われうる点**: DataBrew の代表的変換名（`NEST_TO_MAP` で複数列をキー/値のマップ1列へ、`COUNT_DISTINCT` 集計、列の連結・分割）、DataBrew のプロファイルジョブ、EMR Serverless、Glue のワーカータイプ選定。

---

## 6. Glue ジョブの性能・コスト最適化

- **問われ方**: 「Glueジョブが遅い」「`No space left on device` で失敗する」「`diskSpaceUsed_MB` が上昇」「コストを下げたい」。

- **正解を選ぶ判断基準**:

  | 症状・要件 | 打ち手 |
  |---|---|
  | 大量の**小さいファイル**を読むので遅い | **DynamicFrame の `groupFiles` / `groupSize`** でまとめ読み |
  | 大きなDataFrame同士の**シャッフルで容量不足**（`No space left on device`） | **Glue Spark シャッフルマネージャーで S3 にシャッフルを書く** |
  | 負荷変動でドライバのディスクが逼迫、処理が詰まる | **Glue のオートスケーリングを有効化** |
  | **実行時刻を問わない**バッチのコストを下げたい | **FLEX 実行クラス** |
  | 前回以降の**増分だけ**処理したい | **ジョブブックマーク** |
  | 特定の重い変換が繰り返される | パーティション/ファイル形式の見直し、キャッシュ |

- **紛らわしい選択肢との切り分け**:
  - `groupFiles`（**入力の小ファイル問題**） vs シャッフルマネージャー（**中間データの容量問題**）。エラーメッセージがディスク不足かどうかで切る。
  - FLEX（**遅くてもいいバッチ**専用。SLAのあるジョブには使えない） vs オートスケーリング（**負荷変動に追随**）。
  - ジョブブックマーク（**Glue内蔵の増分管理**） vs 自前でS3のタイムスタンプを比較するLambda（コーディング作業が増えるので「最小限のコーディング」では負ける）。

- **ひっかけパターン**:
  - 小ファイル問題に「ワーカータイプを大きくする」「ワーカー数を増やす」を提示する。ファイル数がボトルネックなので効果が薄く、コストだけ増える。
  - 「追加のコストをできるだけかけずに」と書いてあるのにスケールアップ系を選ばせる。
  - FLEX を「レイテンシ要件のあるジョブ」に当てさせる。

- **派生して問われうる点**: ワーカータイプ（G.1X / G.2X / G.4X / G.8X）の選び方、Glue のバージョンと Spark バージョン、DynamicFrame と DataFrame の相互変換、パーティション数の調整。

---

## 7. オーケストレーションの選定（Glue workflows / Step Functions / MWAA / EventBridge）

- **問われ方**: 「複数ジョブを順序どおりに実行したい」「手動起動をやめたい」＋「追加料金なし」「最小リファクタリング」など。

- **正解を選ぶ判断基準**:
  - **Glueジョブ・クローラーだけで完結し、追加のサービス料金を増やしたくない** → **AWS Glue workflows**（Glue内蔵、追加料金なしが決め手）
  - **Glue以外のサービス（EMR、Lambda、SageMaker、外部API等）をまたぐ／分岐・並列・待機が要る** → **AWS Step Functions**
  - **既存のApache Airflow DAG がある／Python・Bashスクリプトをそのまま流したい／リファクタリング不可** → **Amazon MWAA**
  - **「起動のきっかけ」だけが問題（S3到着、ジョブ完了、失敗検知）** → **EventBridge**

- **紛らわしい選択肢との切り分け**:

  | | Glue workflows | Step Functions | MWAA |
  |---|---|---|---|
  | 対象 | Glueジョブ/クローラー/トリガーのみ | ほぼ全AWSサービス | 何でも（Airflowオペレータ次第） |
  | 追加料金 | なし（Glue利用料のみ） | 状態遷移ごと課金 | 環境の時間課金（常時費用） |
  | 分岐・並列・待機 | 条件トリガー程度 | Choice/Parallel/Map/Wait | DAGで自由 |
  | 選ぶ合図 | 「Glueだけ」「追加料金なし」「最もコスト効率」 | 「EMRとGlueをまたぐ」「複雑な制御フロー」 | 「既存Airflow」「Python/Bashをそのまま」 |

- **ひっかけパターン**:
  - **Glueジョブだけのパイプライン**に Step Functions / MWAA を当てさせる。動くが「追加のサービス料金を増やさず／最もコスト効率よく」で負ける。
  - **既存Airflow DAGがある**のに Step Functions を選ばせる（DAGの全面書き換え＝リファクタリングが発生する）。
  - MWAA を「サーバーレス」扱いする選択肢。MWAA は環境が常時稼働するので、断続的なワークロードのコスト最小要件では不利。

- **派生して問われうる点**: Glue トリガー（スケジュール／条件／オンデマンド／EventBridgeイベント）、Step Functions の Standard と Express の違い、MWAA のDAG配置先（S3）とプラグイン。

---

## 8. Step Functions のステート選択

- **問われ方**: 「このワークフロー要件を満たすステートタイプはどれか」。

- **正解を選ぶ判断基準**:

  | 要件の言い回し | ステート |
  |---|---|
  | 受信データの**内容に応じて**実行するステップを変える／条件分岐 | **Choice** |
  | **件数が変動する多数のアイテム**に**同じ処理**を適用（ファイル群、レコード群） | **Map**（大量なら分散マップ） |
  | **異なる処理**を**同時並行**で走らせて合流 | **Parallel** |
  | 外部システムの応答を**長時間待つ**（人手承認、外部API照会） | **Task + Wait for Callback（`.waitForTaskToken`）** |
  | 一定時間待つ | **Wait** |

  - **「長時間の外部照会を順番に3段階」→ Standard ワークフロー + Wait for Callback**。ここは「Lambdaを待たせ続ける」構成に対して、待ち時間に課金されない点でコストが効く。

- **ひっかけパターン**:
  - Map と Parallel の混同。**同じ処理×可変個数 = Map**、**異なる処理×固定個数 = Parallel**。
  - 長時間待機に Express ワークフローを選ばせる（Express は実行時間上限が短く、待機主体には不向き）。
  - 「各段階が前段の結果に依存する」＝順次実行なので Parallel は不正解。

- **派生して問われうる点**: Standard（最長1年、正確に1回、履歴あり）と Express（短時間・高頻度、at-least-once）の使い分け、エラーハンドリング（Retry / Catch）、Step Functions の実行ロールに必要な権限。

---

## 9. イベント駆動の起動方式（S3イベント通知 / EventBridge / Lambda / SNS）

- **問われ方**: 「ファイルが到着したら処理を始めたい」「ジョブの失敗を通知したい」「できるだけ早く処理したい」。

- **正解を選ぶ判断基準**:
  - **単一の宛先（Lambda / SQS / SNS）を叩くだけ、フィルタも拡張子程度** → **S3イベント通知**（`s3:ObjectCreated:*` ＋ サフィックスフィルタ）
  - **複数宛先へファンアウト、他サービスのイベントも扱う、Glueワークフローを直接起動したい** → **EventBridge**
  - **Glueワークフローをファイル到着で起動** → S3のEventBridge通知を有効化 → EventBridgeルール → **イベントベースのGlueトリガー**
  - **ジョブの失敗を通知** → **Glue/Step Functionsの失敗イベントを拾うEventBridgeルール → ターゲットにSNSトピックを直接指定**（間にLambdaを挟まない）
  - **失敗イベントの原因調査** → **DLQ（デッドレターキュー）を設定して失敗イベントを保全**

- **紛らわしい選択肢との切り分け**:

  | | S3イベント通知 | EventBridge |
  |---|---|---|
  | 宛先 | Lambda / SQS / SNS / EventBridge | 多数（Glue、Step Functions、SNS、Lambda…） |
  | フィルタ | プレフィックス／サフィックス | イベントパターンで柔軟 |
  | 選ぶ合図 | 「.csvがアップされたときだけ」 | 「Glueワークフローを起動」「複数の宛先」 |

- **ひっかけパターン**:
  - 「できるだけ早く処理したい」に**スケジュール実行（cron / EventBridge Scheduler）**を混ぜる。到着時刻が読めない前提なら必ず**イベント駆動**が正解。
  - 通知経路に不要な Lambda を挟む選択肢（運用負荷が増えるので負ける）。
  - EventBridge **Scheduler**（時刻起動）と EventBridge **ルール**（イベント起動）の取り違え。問題文が「予測できない時間にアップロード」なら Scheduler は誤り。

- **派生して問われうる点**: EventBridge のリトライとDLQ、`FailedInvocations` メトリクスの読み方、S3イベント通知の遅延特性、SNSのサブスクリプションフィルタ。

---

## 10. データ品質チェック（Glue Data Quality / DQDL / DataBrew）

- **問われ方**: 「nullや参照整合性の破れを検知したい」「しきい値ベースのルールを評価したい」「不完全なら通知したい／上書きを止めたい」。

- **正解を選ぶ判断基準**:
  - **Glue ETLパイプラインの中で品質を検査** → **Glue ETLジョブの「データ品質の評価（Evaluate Data Quality）」変換 + DQDL ルールセット**
  - **アナリストがGUIで品質ルールを作る／プロファイリングしたい** → **DataBrew のプロファイルジョブ + ルールセット**
  - **標準ルールにないカテゴリ（自社独自のPII書式など）を検出** → **DataBrew の正規表現ベースのカスタムルール**、または **Glue Data Quality のカスタムルールセット**
  - **品質NGを検知して通知** → 品質結果を **EventBridge** に発行 → **SNS**

- **代表的なDQDLルール（覚える）**:

  | ルール | 意味 | 使う場面 |
  |---|---|---|
  | `IsComplete "col"` | その列にnullがない | null混入の検知 |
  | `Completeness "col" > 0.99` | 非null率のしきい値 | 「99%以上」系の要件 |
  | `ReferentialIntegrity "a,b" "table.{x,y}" = 1.0` | 複合列の値がマスタに完全に存在する | 参照整合性・マスタ突合 |
  | `ColumnValues "col" between X and Y` | 値域チェック | 「0円超10万円以下」等 |
  | `Uniqueness` / `RowCount` / `ColumnCount` | 一意性・件数・列数 | スキーマ変化や空ファイル検知 |

- **ひっかけパターン**:
  - 「参照整合性」なのに `IsComplete` だけの選択肢、逆に「nullチェック」なのに `ReferentialIntegrity` だけの選択肢。**要件が2つあるなら2種類のルールが両方入っているものを選ぶ**。
  - 品質チェックを Lambda で自作させる選択肢（運用負荷で負ける）。
  - 「複数データセットに一括適用」が要件のとき、単一データセット向けの手段を選ばせる。

- **派生して問われうる点**: 品質スコアのしきい値による分岐（合格時のみ後続へ）、Glue Data Quality の推奨ルール自動生成、品質結果の保存先。

---

## 11. データカタログとメタデータ管理（Glue Data Catalog / Schema Registry / DataZone）

- **問われ方**: 「複数データソースのメタデータを一元管理したい」「スキーマ変更を追跡したい」「ビジネス用語集がほしい」。

- **正解を選ぶ判断基準**:
  - **技術メタデータ（スキーマ、形式、パーティション）の中央リポジトリ** → **Glue Data Catalog**（＋クローラーを定期実行して変更を反映）
  - **ストリーミング等でスキーマが進化する。バージョン管理と互換性チェックがほしい** → **Glue Schema Registry**
  - **ビジネスメタデータ、ビジネス用語集（グロッサリ）、データポータルでの検索・購読** → **Amazon DataZone**
  - **既存の Hive メタストアを引き継ぐ／EMRとAthenaで共有** → **Glue Data Catalog を外部メタストアとして使う**（Hiveメタストアを移行）
  - **リレーショナルDBのスキーマ変更履歴を自動追跡** → JDBC接続の **Glueクローラーを定期実行**して Data Catalog へ出力

- **紛らわしい選択肢との切り分け**:

  | | Glue Data Catalog | Glue Schema Registry | DataZone |
  |---|---|---|---|
  | 扱うもの | テーブル定義・パーティション | スキーマのバージョンと互換性 | ビジネス用語・データ資産の公開/購読 |
  | 主な利用者 | Athena / EMR / Redshift Spectrum / Glue | ストリーミングのプロデューサ/コンシューマ | データスチュワード、業務部門 |
  | 選ぶ合図 | 「中央メタデータリポジトリ」「クローラー」 | 「スキーマが四半期ごとに変わる」「バージョン追跡」 | 「ビジネス用語集」「メタデータフォーム」「ポータル」 |

- **ひっかけパターン**:
  - 「ビジネス用語集」が要件にあるのに Glue Data Catalog を選ばせる（Data Catalog は技術メタデータが主）。
  - 「スキーマのバージョンを追跡」に対して、クローラーの再実行だけの選択肢（履歴が残らない）。
  - EMRのHiveメタストアをクラスタ内に残す選択肢（クラスタ終了で消える＝永続性の要件を満たさない）。

- **派生して問われうる点**: クローラーの分類子（Classifier）でカスタム形式を認識させる、クローラーの除外パターン、Data Catalog のパーティションインデックス、Lake Formation との権限連携。

---

## 12. パーティション管理（クローラー / MSCK REPAIR / パーティションプロジェクション / API）

- **問われ方**: 「Athenaがデータを返さない」「レコードが欠落・古い」「新パーティションの登録を自動化したい」「クエリが遅い」。

- **正解を選ぶ判断基準**:

  | 要件 | 打ち手 |
  |---|---|
  | 「データが返らない／レコードが欠落」＋パーティション分割テーブル | **カタログにパーティションが登録されていない**のが原因。`MSCK REPAIR TABLE` で追加。あわせて**テーブルのS3ロケーションが正しいか**確認 |
  | パーティションが日付規則で大量、手動登録をやめてクエリも速くしたい | **パーティションプロジェクション**（テーブルプロパティに範囲とパターンを定義） |
  | 書き込み直後に**最小レイテンシ**でカタログを同期したい | 書き込みコードから **`create_partition` API（Boto3）を直接呼ぶ** |
  | 定期的にスキーマもろとも同期すればよい | **クローラーをスケジュール実行** |

- **紛らわしい選択肢との切り分け**:
  - `MSCK REPAIR TABLE` は**既存の欠損を直す**（既に置かれたパーティションをスキャンして追加）。定常運用の自動化にはプロジェクションかAPIかクローラー。
  - パーティションプロジェクションは**カタログにパーティションを持たない**方式（規則で計算する）。パーティションが極端に多い日付ベースで最も効く。
  - クローラーは**スケジュール実行なのでレイテンシがある**。「最小のレイテンシ」と書かれたらクローラーは負ける。

- **ひっかけパターン**:
  - 「最小レイテンシ」の要件にクローラーの5分ごと実行を当てる。
  - Athenaの空結果に対して「ワークグループの設定」「IAM権限」など見当違いの層を混ぜる（パーティションが定番）。
  - `ALTER TABLE ADD PARTITION` を「自動化」の答えとして混ぜる（手動DDLは自動化ではない）。

- **派生して問われうる点**: パーティションキーの選び方（クエリのWHERE句に合わせる）、パーティションプロジェクションの型（date / integer / enum / injected）、パーティションインデックス。

---

## 13. ファイル形式・圧縮・小ファイル問題

- **問われ方**: 「クエリ性能を上げたい」「スキャン量を減らしたい」「Redshift Spectrum / Athena が遅くなった」。

- **正解を選ぶ判断基準**:
  - **列のサブセットを読む分析クエリ → 列指向（Parquet / ORC）に変換**。これは「性能」と「コスト（スキャン量課金）」の両方に効くので最頻出。
  - **WHERE句で頻繁に絞る列（日付など）→ その列でパーティション分割**。
  - **性能改善の「2つ選択」問題では、"列指向化" と "パーティション分割" のペア**が定番の正解。
  - **同一リージョン配置**（S3とAthena/Redshiftを同じリージョンに）もコスト・性能の選択肢として正解になり得る。
  - **小ファイルが大量 → コンパクション**（Icebergなら `rewriteDataFiles` / bin-pack、Glueなら `groupFiles`）。

- **紛らわしい選択肢との切り分け**:

  | 打ち手 | 効くもの | 効かないもの |
  |---|---|---|
  | Parquet化 | 列の絞り込み、スキャン量、圧縮率 | 行全体を読む処理 |
  | パーティション分割 | WHERE句での枝刈り | 全期間スキャン |
  | 圧縮（gzip/snappy） | 転送量・保存量 | gzipは分割不可なので並列度が落ちる場合あり |
  | コンパクション | ファイル数が多いことによるオーバーヘッド | データ量そのもの |

- **ひっかけパターン**:
  - 「ソース側でParquetを生成させる」vs「取り込み後にParquetへ変換する」。**最小の開発工数**と言われたらソース側で出せるなら出させる方が有利になる場合がある。設問の「開発工数」がどこに掛かっているかを読む。
  - CSVのまま「圧縮するだけ」の選択肢（列の絞り込みは効かない）。
  - 小ファイル問題にワーカー増強を当てる。

- **派生して問われうる点**: gzip/snappy/zstd の分割可能性、Parquet の述語プッシュダウン、ORCとParquetの使い分け（**要確認**：試験では明確な優劣は問われにくい）。

---

## 14. オープンテーブルフォーマット（Iceberg / Delta Lake / Hudi）

- **問われ方**: 「ACIDが必要」「日次スナップショットから差分だけ反映したい（CDC）」「Icebergのクエリが遅くなってきた」。

- **正解を選ぶ判断基準**:
  - **データレイクで ACID / UPSERT / タイムトラベル が必要** → Iceberg（またはDelta / Hudi）
  - **ソースが毎日フルスナップショットしか出さない → テーブルフォーマットでMERGE（挿入＋更新）**。差分を自前で計算する構成より効率的。
  - **Icebergの性能劣化 → コンパクション（`rewriteDataFiles` / bin-pack）を短い間隔で定期実行**。Glue Data Catalog の自動コンパクションを使う手もある。
  - **読み取り重視 → copy-on-write、書き込み重視 → merge-on-read**。クエリ性能改善が要件なら **copy-on-write** 側。

- **紛らわしい選択肢との切り分け**:

  | | copy-on-write | merge-on-read |
  |---|---|---|
  | 書き込み | 重い（ファイル書き直し） | 軽い（差分ファイル追記） |
  | 読み取り | 速い | 遅い（マージが必要） |
  | 選ぶ合図 | 「クエリ性能を改善」 | 「書き込みが頻繁で取り込み遅延を減らしたい」 |

- **ひっかけパターン**:
  - Iceberg性能問題に「パーティションを増やす」「ワーカーを増やす」を当てる。原因は**小ファイルの蓄積とスナップショットの肥大**。
  - ACID要件のある「サーバーレスでコスト効率よく」に Redshift や EMR を当てる（Athena + Iceberg が有力）。

- **派生して問われうる点**: スナップショットの期限切れ（expire snapshots）、マニフェストの書き換え、Athena / Glue / EMR / Redshift Spectrum からの Iceberg 読み書き対応状況（**要確認**：対応範囲は更新が速い）。

---

## 15. Athena の設定と運用

- **問われ方**: 「クエリがキューに入る」「コストを制御したい」「実行計画と各操作のコストを見たい」「空のテーブルを作りたい」。

- **正解を選ぶ判断基準**:

  | 要件 | 打ち手 |
  |---|---|
  | クエリがキュー待ちする／同時実行を確保したい | **ワークグループにプロビジョニングされた容量（キャパシティ予約）を設定** |
  | スキャン量を上限で制御したい／部署別に課金を分けたい | **ワークグループのデータ使用量の制限** |
  | 実行計画＋**実際の実行統計/コスト**を見たい | **`EXPLAIN ANALYZE SELECT ...`**（計画だけなら `EXPLAIN`） |
  | 同じ列定義で**空**のテーブルを作りたい | **`CREATE TABLE ... AS (SELECT * FROM src) WITH NO DATA`** |
  | S3以外（RDS/DynamoDB等）も同じSQLで引きたい | **フェデレーテッドクエリ**（Lambdaコネクタ経由） |
  | 複数の異種ソースを最小運用負荷でSQL統合 | **Glueでクロールしてカタログ化 → Athena**（JSONはSerDe/JSON関数） |

- **ひっかけパターン**:
  - キューイングに対して「ユーザーをワークグループに追加」「結果の制限を引き上げ」など**リソース量と無関係な設定**を混ぜる。キューの原因は常に**コンピュート不足**。
  - `EXPLAIN ANALYZE` の構文を崩した選択肢（`SELECT` を省いたもの等）。**構文が正しいか**まで見る。
  - フェデレーテッドクエリを性能改善策として提示する（むしろ遅くなる可能性がある）。

- **派生して問われうる点**: Athena のワークグループ別の結果出力先とKMS設定、CTAS でのフォーマット/パーティション指定、Athena for Spark（**要確認**）。

---

## 16. Redshift の物理設計（分散スタイル・ソートキー）

- **問われ方**: 「1ノードだけCPUが高い」「巨大テーブル同士のJOINが遅い」「分散キーに適した列がない」。

- **正解を選ぶ判断基準**:

  | 状況 | 選ぶ分散スタイル |
  |---|---|
  | 特定ノードに偏り／JOINが遅い、**結合列がある** | **KEY 分散**（**高カーディナリティの結合列**を選ぶ） |
  | 小さなディメンションテーブル、全ノードで結合したい | **ALL 分散** |
  | 均等に散らしたいだけ、明確な結合列なし | **EVEN 分散** |
  | **適した分散キー列がない／メンテナンス負担を最小に** | **AUTO 分散**（Redshiftに任せる） |

  - 「ノード間の偏り」の原因は**分散キーのカーディナリティが低い**こと。**分散キーを高カーディナリティ列に変更**が定番の答え。
  - 巨大テーブル同士のJOIN高速化は **両テーブルに同じ結合列でKEY分散**（＝コロケーション）。
  - あわせて **Redshift Advisor** の推奨確認が「2つ選択」のもう一方になりやすい。

- **ソートキーの扱い**:
  - 単一列で範囲検索が中心 → 複合（compound）ソートキー
  - 複数列を等しい頻度で条件に使う → インターリーブド（interleaved）ソートキー
  - **インターリーブドは更新を重ねると劣化する → `VACUUM REINDEX`**

- **VACUUM の使い分け**:

  | コマンド | 何をする | 選ぶ合図 |
  |---|---|---|
  | `VACUUM FULL` | 領域回収＋ソート | 一般的な回収 |
  | `VACUUM DELETE ONLY` | 削除行の領域回収のみ | ソート不要 |
  | `VACUUM SORT ONLY` | ソートのみ | 領域は足りている |
  | `VACUUM REINDEX` | **インターリーブドソートキーの再インデックス＋領域回収＋ソートキー値の分布分析** | 「インターリーブド」＋「ディスク回収」＋「分布を分析」の3点セット |

- **ひっかけパターン**:
  - 「ノードを追加する」「クラスタを増やす」を混ぜる。問題文に「現在のノード数を維持したい」「追加のクラスターを増やさずに」と書かれているので即切り。
  - `ANALYZE`（統計更新）と `VACUUM`（領域回収）の混同。「ディスク領域を回収」なら VACUUM。
  - 分散キーに**低カーディナリティ列（地域、ステータス等）**を選ばせる。

- **派生して問われうる点**: `ANALYZE` と統計、圧縮エンコーディング（`ANALYZE COMPRESSION`）、WLM とキュー、Concurrency Scaling。

---

## 17. Redshift のロード／アンロード／クエリ高速化

- **問われ方**: 「COPYが遅い」「定期的にS3へ出力したい」「変換クエリが数時間かかる」「JSONをどう入れるか」。

- **正解を選ぶ判断基準**:

  | 要件 | 打ち手 |
  |---|---|
  | 複数の場所に散ったファイルを1テーブルへ**速く**ロード（コストは増やさない） | **マニフェストファイルを作って COPY を1回**（場所ごとにCOPYを繰り返さない） |
  | 定期的にRedshift→S3へデータを出す | **UNLOAD**（Glueジョブ＋Glueトリガー等でスケジュール） |
  | 同じ重い集計が繰り返される | **マテリアライズドビュー**で事前計算 |
  | マテビューを定期的に更新したい（最小の労力） | **Query Editor v2 のスケジュール機能で `REFRESH MATERIALIZED VIEW`** |
  | JSON値を含む列を手間なく取り込む | **SUPER データ型**（＋PartiQL） |
  | S3のファイル数が非常に多く小さい | ロード前に結合／Parquet化 |
  | ロード完了をトリガに後続処理 | **Redshift Data API で実行 → 完了イベントが EventBridge → Lambda** |

- **ひっかけパターン**:
  - JSONに対して「Lambdaでフラット化してからロード」を混ぜる。**「手間をかけずに」なら SUPER 型**。
  - マテビュー更新に Lambda + スケジューラを自作させる（Query Editor v2 のスケジュールで足りる）。
  - Redshift の完了検知に「Lambdaでポーリング」を混ぜる。Data API + EventBridge が正解。

- **派生して問われうる点**: 自動マテビュー更新（AUTO REFRESH）、COPY の圧縮ファイル対応、`COPY` の IAM ロール指定、Data API の非同期実行と結果取得。

---

## 18. Redshift のスケーリング形態とデータ共有

- **問われ方**: 「月1回だけ重い処理」「ETL用とBI用でクラスタが別」「クラスタを自分で管理したくない」。

- **正解を選ぶ判断基準**:
  - **利用が断続的・予測不能／クラスタ管理をやめたい** → **Redshift Serverless**
  - **別クラスタ間でデータを共有したいが、提供側のコンピュートを消費したくない・データをコピーしたくない** → **Redshift データ共有（datashare）**。提供側がプロデューサ、利用側がコンシューマ。
  - **データメッシュで中央ガバナンス経由で共有** → **Redshift datashare を作り、それを Lake Formation カタログへ共有**（そのうえでLake Formationで列/行レベル制御）
  - **S3のデータを移動せずRedshiftから引く** → **Redshift Spectrum**

- **紛らわしい選択肢との切り分け**:

  | | datashare | UNLOAD/COPYでコピー | Spectrum |
  |---|---|---|---|
  | データの重複 | なし | できる | なし |
  | 提供側の負荷 | ほぼなし | あり | – |
  | 選ぶ合図 | 「重要処理を中断させない」「コンピュート使用を最小に」 | – | 「S3のデータをそのまま」 |

- **ひっかけパターン**:
  - 「データの重複を作ってはいけない」と書いてあるのに、コピーやETLで別テーブルを作る選択肢。
  - データメッシュ問題で **RAM で直接Redshift datashareを部門アカウントへ共有**する選択肢。**列レベル制御が要件なら Lake Formation を経由させる**必要がある。
  - Serverless が常に正解とは限らない。**継続的に高負荷が読める**なら予約インスタンスのプロビジョンドが有利になる場合がある。

- **派生して問われうる点**: RPU の考え方、Redshift ML（SQLからモデル作成＋推論、既存SageMakerエンドポイントの呼び出し）、Redshift のクロスリージョンスナップショット。

---

## 19. Redshift のセキュリティ（RBAC / 動的マスキング / 行レベル）

- **問われ方**: 「役割ごとに見える範囲を変えたい」「一部ユーザーは下4桁だけ、監査は全桁」「多数のユーザーとロールを管理したい」。

- **正解を選ぶ判断基準**:
  - **職務役割ごとにスキーマ/テーブル単位の権限を管理** → **Redshift RBAC（ロールベースアクセス制御）**
  - **同じ列を、見る人によって違う形（マスク/一部表示/全表示）で見せる。データは重複させない** → **動的データマスキング（DDM）ポリシー**をロール/ユーザーごとに割り当て
  - **行を絞る** → 行レベルセキュリティ（RLS）
  - **S3データレイク側で行/列を絞る** → **Lake Formation のデータフィルター**

- **ひっかけパターン**:
  - 「データの重複を作らない」要件に対し、**マスク済みの別テーブル／別ビューを作る**選択肢。ビューを大量に作る案は運用負荷でも負ける。
  - Redshift 内のデータに Lake Formation の行レベルセキュリティを当てる（Lake Formation は主にS3データレイク/カタログ側。Redshiftはdatashare経由でカタログに載せる必要がある）。
  - IAM ポリシーで列単位を制御しようとする選択肢（IAMはテーブル内部の列/行までは見ない）。

- **派生して問われうる点**: マスキングポリシーの適用順、`GRANT ROLE`、Redshift のフェデレーテッド認証、監査ログ（監査ログのS3出力）。

---

## 20. Lake Formation によるきめ細かいアクセス制御とデータメッシュ

- **問われ方**: 「部門ごとに自分の行だけ」「PII列は見せない」「クロスアカウント/クロスドメインで共有したい」「監査したい」。

- **正解を選ぶ判断基準**:
  - **S3データレイクで行レベル/列レベル/セル単位の制御を最小運用負荷で** → **Lake Formation**
  - 手順のセットで問われたら **①S3パスをLake Formationのデータレイクロケーションとして登録 → ②きめ細かいアクセス制御（データフィルター）を設定** の2ステップが正解の組み合わせになりやすい。
  - **多数のテーブル・多数のチーム・クロスドメイン分析＋PII制限＋監査** → Lake Formation の集中権限管理。
  - **ビジネス用語集やデータの公開・購読のワークフロー**まで求められたら DataZone 側。

- **紛らわしい選択肢との切り分け**:

  | 制御手段 | 粒度 | 選ぶ合図 |
  |---|---|---|
  | IAM / バケットポリシー | バケット・プレフィックス | 「バケット単位で分ける」 |
  | Lake Formation | DB / テーブル / 列 / 行 / セル | 「行レベル」「列レベル」「最小運用負荷」 |
  | S3 プレフィックス分割 + IAM | プレフィックス | 「部門ごとにパスを分けられる」設計変更が許される場合 |
  | Redshift RBAC / DDM | Redshift内部 | データがRedshiftにある |

- **ひっかけパターン**:
  - 行レベル制御を **プレフィックスを分けてIAMで制御**する案に誘導する。データが単一パスに混在している前提だと成立しないうえ、運用負荷が高い。
  - 「S3をLake Formationに登録する」ステップを飛ばした選択肢（登録しないと権限が効かない）。
  - バケットポリシーだけで列レベルを実現しようとする選択肢。

- **派生して問われうる点**: LF-Tags（タグベースアクセス制御）、クロスアカウント共有（RAM連携）、Lake Formation と IAM の権限の重なり、Data Catalog のリソースリンク。

---

## 21. 暗号化（S3 / KMS / EMR）

- **問われ方**: 「暗号化データが読めない（AccessDenied）」「2層の暗号化が必須」「特定の従業員だけがキーを使える」「保存時と転送時の両方」。

- **正解を選ぶ判断基準**:
  - **SSE-KMS のオブジェクトを読めない → 実行ロールに `kms:Decrypt` がない**（S3の権限だけでは足りない）。書き込みには `kms:GenerateDataKey` も要る。
  - **キーを使える人を絞りたい** → **SSE-KMS のカスタマー管理キー + キーポリシー/IAMポリシーで限定**
  - **2層のサーバーサイド暗号化が義務** → **DSSE-KMS**
  - **EMRで保存時＋転送時の暗号化** → **1つの「セキュリティ設定」にKMSキー（at-rest）とPEM証明書のS3パス（in-transit）をまとめて指定し、クラスタ作成時に渡す**
  - **転送中のTLSバージョン強制（Transfer Family）** → サーバーのセキュリティポリシーで最低TLSバージョンを指定

- **紛らわしい選択肢との切り分け**:

  | 方式 | キー管理 | 選ぶ合図 |
  |---|---|---|
  | SSE-S3 | AWS任せ | 「暗号化さえされていればよい」 |
  | SSE-KMS（AWS管理キー `aws/s3`） | キーポリシー編集不可 | 特定ユーザーに絞る要件がない |
  | SSE-KMS（カスタマー管理キー） | キーポリシー編集可 | 「特定の従業員だけ」「監査」「ローテーション制御」 |
  | DSSE-KMS | 二重 | 「2層」「dual-layer」が明記 |
  | SSE-C / クライアントサイド | 自前 | 「AWSにキーを渡さない」 |

- **ひっかけパターン**:
  - AccessDenied の原因として「リージョンが違うのでレイテンシで失敗」「メモリ不足」など**別レイヤーの尤もらしい説明**を並べる。**問題文で既に付与済みと書かれた権限は原因ではない**。
  - S3バケットポリシーの不備を原因に挙げる（実行ロールにS3権限を付与済みと明記されている場合は誤り）。
  - EMRで「保存時用」と「転送時用」の設定を**別々のセキュリティ設定に分ける**選択肢。1つにまとめる。

- **派生して問われうる点**: S3 バケットキーによるKMSリクエスト削減、KMSのキーポリシーとIAMポリシーの併用、クロスアカウントでのKMS権限、EMRのローカルディスク暗号化（LUKS）。

---

## 22. IAM とサービス間の権限（実行ロール / リソースベースポリシー / PassRole / IRSA）

- **問われ方**: 「AccessDeniedException が出る」「EventBridgeからLambdaを呼べない」「クロスアカウントでKinesisを読みたい」「コンテナに認証情報を埋め込みたくない」。

- **正解を選ぶ判断基準**:

  | 状況 | 見るべき場所 |
  |---|---|
  | **AがBを呼び出せない**（EventBridge → Lambda、S3 → Lambda） | **呼ばれる側（B）のリソースベースポリシー** |
  | AがBのデータを読めない（Lambda → S3/KMS） | **呼ぶ側（A）の実行ロールのIAMポリシー** |
  | **クロスアカウント**でストリーム/バケットを読ませる | **提供側のリソースベースポリシー**（＋利用側のIAM） |
  | サービスに別のロールを渡す（SageMaker → Glue、Glue → 他） | **`iam:PassRole`** |
  | EKSのPodにAWS権限を与える、認証情報を埋め込まない | **IRSA（IAM Roles for Service Accounts）** |
  | リージョンごとに権限を絞りたいが、ロールは増やしたくない | **1つのロール + セッションポリシー**を `AssumeRole` 時に渡す |

- **ひっかけパターン**:
  - 「EventBridgeがLambdaを呼べない」に対し、**Lambdaの実行ロールの信頼ポリシー**を確認させる選択肢。実行ロールはLambdaが**他サービスを呼ぶ**ための権限であり、**呼ばれる**側の制御はリソースベースポリシー。この左右の取り違えが定番。
  - クロスアカウントで「利用側にIAM権限を付けた」と書いてあるのに、さらに利用側をいじる選択肢。**足りないのは提供側**。
  - EKSで「ワーカーノードのインスタンスロールに全部付ける」だけの選択肢（Pod単位の分離ができない。IRSA を伴う選択肢が正解になりやすい）。

- **派生して問われうる点**: 信頼ポリシー（誰がAssumeできるか）vs 許可ポリシー（何ができるか）、権限境界、Secrets Manager での認証情報管理、STSの一時認証情報。

---

## 23. ネットワーク（VPCエンドポイント / セキュリティグループ / Lambda in VPC）

- **問われ方**: 「プライベートサブネットのLambdaがS3へアップロードするとタイムアウト」「QuickSightからRedshiftへ繋がらない」「インターネットを経由してはいけない」。

- **正解を選ぶ判断基準**:
  - **VPC内リソース → S3 / DynamoDB へ、インターネットを使わず、コスト効率よく** → **ゲートウェイVPCエンドポイント**（追加料金なし。ルートテーブルにルートを追加）
  - **VPC内リソース → その他のAWSサービス（KMS、Glue、SNS等）をプライベートに** → **インターフェースVPCエンドポイント（PrivateLink、時間課金あり）**
  - **NAT Gateway** は動くが**コストで負ける**（ゲートウェイエンドポイントが使える相手なら常にそちらが有利）
  - **接続元がAWSサービス/別VPCで、SGのインバウンドが原因** → **接続先のSGに、接続元のSGをソースとして指定したインバウンドルールを追加**
  - **オンプレからインターネットを経由しない** → Direct Connect（または VPN）

- **ひっかけパターン**:
  - S3へのタイムアウトに **Lambdaのタイムアウト値やメモリを増やす**選択肢。原因は**経路がないこと**。
  - SG の設定で「アウトバウンドを許可する」だけの選択肢。SGはステートフルなので**接続先のインバウンド**を開ける必要がある。
  - NAT Gateway を「最もコスト効率の良い」答えとして混ぜる。

- **派生して問われうる点**: SGとネットワークACLの違い（ステートフル/ステートレス）、VPCフローログでの疎通確認、Lambda を VPC に置いたときのインターネット到達性、EMRクラスタのSGとデータソース疎通。

---

## 24. S3 ストレージクラスとライフサイクル

- **問われ方**: 「N日は即時アクセス、その後はまれに、さらに後は◯時間以内でよい、最後は削除」「保存コストを下げたい」「WORM/改変不可」。

- **正解を選ぶ判断基準**:
  - 要件文の**アクセス頻度と取り出し許容時間**を順に読み、対応するクラスへ移行するライフサイクルを組む。
    - **即時アクセスが必要** → Standard（または Standard-IA / Glacier Instant Retrieval）
    - **まれにアクセスするが即時に必要** → **Standard-IA**（低頻度＋即時）
    - **「◯時間以内にアクセスできればよい」（1〜12時間）** → **Glacier Flexible Retrieval**
    - **ほぼ取り出さない、長期保管** → Glacier Deep Archive
    - **アクセスパターンが読めない** → Intelligent-Tiering
  - **WORM／変更不可／全バージョンを保持** → **S3 Object Lock**（＋バージョニング）＋ライフサイクル
  - **一定期間後に削除** → ライフサイクルの**有効期限（Expiration）ルール**

- **ひっかけパターン**:
  - 「5時間以内にアクセス」に **Deep Archive** を当てる（標準取り出しに半日規模かかる）。
  - 「即時アクセスが必要」なのに Glacier Flexible Retrieval を当てる。
  - 削除の自動化に Lambda + スケジューラを自作させる（ライフサイクルで足りる）。
  - Intelligent-Tiering を「常に最適」として選ばせる。**アクセスパターンが明確に書かれている問題では、明示的なライフサイクルの方が安い**。

- **派生して問われうる点**: 最小保存期間の課金（下記数値編）、小さいオブジェクトの扱い、バージョニングと非現行バージョンのライフサイクル、レプリケーション。

---

## 25. DynamoDB の設計

- **問われ方**: 「テーブル全体をスキャンせずに◯◯で引きたい」「古いデータを自動削除したい」「バーストするトラフィックで1桁ミリ秒」。

- **正解を選ぶ判断基準**:
  - **既存テーブルのキーと違う属性で引きたい／後から追加したい** → **GSI（グローバルセカンダリインデックス）**。「パーティションキーを◯◯、ソートキーを△△にしたGSI」という形が正解。
  - **有効期限による自動削除、稼働中の読み書きを妨げない** → **TTL**
  - **トラフィックが予測できない／急増する** → **オンデマンドキャパシティ**
  - **変更をストリームで下流に流したい** → DynamoDB Streams

- **紛らわしい選択肢との切り分け**:

  | | GSI | LSI |
  |---|---|---|
  | パーティションキー | 変更できる | ベーステーブルと同じ |
  | 作成タイミング | いつでも | **テーブル作成時のみ** |
  | 整合性 | 結果整合性のみ | 強い整合性も可 |
  | 選ぶ合図 | 「別の属性で検索」「後から追加」 | 「同じPKで別のソート順」 |

- **ひっかけパターン**:
  - 「スキャンせずに」の要件に **Scan + FilterExpression** を当てる（フィルタはスキャン後に適用されるので読み取り量は減らない）。
  - 既存テーブルに LSI を追加する選択肢（後から追加できない）。
  - 自動削除に「Lambdaを定期実行して削除」を当てる（TTLで足りる、かつ書き込みキャパを消費しない）。

- **派生して問われうる点**: パーティションキーのカーディナリティとホットパーティション、書き込みシャーディング、項目サイズと課金単位、DAX（キャッシュ）。

---

## 26. EMR のコスト・性能最適化

- **問われ方**: 「CPUは張り付くがメモリは余っている」「長時間ジョブを安く」「クラスタを消してもデータを残したい」。

- **正解を選ぶ判断基準**:
  - **CPU使用率が高くメモリが余る → コンピューティング最適化インスタンス**（逆ならメモリ最適化）。**ノードを増やす前にインスタンスタイプを見直す**。
  - **同等性能でコストを下げる → Graviton インスタンス**
  - **クラスタと独立してデータを永続化 → S3（EMRFS）をストア**にする（HDFSにため込まない）
  - **中断耐性のある処理 → タスクノードにスポット**。コアノードはスポットにしない（データ喪失リスク）。
  - **メタストアを永続化・共有 → Glue Data Catalog を外部メタストアに**
  - **クラスタ管理をやめる → EMR Serverless**

- **紛らわしい選択肢との切り分け**:

  | ノード種別 | 役割 | スポット可否 |
  |---|---|---|
  | プライマリ | クラスタ管理 | 基本不可 |
  | コア | HDFSデータを保持＋処理 | 非推奨 |
  | タスク | 処理のみ | 推奨 |

- **ひっかけパターン**:
  - リソース不均衡（CPU逼迫／メモリ余剰）に対し「スケールアウトの上限を上げる」を当てる。無駄なノードが増えてコストが悪化する。
  - 「長期間データを保持しながら分析」に HDFS のままの構成を当てる。
  - Glueで代替できない要件（Pig/HBase/Oozie/Flink併用）に Glue を当てる。

- **派生して問われうる点**: EMRマネージドスケーリング、インスタンスフリート、EMRのセキュリティ設定、EMR on EKS（**要確認**：出題頻度は低め）。

---

## 27. 可視化（QuickSight）

- **問われ方**: 「ダッシュボードで権限エラーが出る」「コスト効率よく多人数に見せたい」「ドリルダウンしても総計を出したい」「VPC内のRedshiftへ繋げたい」。

- **正解を選ぶ判断基準**:
  - **多人数・日次更新・コスト効率 → SPICE に取り込み、更新スケジュールを設定**（毎回のダイレクトクエリはスキャン課金と負荷が増える）
  - **常に最新が必要 → ダイレクトクエリ**
  - **ドリルダウンの階層に依存しない集計値を出す → LAC-A（レベル認識計算の集計）関数**
  - **権限エラーの原因 → QuickSight自身のサービスロールに、S3へのアクセス権と、暗号化されている場合の KMS 復号権限があるか**（2つ選択問題で両方が正解になりやすい）
  - **VPC内のリソースへ接続 → QuickSightのVPC接続、かつ接続先SGにQuickSight側SGからのインバウンドを許可**

- **ひっかけパターン**:
  - 権限エラーの原因に「ユーザーの権限」「データセットの共有設定」など**QuickSightサービス側ではない層**を混ぜる。S3+KMS のセットを疑う。
  - 「コスト効率よく」と書いてあるのにダイレクトクエリを選ばせる。
  - LAC-A（集計のレベルを指定）と LAC-W（ウィンドウ関数のレベル）の取り違え。**要確認**：LAC-W が問われる頻度は低い。

- **派生して問われうる点**: SPICE の更新頻度と容量、Enterprise Edition の機能（行レベルセキュリティ、VPC接続）、埋め込みダッシュボード。

---

## 28. 監視とトラブルシューティング（CloudWatch メトリクスの読み方）

- **問われ方**: 「原因を特定するには何を見るか」「アラームにどう対処するか」「最小の運用労力で監視したい」。

- **覚えるべきメトリクスと対応**:

  | メトリクス | サービス | 意味 / 対応 |
  |---|---|---|
  | `CDCLatencySource` | DMS | **ソースDB側**のCDC遅延。ソースが原因かを切り分ける |
  | `CDCLatencyTarget` | DMS | ターゲット側の遅延 |
  | `WriteProvisionedThroughputExceeded` / `WriteThroughputExceeded` | Kinesis | 書き込みスロットリング（偏りかキャパ不足） |
  | `IncomingBytes` / `OutgoingBytes` | Kinesis | 取り込み/読み出し量 |
  | `DeliveryToS3.Success` | Firehose | S3配信の成否 |
  | `KafkaDataLogsDiskUsed` | MSK | **ブローカーのデータログ用ディスク逼迫** → ストレージ拡張＋ストレージ自動スケーリング有効化 |
  | `glue.driver.BlockManager.disk.diskSpaceUsed_MB` | Glue | ドライバのディスク使用量上昇 → **オートスケーリング有効化**（またはシャッフルのS3外出し） |
  | `FailedInvocations` | EventBridge | ターゲット呼び出し失敗 → **DLQ を設定して失敗イベントを保全し原因分析** |

- **一般的な監視要件の答え方**: 「最小の運用労力でパイプラインのヘルスを監視」→ **CloudWatch の主要メトリクスにアラームを設定**。カスタムの監視基盤を作る選択肢は負ける。

- **ひっかけパターン**:
  - MSKのディスク逼迫に「ブローカー数を増やす」「保持期間だけを縮める」を当てる。**まずストレージ拡張＋自動スケーリング**。
  - Glue のディスク逼迫に「SNSのスループットを上げる」など**症状が出ている場所（SNSの遅延）を直そうとする**選択肢。遅延の**原因**はGlue側。
  - DMSのレイテンシ原因特定で、ソースDB側のログを手動で見に行く選択肢（メトリクスで切り分けられる）。

- **派生して問われうる点**: CloudWatch Logs Insights、X-Ray、EventBridge のリトライ設定、Glue ジョブの継続的ロギング。

---

## 29. 可観測性（ログとトレースの相関）

- **問われ方**: 「コンテナ基盤のログとトレースを相関させて障害点を特定したい」「最小限の開発作業で」。

- **正解を選ぶ判断基準**:
  - **収集**：ログ = **Fluent Bit**（K8sのDaemonSet）、トレース = **OpenTelemetry / ADOT**
  - **分析・相関**：**Amazon OpenSearch Service**（サービスマップ、トレース分析、トレースIDでの相関）
  - **2つ選択**なら「収集の手段」＋「相関・分析の基盤」の**組み合わせ**が正解。

- **ひっかけパターン**:
  - トレース収集に Kinesis / MSK / CloudWatch を当てる。これらは**トレースの収集・可視化・分析機能を持たない**。
  - 相関を Lambda で自作する選択肢（「最小限の開発作業」に反する）。

- **派生して問われうる点**: X-Ray との使い分け、OTLP、CloudWatch Container Insights。

---

## 30. PII の検出とマスキング

- **問われ方**: 「共有前にPIIを検出してマスクしたい」「独自のPIIカテゴリがある」「一部チームは生データを見る必要がある」。

- **正解を選ぶ判断基準**:

  | 要件 | サービス |
  |---|---|
  | **S3上のデータからPIIを自動検出**（スキャン、機械学習ベース） | **Amazon Macie** |
  | ETL/前処理の中でPIIをマスクする | **Glue DataBrew**（マスキング変換）／Glue の PII検出変換 |
  | **標準カテゴリにない独自のPII書式**（証券番号、加入者番号等） | **DataBrew の正規表現ベースのカスタムルール**（複数データセットへ一括適用） |
  | Redshift内で見る人ごとに表示を変える | **動的データマスキング** |
  | 生データへのアクセスを一部チームに限定 | **IAM（＋KMSキーポリシー）** |

  - 「マスクする」＋「一部チームには生データを許可」の2つ選択は、**マスキングの手段（DataBrew等）** と **アクセス制御（IAM）** の組み合わせになる。同じ層の対策を2つ選ばない。

- **ひっかけパターン**:
  - 「検出」が要件なのにマスキング手段だけの選択肢、「マスキング」が要件なのに検出だけの選択肢。
  - Macie を ETL の変換手段として使わせる（Macie は検出・分類が主）。**要確認**：Macieの自動マスキング機能の有無は最新仕様を確認すること。
  - 独自カテゴリに対して「標準のデータ品質ルールを使う」選択肢。

- **派生して問われうる点**: Macie の検出結果と Security Hub / EventBridge 連携、Glue の PII 検出変換の粒度（列全体 / セル単位）。

---

## 31. レコードの名寄せ・重複排除（FindMatches）

- **問われ方**: 「共通の一意識別子がない」「表記ゆれで同じ人が複数レコードになっている」「フィールド名が異なる」。

- **正解を選ぶ判断基準**:
  - **共通キーがない／ファジーマッチが必要 → AWS Lake Formation の FindMatches ML変換**（Glue ETLジョブに組み込む）。学習用のラベルセットで精度をチューニングする。
  - 前段としてクローラーでスキーマをカタログ化するステップが一緒に出ることがある。

- **ひっかけパターン**:
  - 単純な `DISTINCT` / `GROUP BY` / 完全一致の重複排除を当てる。**表記ゆれがある時点で完全一致では解決しない**。
  - 自前のML（SageMaker）を組む選択肢（運用負荷で負ける）。
  - DataBrew の重複排除を当てる（完全一致の重複には効くが、ファジーマッチではない。**要確認**）。

- **派生して問われうる点**: ラベリングセットの作成、precision/recall のトレードオフ設定、AWS Entity Resolution（**要確認**：DEA-C01の出題範囲に含まれるかは未確認）。

---

## 32. 移行戦略とプラットフォーム選定

- **問われ方**: 「リプラットフォームで移したい」「リファクタリングは避けたい」「既存の◯◯をそのまま」。

- **正解を選ぶ判断基準**:
  - **リプラットフォーム（ほぼそのまま、マネージドへ）**：Kafka → **MSK / MSK Serverless**、Airflow → **MWAA**、Hadoop → **EMR**、Hive メタストア → **Glue Data Catalog**
  - **異種DWH（Teradata / Oracle など）→ Redshift**：**SCT でスキーマ変換 + DMS でデータ移行**
  - **ダウンタイム最小**：DMS のフルロード + CDC

- **ひっかけパターン**:
  - 「リプラットフォーム戦略を採用したい」と明記されているのに、**Kinesis へ書き換える**などリファクタリングを伴う選択肢を混ぜる。
  - SCT を使わずに DMS だけでスキーマまで移行させる選択肢（異種DB間ではスキーマ変換が必要）。
  - EC2上に自前でKafka/Airflowを立てる選択肢（管理オーバーヘッドで負ける）。

- **派生して問われうる点**: DMS のフルロード/CDC/フルロード+CDC の使い分け、SCT の評価レポート、MSK Serverless と プロビジョンドの選択。

---

## 33. データ整合性の検証（チェックサム）

- **問われ方**: 「転送中に破損していないことを検証したい」「オンプレとS3のログが同一であることを監査要件で確認したい」。

- **正解を選ぶ判断基準**:
  - **アップロード時にチェックサムを計算し、リクエストに含める／オブジェクトメタデータに保存する**。S3が受信側で検証する。
  - 目視・手動比較や、独自に再ダウンロードして比較する選択肢は「独立した検証」「運用効率」で負ける。
  - アルゴリズムは SHA-256 / SHA-1 / CRC32 / CRC32C などから選べる（**強度が要件なら SHA-256、速度重視なら CRC32**）。

- **ひっかけパターン**:
  - 「S3のバージョニングを有効にする」「レプリケーションを設定する」など、**整合性検証ではない機能**を混ぜる。
  - ETag をMD5として使う前提の選択肢（マルチパートアップロードではETagはMD5と一致しない）。**要確認**：この論点が直接問われるかは未確認だが、ETagベースの選択肢は疑ってよい。

- **派生して問われうる点**: マルチパートアップロードとチェックサム、DataSync の転送検証機能。

---

## 34. Lambda の運用（Layers / 同時実行 / VPC）

- **問われ方**: 「共通スクリプトを多数の関数で使い回したい」「低レイテンシ応答が必須」「VPC内のLambdaが外部へ出られない」。

- **正解を選ぶ判断基準**:
  - **共通コード/ライブラリを一元管理し、更新の手間を減らす** → **Lambda Layers**
  - **呼び出し頻度が低いが低レイテンシ必須（コールドスタート回避）** → **プロビジョンドコンカレンシー**
  - **急増するトラフィックの立ち上がりを滑らかに** → Provisioned Concurrency + Application Auto Scaling（**要確認**）
  - **VPC内 → S3** → ゲートウェイVPCエンドポイント（論点23）

- **ひっかけパターン**:
  - 共通コードの配布に「S3に置いて起動時にダウンロード」「各関数にコピー」を当てる（更新の手間が減らない）。
  - 低レイテンシ要件に「メモリを増やす」だけの選択肢（コールドスタートは残る）。
  - 15分を超える処理をLambdaでやらせる選択肢（時間上限を超える）。

- **派生して問われうる点**: Layers の個数・サイズ上限、コンテナイメージLambda、予約済み同時実行との違い。

---

## 35. SQS のメッセージ喪失対策

- **問われ方**: 「コンシューマがダウンしている間にメッセージが消えた」「データ損失を最小限にしたい（2つ選択）」。

- **正解を選ぶ判断基準**:
  - **保持期間の延長**（既定より長くして、ダウンタイム中の期限切れを防ぐ）
  - **デッドレターキュー（DLQ）のアタッチ**（処理に繰り返し失敗したメッセージを退避）
  - この2つのペアが定番の正解。

- **ひっかけパターン**:
  - 可視性タイムアウトの調整を当てる（**重複処理**には効くが、**期限切れによる喪失**には効かない）。
  - FIFOキューへの変更を当てる（順序と重複排除の話であり、保持期間の問題ではない）。

- **派生して問われうる点**: 可視性タイムアウトとポーリング、SQS と SNS のファンアウト、Lambda のイベントソースマッピング。

---

## 36. Athena / Glue / SQL の細かい構文

- **問われ方**: 「このクエリがエラーになる理由／修正方法は」「この目的に使うSQL文は」。

- **正解を選ぶ判断基準**:
  - **存在しない列を WHERE で使っている** → 実データの列から導出する式に直す（例：日付列から年を取り出す `EXTRACT(year FROM date_col) = 2023`）。パーティション列でない限り、勝手に `year` は存在しない。
  - **空のテーブルを同じ列定義で作る** → `CREATE TABLE ... AS SELECT ... WITH NO DATA`
  - **実行計画＋実測コスト** → `EXPLAIN ANALYZE SELECT ...`

- **ひっかけパターン**:
  - **構文が微妙に壊れた選択肢**を混ぜる（`EXPLAIN ANALYZE FROM t;` のように `SELECT` が抜けている等）。意味だけでなく構文の妥当性を確認する。
  - 集計関数を別のものに置き換えて「エラーは消えるが要件を満たさない」選択肢。

- **派生して問われうる点**: パーティション列を WHERE で使うことによる枝刈り、Presto/Trino 系関数、SerDe とJSON関数。

---

## 37. 分析ワークロードの置き場所（Athena vs Redshift vs OpenSearch）

- **問われ方**: 「どのサービスでクエリさせるか」＋「コスト効率」「全文検索」「ACID」。

- **正解を選ぶ判断基準**:

  | 要件 | 選ぶ |
  |---|---|
  | アドホック・低頻度・サーバーレス・S3上のデータ | **Athena** |
  | S3上でACIDとパーティションを両立、サーバーレス、コスト効率 | **Athena + Iceberg（オープンテーブルフォーマット）** |
  | 高頻度・継続的なBI、複雑なJOIN、多数の同時ユーザー | **Redshift**（断続的なら Redshift Serverless） |
  | **複雑な全文検索** | **OpenSearch Service** |
  | 時系列のリアルタイム監視 | Timestream（+ Grafana） |
  | 1桁ミリ秒のキーバリュー参照 | DynamoDB |

- **ひっかけパターン**:
  - 「全文検索」の要件に Athena や Redshift を当てる。**全文検索と書かれたら OpenSearch**。
  - 週に数回のアドホック分析に Redshift プロビジョンドクラスタを常時稼働させる。
  - 「ACIDを維持」に単純なAthena+CSVを当てる（テーブルフォーマットが必要）。

- **派生して問われうる点**: Redshift Spectrum と Athena の使い分け、フェデレーテッドクエリ、複数ソース横断SQL（Glueでカタログ化してAthena）。

---

## 38. 機械学習との接続

- **問われ方**: 「SQLから予測したい」「既存のSageMakerエンドポイントを使いたい」「2つ選択」。

- **正解を選ぶ判断基準**:
  - **Redshiftのデータでモデルを作り、SQLで推論** → **Redshift ML（`CREATE MODEL`）**
  - **既にデプロイ済みのSageMakerリアルタイムエンドポイントがある** → **Redshift ML からそのエンドポイントを呼び出す**（モデルを作り直さない）
  - 2つ選択の場合、「新規にRedshift MLでモデルを作る」＋「既存エンドポイントをSQLから呼ぶ」の**両方**が並ぶ構成になりやすい。

- **ひっかけパターン**:
  - 既存エンドポイントがあるのに「SageMakerで再学習する」選択肢。
  - データをS3へエクスポートしてバッチ推論する選択肢（リアルタイム要件に反する）。

- **派生して問われうる点**: SageMaker と Glue インタラクティブセッションの権限（`AwsGlueSessionUserRestrictedServiceRole` + `iam:PassRole`）、Glue の FindMatches（論点31）。

---

## 39. スキーマ進化への対応

- **問われ方**: 「四半期ごとにファームウェア更新でデータ構造が変わる」「スキーマが未定義または運用中に変わる」。

- **正解を選ぶ判断基準**:
  - **スキーマのバージョン追跡と互換性チェック** → **Glue Schema Registry**
  - **スキーマの自動検出** → **Glue クローラー**（定期実行）
  - **半構造化のまま取り込んで後で解釈** → Redshift の **SUPER 型**、Athena の JSON SerDe
  - 組み合わせとして「Data Catalog でテーブル管理 + Schema Registry でバージョン管理 + Glue で取り込み + Redshift Serverless へロード」という**フルマネージドの束**がコスト効率の答えになりやすい。

- **ひっかけパターン**:
  - スキーマ変更のたびに手動でテーブル定義を更新する選択肢。
  - Schema Registry を「メタデータカタログ」として使わせる（役割が違う）。

- **派生して問われうる点**: 互換性モード（BACKWARD / FORWARD / FULL）、クローラーのスキーマ変更ポリシー（更新する/無視する）。

---

## 40. クローラーの除外とクエリ範囲

- **問われ方**: 「除外したはずのファイルまでクエリで読まれてスキャン量が増える」。

- **正解を選ぶ判断基準**:
  - **クローラーの除外パターンは「カタログに登録しない」だけ**であり、**テーブルのS3ロケーション配下にファイルが存在すればクエリは読んでしまう**。
  - よって答えは **物理的に別プレフィックス（別の場所）へ分離する**。

- **ひっかけパターン**:
  - 除外パターンを書き直す／クローラーを再実行する選択肢。カタログの問題ではないので解決しない。
  - Athena側でWHERE句で絞る選択肢（スキャン量は減らない）。

- **派生して問われうる点**: テーブルのLOCATIONとスキャン範囲の関係、パーティションによる枝刈り、S3のプレフィックス設計。

---

# 付録A：数値で覚えるもの

数値問題そのものより、「この上限があるからこの選択肢は成立しない」という**消去法の根拠**として使う。

## Kinesis Data Streams
- シャードあたり書き込み：**1 MB/秒 または 1,000 レコード/秒**
- シャードあたり読み取り（共有）：**2 MB/秒**、GetRecords は **5回/秒**
- **拡張ファンアウト**：コンシューマごとに **2 MB/秒/シャード**の専有帯域
- レコード最大サイズ：**1 MB**
- `PutRecords`：1リクエストあたり最大 **500 レコード / 5 MB**
- 保持期間：既定 **24時間**、延長で **7日**、長期保持で最大 **365日**
- 拡張ファンアウトのコンシューマ数：ストリームあたり **20**（**要確認**：上限は緩和されている可能性あり）

## Amazon Data Firehose
- バッファサイズ／バッファ間隔でトリガー（S3宛はサイズ **1〜128 MB**、間隔 **60〜900秒** が代表値）
- → 「秒未満のレイテンシ」要件を Firehose では満たせない、という判断に使う
- **要確認**：バッファ間隔の下限は宛先やアップデートにより変わる（0秒設定が可能な構成あり）

## AWS Lambda
- 最大実行時間：**15分**
- メモリ：**128 MB 〜 10,240 MB**
- `/tmp`：**512 MB 〜 10 GB**
- デプロイパッケージ：zip **50 MB**（直接アップロード）／展開後 **250 MB**
- **Layers：関数あたり 5 つまで**、展開後の合計 250 MB に含まれる
- ペイロード：同期 **6 MB**、非同期 **256 KB**

## AWS Glue
- ワーカー：**G.1X = 4 vCPU / 16 GB**、**G.2X = 8 vCPU / 32 GB**（G.4X、G.8X もあり）
- **Python shell ジョブ：0.0625 DPU または 1 DPU**（小さい処理に使う根拠）
- 課金は秒単位、**最小課金時間あり**（**要確認**：Sparkジョブは1分、Python shellも同様とされるがバージョンにより差異）
- **FLEX 実行クラス**：スペアキャパシティを使うため安いが**開始時刻・所要時間が保証されない**
- クローラーのスケジュール最短間隔：**5分**（**要確認**）

## Amazon S3
- 単一オブジェクト最大：**5 TB**、単一 PUT：**5 GB**
- ライフサイクル：Standard → Standard-IA への移行は**最短30日**
- 最小課金保存期間：**Standard-IA / One Zone-IA = 30日**、**Glacier Instant Retrieval = 90日**、**Glacier Flexible Retrieval = 90日**、**Glacier Deep Archive = 180日**
- 最小課金オブジェクトサイズ：IA系・Glacier系は **128 KB**（小さいオブジェクトが多いとIA化で損をする）
- Intelligent-Tiering：**128 KB未満のオブジェクトは自動階層化の対象外**、監視・自動化の料金がかかる
- 取り出し時間の目安：
  - Glacier Instant Retrieval：**ミリ秒**
  - Glacier Flexible Retrieval：Expedited **1〜5分** / Standard **3〜5時間** / Bulk **5〜12時間**
  - Glacier Deep Archive：Standard **12時間以内** / Bulk **48時間以内**
- → 「5時間以内」「1時間以内」という要件文はこの表と突き合わせる

## Amazon DynamoDB
- 項目の最大サイズ：**400 KB**
- パーティションあたり：**3,000 RCU / 1,000 WCU**、**10 GB**
- **LSI：テーブル作成時のみ、テーブルあたり 5 つ**、パーティションキーはベースと同一
- **GSI：後から追加可、テーブルあたり 20**（既定、緩和申請可）
- TTL：期限切れ後の**削除は即時ではない**（通常48時間以内）。削除は書き込みキャパを消費しない
- **要確認**：TTL削除の所要時間はSLAではなく目安

## Amazon SQS
- メッセージ保持期間：既定 **4日**、**最大14日**
- 可視性タイムアウト：既定 **30秒**、最大 **12時間**
- メッセージ最大サイズ：**256 KB**
- FIFO：**300 TPS**（バッチ利用で 3,000 TPS）（**要確認**：高スループットFIFOで上限が異なる）

## Amazon Redshift
- インターリーブドソートキー：最大 **8 列**
- Concurrency Scaling：**24時間の利用ごとに1時間分の無料クレジット**（**要確認**）
- スナップショットのクロスリージョンコピー：**宛先リージョンでKMSキーを作成し、スナップショットコピーグラントを設定**→**ソースリージョンでスナップショットコピーを有効化しグラント名を指定**（手順の順序が問われる）

## Amazon Athena
- 既定のクエリ実行タイムアウト：**30分**（DML、変更可）（**要確認**）
- 課金：**スキャンしたデータ量**（圧縮・列指向・パーティションが直接コストに効く）
- **プロビジョニングされた容量**：DPU 単位でワークグループに割り当て。**要確認**：最小DPU数と最小課金時間は改定されているため最新値を確認すること（教材内には「最小4 DPU・最小1分」という記述がある一方、従来は最小24 DPU・最小8時間だった）

## Amazon MSK
- ブローカーのストレージ自動スケーリング：使用率がしきい値を超えると自動拡張
- 監視の要は **`KafkaDataLogsDiskUsed`**

## AWS Step Functions
- Standard：最大実行期間 **1年**、**exactly-once**、実行履歴あり
- Express：最大実行期間 **5分**、**at-least-once**、高スループット向け

---

# 付録B：頻出する「組み合わせパターン」

「2つ選択」問題は、**別レイヤーを1つずつ**選ぶのが基本。同じ層の対策を2つ並べた選択肢は誤答に作られていることが多い。

| 問題の型 | 正解の組み合わせ方 |
|---|---|
| クエリ性能改善（S3+Athena/Spectrum） | **ファイル形式（Parquet化）** ＋ **パーティション分割**（または同一リージョン配置） |
| PII保護 | **マスキング手段（DataBrew等）** ＋ **アクセス制御（IAM/KMS）** |
| データメッシュでの共有 | **共有の仕組み（datashare / RAM / LFカタログ）** ＋ **きめ細かい権限（LFデータフィルター）** |
| Lake Formation の導入手順 | **S3をデータレイクロケーションとして登録** ＋ **データフィルターで行/列制御** |
| 可観測性 | **収集（Fluent Bit + OpenTelemetry）** ＋ **相関・分析（OpenSearch）** |
| メッセージ喪失対策 | **保持期間の延長** ＋ **DLQ** |
| EMRコスト最適化 | **インスタンスタイプ最適化（Graviton / compute optimized）** ＋ **ストレージをS3へ外出し** |
| Redshift JOIN高速化 | **両テーブルに同じ高カーディナリティ列でKEY分散** ＋ **Advisorの推奨確認** |
| クロスリージョンスナップショット | **宛先でKMSキー＋コピーグラント作成** ＋ **ソースでスナップショットコピー有効化** |
| トラブル原因の切り分け | **ネットワーク層（SG / VPCフローログ）** ＋ **権限層（IAM / リソースポリシー）** |
| Iceberg性能 | **自動コンパクション** ＋ **copy-on-write** |

---

# 付録C：要確認リスト（断定していない箇所）

試験前に AWS 公式ドキュメントで裏を取るべき項目。教材の記述と一般的な知識が食い違う、または仕様変更が速い領域。

1. **Athena プロビジョニングされた容量の最小DPU・最小課金時間**。教材は「最小4 DPU・最小1分（2026年2月アップデート）」としているが、従来仕様（最小24 DPU・8時間）と大きく異なる。最新のドキュメントで確認すること。
2. **Firehose のバッファ間隔の下限**。宛先やアップデートにより0秒設定が可能な構成がある。
3. **Kinesis 拡張ファンアウトのコンシューマ数上限**（20が既定だが緩和状況を確認）。
4. **Glue ジョブの最小課金時間**（Glueバージョンにより1分／10分の差異があった経緯がある）。
5. **Glue Flex の割引率**と、Flex が使えないジョブ種別。
6. **DynamoDB GSI の既定上限**（20だが緩和申請可）と、TTL削除の所要時間の保証範囲。
7. **SQS FIFO のスループット上限**（高スループットFIFOモードで大きく変わる）。
8. **Redshift Concurrency Scaling の無料クレジット条件**。
9. **Athena のクエリタイムアウト既定値**と変更可否。
10. **Amazon Macie がマスキング（編集）まで行うのか、検出・分類までなのか**。設問では「検出してマスキングできるようにする」という文脈でMacieが正解になっていたが、実際の機能境界を確認すること。
11. **AWS Entity Resolution** が DEA-C01 の出題範囲に入っているか（FindMatches との使い分け）。
12. **DataBrew の重複排除機能**がファジーマッチに対応するか（FindMatches との境界）。
13. **Iceberg の各サービス（Athena / Glue / EMR / Redshift Spectrum）からの読み書き対応範囲**。更新が速い。
14. **QuickSight LAC-W の出題有無**（LAC-A は確認済み）。
15. **Kinesis で順序保証が要件に含まれる場合の、ホットシャード対策の分岐**。ランダムキーは順序を壊すため、その場合の推奨アプローチ。
16. **ORCとParquetの使い分け**が明示的に問われるか。
17. **マルチパートアップロード時のETagとチェックサム検証の関係**。
18. **EMR on EKS / EMR Serverless** の出題比重。
19. **S3 Express One Zone** などの新しいストレージクラスの出題有無。
20. **Glue クローラーのスケジュール最短間隔**。

---

# 付録D：試験当日の解き方の手順

1. **最後の一文（末尾の修飾語）を先に読む**。「最小限の運用負荷」「最もコスト効率よく」「最小限のリファクタリング」のどれかで、正解の方向が半分決まる。
2. **問題文中の「すでに設定済み」を探す**。そこは原因ではない＝その層を触る選択肢は切れる。
3. **数値（件数、サイズ、頻度）は規模感の判定に使う**。「週に数回」「月に1回」ならサーバーレス、「毎秒数万件」なら専用のストリーミング基盤。
4. **選択肢を層で分類する**（ネットワーク層 / 権限層 / データ形式層 / コンピュート層）。2つ選択なら異なる層から1つずつを疑う。
5. **構文が書かれている選択肢は構文の妥当性まで見る**（SQL、DQDLルール、コマンド）。
6. **迷ったら「AWSが用意した専用機能があるか」を考える**。自前実装（Lambdaでロジックを書く、スクリプトを回す）は、専用機能が存在する限りほぼ負ける。
