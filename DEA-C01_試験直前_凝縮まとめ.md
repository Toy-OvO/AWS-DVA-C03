# DEA-C01 試験直前 凝縮まとめ（マーク57問・分野別要点集約版）

> **対象**: 演習1〜4(各65問・計260問)中、**マーク済57問**（不正解35問 + 正解だがマーク済22問）
> **凡例**: ✗=不正解 / 🏷=マーク済(正解したが自信が薄い)
> **本資料の狙い**: 類似問題を分野グループに集約し、各グループの **共通教訓** + 個別問題は **1行要約** に凝縮。
> **DEA特有の注意**: DOPが「そのサービスのどの機能・どのフックか」を問うなら、DEAは **「同じ目的を果たせる複数の正しい構成のうち、どれが要件文の修飾語に合うか」** を問う。選択肢は**全部動く**ことが多い。決め手は末尾の **「最もコスト効率よく」「最小の運用労力」「最小の開発工数」「最短の実行時間」「最も低いレイテンシ」** のどれが書かれているか。
> **注**: 演習5は今回の対象外。

---

## 📊 苦手分野の俯瞰

| ドメイン | 試験比率 | 苦手数 | ✗不正解 | 🏷マーク済のみ | 誤答率 |
|---|---|---|---|---|---|
| **D1 データの取り込みと変換** | 34% | 15問 | 12 | 3 | 80% |
| **D2 データストア管理** | 26% | 18問 | 11 | 7 | 61% |
| **D3 データ運用とサポート** | 22% | 16問 | 8 | 8 | 50% |
| **D4 データセキュリティとガバナンス** | 18% | 8問 | 4 | 4 | 50% |
| **合計** | 100% | **57問** | 35 | 22 | 61% |

**優先度**: 「誤答率 × 試験比率」で **D1 >> D2 > D3 ≒ D4**。

- **D1(誤答12/15)** … **最大配点34%かつ誤答率80%**。演習3を足しても比率が下がらず、むしろ悪化した。ここが唯一にして最大の弱点。落とし方は「KDS と Firehose の役割混同」「マネージド機能があるのに自作構成を選ぶ」「スループット制約の理解不足」の3パターン。
- **D2(誤答11/18)** … 問題数が最多。分散スタイル・パーティション管理・テーブルフォーマットの3本柱で、いずれも**丸暗記が効く**領域。
- **D3** … 不正解8・自信薄8で半々。知識はあるが確信が持てていない状態。判断軸を言語化すれば安定する。
- **D4** … 問題数は少ないが出題は18%ある。Redshift のマスキング、暗号化キーの制御、ネットワーク境界の3点。

---

## 🎯 全体キーワード早見表(頻出のみ)

### ストリーミング取り込み
- **KDS → Redshift をリアルタイムで** = **Redshift ストリーミング取り込み**（外部スキーマ + マテリアライズドビュー）。**Firehose 経由は S3 に一時保存 → COPY の2段階**なので遅く、管理対象も増える
- **KDS → 変換して OpenSearch/S3** = **Firehose + Lambda 変換**（自作コンシューマを書かない）
- **複数チームがそれぞれ専用のスループットで読む** = **拡張ファンアウト（enhanced fan-out）**。EC2 をオートスケールしても**シャードあたり2MB/秒の共有制約は解消しない**
- **高スループットで送信したい** = **`PutRecords`（バッチ送信）**、小レコードの集約なら **KPL**
- **KCL / 自前コンシューマ** = ホスティング・スケーリング・チェックポイント管理が自前 → 「運用負担最小」では**必ず負ける**
- **ウィンドウ集計・時間ベース分析・耐障害性** = **Managed Service for Apache Flink**
- **CloudWatch Logs → 外部SIEM(Splunk等)** = **サブスクリプションフィルター → Firehose**（Lambda を挟まない）

### ETL・変換
- **`No space left on device`** = **Glue Spark シャッフルマネージャーで `--write-shuffle-files-to-s3`**。
  - **特定キーに集中と書かれていたら** ソルティングも併用し、監視は **Spark UI + Glue メトリクス**
  - **Glue に EBS を増設することはできない**（サーバーレスでワーカーのローカル容量は固定）
- **Glue ジョブ再実行で重複** = **ステージングテーブルへロード → SQL で UPDATE/MERGE**
- **毎日フルスナップショットが来る → 差分だけ反映** = **Iceberg / Delta / Hudi の MERGE**（Lambda での差分比較は 15分・10GB の上限で不可）
- **日々フォーマットが変わるデータのスキーマ識別** = **Glue クローラー**（Athena はスキーマ検出ツールではない）
- **Aurora → Redshift をほぼリアルタイム・ソースに影響なし** = **Aurora Zero-ETL 統合**
- **複数の JDBC データベースを横断集計** = **Glue で Redshift へ集約 → QuickSight**（DynamoDB は集計・ジョインが苦手で QuickSight のネイティブ接続先でもない）
- **RDS 全テーブルを分析用 + 1テーブルだけ REST API でリアルタイム** = **DMS タスクを2本**（S3向け / KDS向け）+ Lambda

### Redshift
- **巨大 × 巨大の JOIN** = **両方に KEY 分散（高カーディナリティの結合列）** + **Redshift Advisor**
- **巨大 × 極小の JOIN** = **小さい方に ALL 分散**
- **分散キーに適した列がない / メンテ最小** = **AUTO 分散**
- **同じ重い変換が繰り返される** = **マテリアライズドビューで事前計算**
- **マテビューの更新を最小労力で自動化** = **Query Editor v2 のスケジュール機能**
- **全行削除 + 領域回収を最大** = **`TRUNCATE`**（`DELETE` は VACUUM が別途必要）
- **JSON 値を手間なくロード** = **`SUPER` データ型**
- **別クラスターへ提供側の負荷をかけずに共有** = **Redshift データ共有（datashare）**
- **Spectrum が遅い** = **パーティション分割** ＋ **カラムナー形式（Parquet）**。**50〜70MB のファイルは既に適正サイズなので結合しない**
- **見る人ごとに列の見え方を変える** = **動的データマスキング + データベースロール**
- **多数オブジェクトの部門別権限** = **部門別スキーマ + スキーマ単位 GRANT**

### Athena・パーティション・カタログ
- **クエリプランが遅い（パーティションが大量）** = **Glue パーティションインデックス + パーティションフィルタリング** ＋ **パーティションプロジェクション**
- **スキャン量が多い** = **WHERE 句にパーティションフィルター**（クローラー頻度を上げても減らない）
- **毎日パーティションを作る + スキーマも変わる** = **パーティション射影（プロジェクション）**
- **新パーティションを最小レイテンシでカタログに反映** = **Boto3 の `create_partition` API を書き込みコードから直接呼ぶ**（クローラーはスケジュール実行なので遅い）
- **非パーティションのログテーブルが遅い・運用最小** = **分類子付き Glue Crawler**
- **異種ソース(S3/RDS/DynamoDB/Redshift)を SQL 類似構文で横断** = **Glue でクロール → Athena**（JSON は SerDe と JSON 関数）
- **部署ごとにクエリ実行環境と履歴を分離** = **ワークグループ + タグ + IAM**（Lake Formation では履歴を分離できない）

### テーブルフォーマット・ストア選定
- **Iceberg のクエリが遅い** = **`rewriteDataFiles`（bin-pack コンパクション）を短い間隔でスケジュール実行**。**1日1回では毎時5,000ファイル環境に追いつかない**。あわせて **copy-on-write**
- **1桁ミリ秒 + スパイク + セカンダリ属性検索** = **DynamoDB オンデマンド + GSI**
- **Neptune のクエリ言語** = **Gremlin（プロパティグラフ）** と **SPARQL（RDF）**

### 運用・監視・接続
- **パイプラインのヘルス監視・最小労力** = **CloudWatch アラーム**（`IncomingBytes` / `OutgoingBytes` / `DeliveryToS3.Success`）
- **EMR ジョブのオーケストレーション** = **Step Functions**。**Glue workflows は EMR を制御できない**
- **Python と Bash をそのまま / OSS / ポータビリティ** = **MWAA**
- **1日数回しか呼ばれないが低レイテンシ必須** = **Lambda + プロビジョンドコンカレンシー**
- **大型スクリーンでのリアルタイム監視** = **Flink → Timestream → Grafana**
- **同一 SG 内で通信できない** = **ソースに SG 自身を指定したインバウンドルール**
- **S3 ゲートウェイエンドポイントのエラー** = **ルートテーブルのルート**
- **ドリルダウンしても上位の集計値を固定** = **QuickSight の LAC-A**
- **参照整合性の検証** = **`ReferentialIntegrity "pref,city" "area_master.{master_pref,master_city}" = 1.0`**（比率は **0.0〜1.0**。`= 100` は成立しない）

### セキュリティ・ガバナンス
- **特定の従業員だけが使えるキーで暗号化** = **SSE-KMS（カスタマー管理キー）+ キーポリシー/IAM で限定**。**SSE-S3 はキーを個別制御できない**ので不可。CloudHSM は要件がなければ過剰
- **EMR の保存時 + 転送時暗号化** = **1つのセキュリティ設定にまとめてクラスター作成時に渡す**
- **リージョンごとに権限を絞るがロールは増やさない** = **1ロール + セッションポリシー**
- **Redshift/S3 の通信を AWS 環境内に閉じる** = **プライベートサブネット + インターフェイス VPC エンドポイント** ＋ **拡張 VPC ルーティング + Direct Connect**
- **ビジネス用語集・ビジネスメタデータ・データポータル** = **Amazon DataZone**（Glue Data Catalog は技術メタデータのみ）

---

## 🚚 D1: データの取り込みと変換 (15問) ⚠️ 最大配点34%・誤答率80%

### G1-1. ストリーミング経路の選択 (5問) ⚠️ 最頻出・全滅しやすい

**共通教訓**:
- **「宛先」で機械的に切る。**
  - 宛先が **Redshift** かつ「リアルタイム」→ **Redshift ストリーミング取り込み**（外部スキーマ + マテビュー）
  - 宛先が **S3 / OpenSearch / Splunk** かつ「変換あり」→ **Firehose + Lambda 変換**
  - 宛先が「集計結果」「異常検知」→ **Managed Service for Apache Flink**
- **Firehose → Redshift は "直接配信" ではない**。内部で **S3 に書いて COPY** する2段階。バッファ間隔を詰めても中間 S3 と IAM ロールの管理が残るため「運用負担最小」では負ける。**要確認**: バッファ間隔の下限は宛先により異なる。
- **送信側と読み取り側は別の問題として切る。**

  | 要件の言い回し | 打ち手 |
  |---|---|
  | 高いスループットで送信したい | **`PutRecords`（バッチ）** |
  | 小さいレコードでシャード効率を上げたい | **KPL（集約 + リトライ）** |
  | **複数チームがそれぞれ専用のスループットで読む** | **拡張ファンアウト** |
  | コンシューマの処理能力を上げたい | ワーカー増強（ただし読み取り帯域は増えない） |

- **拡張ファンアウトが要件の問題では、EC2 のオートスケーリングが罠**。処理能力は上がるが、**シャードあたり2MB/秒を全コンシューマーで共有する制約は解消されない**。「専用のスループット」という語を見たら拡張ファンアウト一択。
- **自作コンシューマ（KCL / Lambda を並べる / EC2・ECS でホスト）は「運用負担を最小」では常に誤答**。
- **Firehose は KDS への送信手段ではない**（出口であって入口ではない）。
- **時間ウィンドウ（◯分にわたる集計）と書かれたら Flink 一択**。要件文にない機能（重複処理など）を語る選択肢は、Flink を使っていても不正解。

**問題ごとの1行要約**:
| 問題 | 教訓 |
|---|---|
| [✗] 演習1-問題35 | KDS→Redshift でリアルタイム+エンリッチ = **Redshift ストリーミング取り込み**。Firehose は S3 経由 COPY で不利 |
| [✗] 演習1-問題57 | KDS→変換→OpenSearch = **Firehose の Lambda 変換**。KCL で自作は運用負担で負ける |
| [✗] 演習2-問題10 | 10バイト × 毎秒1万件でシャード効率最大 = **KPL**。Firehose は KDS への送信手段ではない |
| [✗] 演習3-問題55 | 高スループット送信 + 3チームが専用帯域 = **`PutRecords`** ＋ **拡張ファンアウト**。EC2 増強では帯域制約は消えない |
| [🏷] 演習4-問題37 | 最大30分の**時間ウィンドウ**集計 + 耐障害性 = **Managed Service for Apache Flink** |

---

### G1-2. ログ・DBからの取り込み経路 (4問)

**共通教訓**:
- **CloudWatch Logs から外部へ出す** = **サブスクリプションフィルター → Firehose → 宛先**。**Lambda を挟む選択肢は運用の手間が増えるだけ**。
- **Aurora / RDS → Redshift でソースに影響を与えず継続連携** = **Zero-ETL 統合**。DMS でも実現できるが CDC タスク・マッピング・監視の設定が残る。
- **複数の運用 DB を横断して分析・可視化** = **Glue で Redshift へ集約 → QuickSight**。
  - **DynamoDB は集計・ジョインが苦手**で、**QuickSight のネイティブ接続先にも含まれない**（Athena のフェデレーテッドクエリを別途用意することになる）。「可視化手段は合っているが、その前段の格納先が違う」というタイプの誤答に注意。
- **要件が2つに分かれていたらタスクも2本**。「全テーブルを分析用に」＋「1テーブルだけリアルタイムで REST API」→ **DMS タスクを2つ（S3向け / KDS向け）** + Lambda。
- **ポーリングはイベント駆動ではない**。「Data API で繰り返しクエリ」「1分ごとに DB を見に行く」は "変更を起点に処理" を満たさない。

**問題ごとの1行要約**:
| 問題 | 教訓 |
|---|---|
| [✗] 演習2-問題5 | CloudWatch Logs → Splunk = **サブスクリプションフィルター + Firehose**。Lambda 併用は不要な手間 |
| [🏷] 演習2-問題50 | Aurora→Redshift で本番影響なし+運用最小 = **Zero-ETL 統合 + マテビューで結合** |
| [✗] 演習2-問題63 | 全テーブル分析 + 1テーブルだけリアルタイム = **DMS タスク2本（S3 / KDS）+ Lambda** |
| [✗] 演習3-問題3 | 3つの JDBC DB を1時間ごとに横断追跡 = **Glue で Redshift へ + QuickSight**。DynamoDB は分析にも可視化にも不適 |

---

### G1-3. Glue ETL の実行時トラブル (3問) ⚠️ 誤答3/3

**共通教訓**:
- **`No space left on device` = シャッフルの中間ファイルがローカルディスクを食い潰している**。
  - **第一の答えは常に `--write-shuffle-files-to-s3`（Glue Spark シャッフルマネージャー）**。
  - **「特定のキーに集中」と書かれている場合のみ**、スキュー対策として **ソルティング** を併用し、監視は **Spark UI + Glue メトリクス**。
  - **同程度に大きい DataFrame 同士のソートマージ結合**なら、スキューの記述がなくてもシャッフル量そのものが原因。
- **切ってよい選択肢は決まっている。**
  - **ワーカータイプの拡大（垂直スケーリング）** … 偏りもシャッフル量も減らさず、コストだけ増える
  - **EBS ボリュームの追加** … **Glue はサーバーレスでワーカーのローカル容量は固定**。そもそも実行できない架空の操作
- **2つ選択の問題では「監視の手段」＋「対策の手段」が正解ペア**。同じ層の対策を2つ並べた選択肢は疑う。
- **ジョブ再実行での重複** = ロード方式の問題。**ステージングテーブルへ入れてから SQL で既存行を UPDATE（マージ）**。ジョブブックマークは「未処理データを二度読まない」機能であって、**既にターゲットに入った行を直すものではない**。

**問題ごとの1行要約**:
| 問題 | 教訓 |
|---|---|
| [✗] 演習2-問題26 | スキュー起因のディスク枯渇 = **Spark UI + Glue メトリクスで監視** ＋ **`--write-shuffle-files-to-s3` + ソルティング** |
| [✗] 演習3-問題32 | 300GB × 300GB のソートマージ結合で枯渇 = **シャッフルマネージャーで S3 へ書き出す**。EBS 増設は Glue では不可能 |
| [✗] 演習2-問題64 | Glue 再実行で重複 = **ステージングテーブル + SQL で UPDATE**（マージパターン） |

---

### G1-4. カタログ連携込みの取り込み設計 (2問)

**共通教訓**:
- **「スキーマが定期的に変わる」+「カタログを作りたい」+「コスト効率」** = **Glue Data Catalog + Glue Schema Registry + Glue（ワークフロー）+ Redshift Serverless** のフルサーバーレス束。
  - **Lambda UDF は SQL 内で呼ぶスカラー関数**であり、取り込みのオーケストレーション用途ではない。
  - **Glue 自体がスケジューリングとワークフローを持つ**ので、Step Functions を足す構成は開発工数の分だけ不利。
- **オーケストレーターが MWAA でも、実処理は別サービスに投げる**。日次300GB とアーカイブTB級の両方を1つの手段で賄うなら **EMR**、スキーマ識別は **Glue クローラー**。**Athena はスキーマ検出ツールではない**。

**問題ごとの1行要約**:
| 問題 | 教訓 |
|---|---|
| [✗] 演習1-問題2 | スキーマ進化 + カタログ + コスト効率 = **Data Catalog + Schema Registry + Glue + Redshift Serverless** |
| [🏷] 演習2-問題59 | MWAA DAG でスケジュールするのは **EMR での変換** ＋ **Glue クローラーでのスキーマ識別** |

---

### G1-5. スナップショットからの差分取り込み (1問)

**共通教訓**:
- **「ソースが毎日フルスナップショットしか送ってこない」+「変更分だけ反映したい」= オープンテーブルフォーマットの MERGE**（Iceberg / Delta Lake / Hudi）。
  - **Lambda で差分比較する案は上限で詰む**：実行時間15分・メモリ10GB・一時ストレージ10GB。数十TBのファイルは扱えない。
  - **ファイルサイズがKB〜数十TBまで幅がある**という記述は、「単一マシンで処理する案を切れ」というヒント。
- 「CDC」という語が出ても、**ソースがログを出さずスナップショットだけなら DMS の CDC は使えない**。この読み替えができるかが問われる。

**問題ごとの1行要約**:
| 問題 | 教訓 |
|---|---|
| [✗] 演習3-問題35 | 毎日のフルスナップショットから差分反映・最安 = **Iceberg / Delta でマージ（挿入＋更新）** |

---

## 🗄️ D2: データストア管理 (18問) ⚠️ 問題数最多・誤答率61%

### G2-1. Redshift の分散スタイル (3問) ⚠️ 3パターンを丸暗記

**共通教訓**:
- **サイズの組み合わせで機械的に決まる**。

  | 状況 | 選ぶ分散スタイル |
  |---|---|
  | **巨大 × 巨大**（100億件 × 9億件） | **両方に KEY 分散**、結合列は**高カーディナリティ** |
  | **巨大 × 極小**（700GB × 10MB） | **小さい方に ALL 分散** |
  | **適した列がない / 増え続ける / メンテ最小** | **AUTO 分散** |

- **ALL 分散を大きいテーブルに当てるのは常に罠**。全ノード複製でストレージと更新負荷が跳ね上がる。
- **EVEN 分散は結合列を考慮しない**ので JOIN 高速化の答えにならない。
- **2つ選択なら、もう一方は「Redshift Advisor で推奨を確認」**。
  - **「オプティマイザで候補を比較して選ぶ」は存在しない機能**。オプティマイザは自動で動く内部コンポーネント。**架空の機能を混ぜる罠**。

**問題ごとの1行要約**:
| 問題 | 教訓 |
|---|---|
| [✗] 演習1-問題49 | 100億×9億の JOIN = **両方に KEY 分散（高カーディナリティ列）** ＋ **Redshift Advisor** |
| [✗] 演習1-問題51 | 適した分散キー列がない + メンテ最小 = **AUTO 分散** |
| [✗] 演習4-問題2 | 700GB × 10MB の JOIN = **小さいテーブルに ALL 分散** ＋ 主キー/外部キーの指定 |

---

### G2-2. パーティション管理 (4問) ⚠️ 「何が遅いか」で答えが変わる

**共通教訓**:
- **症状を4つに切り分ける。ここを混同すると全部落とす。**

  | 症状・要件 | 原因 | 打ち手 |
  |---|---|---|
  | **クエリプラン**が遅い（パーティションが大量） | カタログからの**メタデータ取得** | **Glue パーティションインデックス + フィルタリング** ＋ **プロジェクション** |
  | **スキャン量**が多い | WHERE で枝刈りしていない | **WHERE 句にパーティションフィルター** |
  | パーティションが**カタログに無い** | 登録されていない | **Glue クローラー** / `MSCK REPAIR` / 射影 |
  | 登録を**最小レイテンシ**で反映したい | クローラーはスケジュール実行 | **Boto3 の `create_partition` API を書き込みコードから直接呼ぶ** |

- **「直後に」「最小のレイテンシで」と書かれたらクローラーは負ける**。5分ごとに回しても最大5分の遅れが出る。書き込み処理の中で API を叩くのが最速。
- **クローラーの実行頻度を上げてもスキャン量は減らない**。役割は登録であって最適化ではない。
- **「毎日パーティションを作る」+「スキーマも変わる」= パーティション射影**。
- **非パーティションのログテーブルを運用最小で改善** = **分類子付き Glue Crawler**。Lambda で Parquet 変換する案は性能には効くが**運用労力で負ける**。

**問題ごとの1行要約**:
| 問題 | 教訓 |
|---|---|
| [✗] 演習4-問題27 | **クエリプラン**が遅い = **パーティションインデックス + フィルタリング** ＋ **プロジェクション** |
| [🏷] 演習4-問題33 | 毎日パーティション作成 + スキーマ変更 = **パーティション射影プロパティ**。DataBrew は無関係 |
| [✗] 演習3-問題39 | 新パーティションを**最小レイテンシ**で同期 = **Boto3 `create_partition` API を書き込みコードから呼ぶ** |
| [✗] 演習2-問題47 | 非パーティションの ALB ログ + 運用最小 = **分類子付き Glue Crawler** |

---

### G2-3. テーブルフォーマットとデータレイクの更新 (3問)

**共通教訓**:
- **S3 上の大きなベーステーブルに日次で少量の差分を当てる** = **テーブルフォーマットの upsert**（Hudi / Iceberg）。EMR + Spark で MERGE。**pandas は 1TB を扱えない**。
- **Iceberg のクエリが遅くなった** = **小さいファイルの断片化**が主因。
  - **`rewriteDataFiles`（bin-pack コンパクション）を短い間隔でスケジュール実行**。
  - **「1日の終わりに1回」では追いつかない**。毎時5,000ファイルが届く環境では、その間ずっと性能が落ち続ける。**頻度を問う選択肢の差**に注意。
  - **外部の Glue ジョブ + クローラーで管理する案も誤答**。Iceberg の組み込み機能を使わず、メタデータの一貫性も保ちにくい。
  - **列統計の生成はオプティマイザの見積もり精度を上げるだけ**で、ファイル断片化には効かない。
- **copy-on-write は読み取り重視、merge-on-read は書き込み重視**。「クエリパフォーマンスを改善」なら CoW。

**問題ごとの1行要約**:
| 問題 | 教訓 |
|---|---|
| [✗] 演習2-問題24 | 1TB のディメンションに日次1万件の更新・最短時間 = **EMR + Apache Hudi の upsert** |
| [🏷] 演習4-問題15 | Iceberg の性能低下 = **自動コンパクション** ＋ **copy-on-write**。列統計は効かない |
| [🏷] 演習3-問題31 | 毎時5,000個の小ファイル = **`rewriteDataFiles` を短い間隔でスケジュール実行**。日次1回では遅い |

---

### G2-4. ストア選定とDDL (4問)

**共通教訓**:
- **「1桁ミリ秒」+「突発的なスパイク」+「可変属性」= DynamoDB オンデマンド**。**セカンダリ属性での検索は GSI**。
  - Aurora は数ミリ〜数十ミリ秒で「1桁ミリ秒を一貫して」は保証できず、固定スキーマなので可変属性にも不利。
- **JSON 値を含む列を手間なく Redshift へ** = **`SUPER` データ型**。
  - **「S3 に置いて Athena でクエリ」は "データウェアハウスへロードする" という要件を満たしていない**。要件文が指定した格納先を勝手に読み替える選択肢に注意。
- **Neptune のクエリ言語は Gremlin（プロパティグラフ）と SPARQL（RDF）**。**要確認**: openCypher も現在はサポートされるため、選択肢の顔ぶれ次第では解答が変わりうる。
- **全行削除 + 領域回収を最大化 = `TRUNCATE`**。`DELETE` は削除マークを付けるだけで **`VACUUM` が別途必要**。

**問題ごとの1行要約**:
| 問題 | 教訓 |
|---|---|
| [✗] 演習4-問題29 | 1桁ms + スパイク + セカンダリ検索 = **DynamoDB オンデマンド + GSI** |
| [🏷] 演習3-問題14 | JSON を手間なく Redshift へ = **`SUPER` データ型**。S3+Athena は要件の読み替えで誤答 |
| [🏷] 演習2-問題62 | Neptune のクエリ言語 = **Gremlin** と **SPARQL** |
| [🏷] 演習2-問題42 | マテビューの全行削除 + 領域回収最大 = **`TRUNCATE`** |

---

### G2-5. Redshift のクエリ高速化と共有 (3問)

**共通教訓**:
- **「複雑な変換クエリが数時間かかる」+「実行時間を短縮」= マテリアライズドビューで事前計算**。
  - **S3 へ移して Athena という案は性能を落とす**。Redshift の列指向ストレージ・圧縮・並列実行の方が大規模分析では速い。「サーバーレスだから安い」に釣られない。
- **別クラスターへデータを渡す** = **Redshift データ共有（datashare）**。
  - 決め手は **「提供側のコンピューティングリソースの使用を最小限に」「重要な分析タスクを中断させない」**。コピーや UNLOAD/COPY は提供側に負荷をかける。
- **Spectrum が遅い** = **パーティション分割** ＋ **カラムナー形式（Parquet）への変更** の2点セット。
  - **ファイルを1つにまとめる案は罠**。Spectrum のベストプラクティスは **1ファイル64MB超で複数に分割**。50〜70MB × 30件は既に適正で、まとめると並列度が落ちる。
  - **列のサブセットを選択するクエリ**という記述が、カラムナー形式が正解であることのヒント。

**問題ごとの1行要約**:
| 問題 | 教訓 |
|---|---|
| [🏷] 演習3-問題20 | 数時間かかる変換クエリ = **マテリアライズドビューで事前計算**。Athena 移行は性能低下 |
| [✗] 演習3-問題52 | ETL クラスターの負荷をかけずに BI クラスターへ共有 = **Redshift データ共有（コンシューマー設定）** |
| [✗] 演習3-問題10 | Spectrum が遅い = **取引日でパーティション分割** ＋ **ソース側で Parquet 生成**。ファイル結合は逆効果 |

---

### G2-6. 異種ソース横断のクエリ基盤 (1問)

**共通教訓**:
- **「S3 + RDS + DynamoDB + Redshift を SQL 類似構文で」+「運用オーバーヘッド最小」= Glue でクロール → Athena**。
  - **JSON は SerDe と JSON 関数**でそのまま扱える。
  - **Lake Formation を持ち出す案は過剰**。全データを Parquet へ変換し、変換処理と元データの同期を維持し、権限設定まで行う必要がある。「単純なクエリ要件に対して重すぎる」というのが切り方。

**問題ごとの1行要約**:
| 問題 | 教訓 |
|---|---|
| [✗] 演習3-問題53 | 4種のソースを SQL で横断・運用最小 = **Glue でクロール + Athena（SerDe / JSON 関数）** |

---

## 🔧 D3: データ運用とサポート (16問) ※不正解8・自信薄8

### G3-1. オーケストレーションとスケジュール実行 (5問) ⚠️ 対象サービスで切る

**共通教訓**:
- **「何をオーケストレーションするか」で機械的に決まる**。

  | 対象 | 選ぶもの |
  |---|---|
  | **Glue ジョブとクローラーだけ** | **Glue workflows**（追加料金なし） |
  | **EMR ジョブ** | **Step Functions**（EMR とネイティブ統合） |
  | **既存 Airflow DAG / Python・Bash をそのまま / OSS・ポータビリティ** | **MWAA** |
  | **Redshift の単発 SQL を定期実行するだけ** | **Query Editor v2 のスケジュール機能** |

- **Glue workflows は EMR を制御できない**。クラスターの作成・ジョブ実行・終了を Glue から直接扱う手段はない。「Glue で全部やる」に引っ張られない。
- **Glue は汎用オーケストレーターではない**。Python シェルジョブで単体スクリプトは動くが、**Bash の実行や任意タスクの依存関係の組み立てはできない**。
- **単一タスクの自動化に MWAA / Step Functions を持ち出すのは過剰**。マテビューの `REFRESH` を毎朝走らせるだけなら **Query Editor v2** で終わる。**追加のコンピュート費用がゼロ**なのが決め手。
  - Glue Python シェルは最小 0.0625 DPU でも課金され、スクリプト作成も必要。

**問題ごとの1行要約**:
| 問題 | 教訓 |
|---|---|
| [🏷] 演習1-問題65 | Python/Bash をそのまま + サーバー管理不要 = **MWAA** |
| [🏷] 演習2-問題57 | ハイブリッド + OSS + ポータビリティ = **MWAA** |
| [✗] 演習3-問題57 | **EMR** ジョブのオーケストレーション最安 = **Step Functions**。Glue workflows は EMR を扱えない |
| [🏷] 演習4-問題49 | ストアドプロシージャの毎朝実行・最安 = **Query Editor v2 のスケジュール機能** |
| [🏷] 演習3-問題5 | マテビューの `REFRESH` 自動化・最小労力 = **Query Editor v2**。MWAA は過剰 |

---

### G3-2. 可視化とレイテンシ (4問)

**共通教訓**:
- **「ドリルダウンしても上位の集計値を固定表示」= QuickSight の LAC-A**（レベル認識計算の集計）。**テーブル計算は表示階層に追従してしまう**。
- **「最も低いレイテンシで可視化」= データが着地している先のネイティブな可視化を使う**。
  - OpenSearch に入っているなら **OpenSearch Dashboards**。
  - ストリームを直接処理するなら **Flink → Timestream → Grafana**（時系列DBと時系列ダッシュボードの組み合わせ）。
  - **Athena は必ず遅い**。S3 への書き込みを待ってから定期クエリを走らせるため、ストリームから直接処理する構成に勝てない。**「Athena + スケジュールクエリ + 別ストアへ書き込み」は工程が増えるだけの典型的な誤答**。
- **「異常を直ちに検出」= DB に着地する前のストリーム段階で検知する**。**DB のポーリングは遅延 + DB 負荷**で必ず負ける。

**問題ごとの1行要約**:
| 問題 | 教訓 |
|---|---|
| [🏷] 演習1-問題45 | ドリルダウンしても総売上を固定 = **LAC-A 関数** |
| [✗] 演習2-問題22 | 最低レイテンシの可視化 = **OpenSearch Dashboards で直接** |
| [🏷] 演習3-問題43 | 大型スクリーンのリアルタイム監視 = **Flink → Timestream → Grafana** |
| [🏷] 演習2-問題38 | ハートビート急減を直ちに検出 = **KDS へ発行 + Managed Flink** |

---

### G3-3. 接続トラブルシューティング (2問) ⚠️ 「経路」を疑う

**共通教訓**:
- **セキュリティグループは同一 SG 内の通信を暗黙には許可しない**。
  - **ソースにその SG 自身を指定したインバウンドルール**を明示的に追加する。
  - **接続はクライアント側から開始される**ので、開けるのは**接続先のインバウンド**。ソースに相手の IP を指定する選択肢は方向が逆。
- **S3 ゲートウェイエンドポイントのエラー = ルートテーブル**。エンドポイントを作っただけでルートが無ければ到達できない。
- **タイムアウト系に「Lambda のタイムアウト値」「メモリ」「インスタンスサイズ」を当てる選択肢は罠**。原因は経路か権限のどちらか。

**問題ごとの1行要約**:
| 問題 | 教訓 |
|---|---|
| [🏷] 演習2-問題13 | 同一 SG でも通信不可 = **ソースに SG 自身を指定したインバウンドルール** |
| [✗] 演習4-問題8 | S3 ゲートウェイエンドポイントのエラー = **ルートテーブルのルートを確認** |

---

### G3-4. Athena の運用とコスト (2問)

**共通教訓**:
- **「実行環境の分離」「クエリ履歴の分離」「コスト分離」= Athena ワークグループ + タグ + タグ条件付き IAM ポリシー**。
  - **Lake Formation はデータとメタデータのアクセス制御**であり、**クエリ実行環境やクエリ履歴の分離はできない**。履歴はワークグループ単位で管理される。
- **スキャン量の削減は WHERE 句のパーティションフィルター**（G2-2 参照）。

**問題ごとの1行要約**:
| 問題 | 教訓 |
|---|---|
| [✗] 演習2-問題3 | 実行環境とクエリ履歴の分離 = **ワークグループ + タグ + IAM** |
| [✗] 演習4-問題6 | スキャン量最小化 = **WHERE 句にパーティションフィルター** |

---

### G3-5. パイプライン監視 (1問)

**共通教訓**:
- **「最小の運用労力でヘルスを監視」= CloudWatch アラーム**。ほぼ機械的にこれ。
  - 押さえるメトリクス: **`IncomingBytes` / `OutgoingBytes`（KDS）**、**`DeliveryToS3.Success`（Firehose）**
  - **Flink はビジネスデータの異常検知用**であって、インフラのヘルス監視用ではない。この使い分けが問われる。

**問題ごとの1行要約**:
| 問題 | 教訓 |
|---|---|
| [✗] 演習4-問題46 | KDS + Firehose の監視 = **CloudWatch アラーム**（`IncomingBytes` / `OutgoingBytes` / `DeliveryToS3.Success`） |

---

### G3-6. データ品質ルール (1問)

**共通教訓**:
- **「2つの列の組み合わせが、別テーブルの同じ組み合わせと一致するか」= `ReferentialIntegrity`**。
  - 書式: `ReferentialIntegrity "pref,city" "area_master.{master_pref,master_city}" = 1.0`
  - **一致率は 0.0〜1.0 の比率**。**`= 100` と書かれた選択肢は、どんなデータでも成立しない**ので即切りできる。**閾値の桁で切る**のがこの手の問題の最短ルート。
  - **`DatasetMatch` はキー列で結合した行全体の一致率**を見るルールで、特定列の参照整合性チェックには使わない。
- 選択肢は「ルールタイプ」と「閾値」の2要素で作られる。**両方が正しいものを選ぶ**。

**問題ごとの1行要約**:
| 問題 | 教訓 |
|---|---|
| [✗] 演習3-問題41 | 複合列の参照整合性 = **`ReferentialIntegrity "pref,city" "area_master.{...}" = 1.0`**。`= 100` は範囲外で成立しない |

---

### G3-7. 低レイテンシの API バックエンド (1問)

**共通教訓**:
- **「呼び出しは1日数回」+「低レイテンシ応答が必須」= Lambda + プロビジョンドコンカレンシー**。
  - 呼び出し頻度が低い＝**コールドスタートが毎回起きる**という含意。これを消すのがプロビジョンドコンカレンシー。
  - 「メモリを増やす」だけの選択肢はコールドスタートを消さない。常時稼働のコンテナやEC2は運用オーバーヘッドで負ける。

**問題ごとの1行要約**:
| 問題 | 教訓 |
|---|---|
| [✗] 演習3-問題33 | 1日数回 + 低レイテンシ必須 = **Lambda + プロビジョンドコンカレンシー** |

---

## 🔐 D4: データセキュリティとガバナンス (8問)

### G4-1. Redshift のアクセス制御とマスキング (3問) ⚠️ 頻出

**共通教訓**:
- **権限の粒度は「オブジェクトの束ね方」で解く**。90テーブルを2部門に分けるなら **部門専用スキーマ + スキーマ単位 GRANT**。
  - **テーブルごとの個別 GRANT は運用オーバーヘッドで負ける**。**「命名規則で動的に権限を当てる」機能は Redshift に存在しない**（架空機能の罠）。
- **「見る人によって同じ列の見え方を変える」= 動的データマスキング（DDM）**。
  - 部門ごとに**データベースロール**を作り、**部門ごとのマスキングポリシー**をアタッチする。
  - **ビューを部門数だけ作る案・データをコピーする案は運用過多**。
  - **Redshift のデータに Lake Formation を当てる案は「S3 へ移してデータレイクを再構築」を伴う**ので大掛かりすぎる。現行構成のまま解けるなら DDM。

**問題ごとの1行要約**:
| 問題 | 教訓 |
|---|---|
| [🏷] 演習2-問題8 | 2部門 × 90オブジェクト = **部門別スキーマ + スキーマ単位 GRANT** |
| [🏷] 演習2-問題41 | 機密列の難読化 + 運用最小 = **Glue でロード + 動的データマスキング** |
| [✗] 演習4-問題51 | 3部門で異なるマスキング = **部門別データベースロール + 部門別マスキングポリシー** |

---

### G4-2. 暗号化とネットワーク境界 (3問)

**共通教訓**:
- **「特定の従業員だけが使えるキー」= SSE-KMS（カスタマー管理キー）+ キーポリシー/IAM で限定**。

  | 方式 | キー利用者の制御 | 判定 |
  |---|---|---|
  | **SSE-S3** | できない（S3 が全部管理） | 「特定の従業員だけ」では**必ず誤答** |
  | **SSE-KMS（カスタマー管理キー）** | キーポリシーで可能 | **これが正解** |
  | **CloudHSM** | 可能だが専用HW運用 + アプリ改修 | HSM 必須の要件がなければ**過剰** |
  | **クライアントサイド暗号化** | 可能だがアプリ改修 | 「最小限の労力」では負ける |

  - **S3 の権限があれば SSE-S3 のデータは自動で復号されて読めてしまう**、という理屈を覚えておく。
- **EMR のセキュリティ設定は1クラスターに1つだけ**。**保存時（S3 の KMS キー）と転送時（PEM 証明書の S3 パス）を1つの設定にまとめ、クラスター作成時に渡す**。
  - **「2つに分けて両方アタッチ」も「作成後にアタッチ」もできない**。
  - **ローカルディスク暗号化はインスタンスストア/EBS 向け**で、S3 のデータ保護にはならない。
- **「AWS 環境の外部からアクセスさせない」は2つの側面に分解する**（2つ選択の型）。
  1. **クラスターへの到達を絞る** → プライベートサブネット + **インターフェイス VPC エンドポイント**（+ S3 へのアウトバウンドに NAT ゲートウェイ）
  2. **データ転送経路を保護する** → **拡張 VPC ルーティング**（COPY/UNLOAD を VPC 経由にする）+ **Direct Connect**
- **暗号化（KMS）やテーブル制約は「ネットワーク経路の遮断」という要件には答えていない**。

**問題ごとの1行要約**:
| 問題 | 教訓 |
|---|---|
| [✗] 演習3-問題2 | 特定従業員だけが使えるキー = **SSE-KMS + キーポリシー/IAM**。SSE-S3 は不可、CloudHSM は過剰 |
| [🏷] 演習1-問題18 | EMR の保存時+転送時暗号化 = **1つのセキュリティ設定にまとめてクラスター作成時に渡す** |
| [✗] 演習4-問題48 | AWS 環境内に閉じる = **プライベートサブネット + インターフェイス VPC エンドポイント** ＋ **拡張 VPC ルーティング + Direct Connect** |

---

### G4-3. 一時認証情報の権限設計 (1問)

**共通教訓**:
- **IAM グループは IAM ユーザーにしか効かない**。**AssumeRole で取得した一時認証情報やフェデレーションユーザーには適用されない**。AD 連携では IAM ユーザーを作らないのが普通なので、グループベースの選択肢は成立しない。
- **「ロールを増やさずに条件ごとに権限を絞る」= セッションポリシー**。`AssumeRole` 時にリージョンなどを絞ったポリシーを渡す。

**問題ごとの1行要約**:
| 問題 | 教訓 |
|---|---|
| [✗] 演習1-問題62 | リージョン別の権限を1ロールで = **セッションポリシー**。IAM グループは一時認証情報に効かない |

---

### G4-4. データカタログとビジネスメタデータ (1問)

**共通教訓**:
- **「ビジネスメタデータ」「ビジネス用語集」「データポータルで検索・閲覧」= Amazon DataZone**。
- **Glue Data Catalog は技術メタデータ（テーブル定義・スキーマ・パーティション）専用**。
  - 用語集をテーブルとして自作し、API でプロパティを更新し、閲覧用 Web アプリまで開発する構成になるため、**「管理の手間を最小限」では必ず負ける**。
- **技術メタデータ = Glue Data Catalog / ビジネスメタデータ = DataZone** の対比で覚える。

**問題ごとの1行要約**:
| 問題 | 教訓 |
|---|---|
| [🏷] 演習3-問題21 | ビジネス用語集 + メタデータフォーム + ポータル = **Amazon DataZone**。Glue Data Catalog では自作が必要 |

---

## 🎯 試験前日にこれだけ見る(超要点)

### D1(34%)で最頻出の判断ポイント
1. **KDS→Redshift のリアルタイム = Redshift ストリーミング取り込み**。Firehose は S3 経由 COPY で遅い
2. **KDS→変換→S3/OpenSearch = Firehose + Lambda 変換**。KCL 自作は運用負担で負ける
3. **複数チームが専用帯域 = 拡張ファンアウト**。EC2 増強では2MB/秒の共有制約は消えない
4. **高スループット送信 = `PutRecords` / 小レコード集約 = KPL**。Firehose は入口ではない
5. **時間ウィンドウ集計 = Managed Flink**
6. **`No space left on device` = `--write-shuffle-files-to-s3`**（キー集中の記述があればソルティング併用、Glue に EBS は増設できない）
7. **Glue 再実行の重複 = ステージング + SQL の UPDATE**
8. **毎日フルスナップショット → 差分反映 = Iceberg / Delta の MERGE**（Lambda は15分・10GB上限で不可）
9. **CloudWatch Logs → 外部 = サブスクリプションフィルター + Firehose**
10. **複数 DB を横断分析 = Glue → Redshift → QuickSight**（DynamoDB は不適）
11. **要件が2つあれば構成も2本**（DMS タスクを分ける等）

### D2(26%)で最頻出の判断ポイント
1. **巨大×巨大 = 両方に KEY 分散／巨大×極小 = 小さい方に ALL／適した列なし = AUTO**
2. **クエリプランが遅い = パーティションインデックス + プロジェクション**
3. **スキャン量が多い = WHERE 句のパーティションフィルター**
4. **最小レイテンシでカタログ同期 = `create_partition` API を書き込みコードから直接**
5. **毎日パーティションを作る = パーティション射影**
6. **Iceberg の性能低下 = `rewriteDataFiles` を短い間隔で**（日次1回では遅い）＋ copy-on-write
7. **S3 の大テーブルに差分更新 = Hudi / Iceberg の upsert**
8. **数時間かかる変換 = マテリアライズドビュー**（Athena 移行は性能低下）
9. **クラスター間共有 = datashare**
10. **Spectrum が遅い = パーティション分割 + Parquet**（**ファイル結合は逆効果**、64MB超が適正）
11. **JSON を Redshift へ = `SUPER` 型／全行削除 = `TRUNCATE`／1桁ms = DynamoDB + GSI**
12. **異種ソース横断 = Glue クロール + Athena**

### D3(22%)で最頻出の判断ポイント
1. **Glue だけ = Glue workflows ／ EMR = Step Functions ／ Airflow・Bash = MWAA ／ Redshift の単発SQL = Query Editor v2**
2. **Glue workflows は EMR を制御できない**
3. **ドリルダウンしても固定値 = LAC-A**
4. **最低レイテンシの可視化 = 着地先のネイティブ可視化（OpenSearch Dashboards / Timestream + Grafana）**。Athena は必ず遅い
5. **異常を直ちに検出 = DB 着地前のストリーム段階（KDS + Flink）**
6. **同一 SG でも通信不可 = SG 自身をソースにしたインバウンドルール**
7. **ゲートウェイエンドポイントのエラー = ルートテーブル**
8. **クエリ履歴・実行環境の分離 = Athena ワークグループ**（Lake Formation ではない）
9. **ヘルス監視 = CloudWatch アラーム**（Flink は過剰）
10. **複合列の参照整合性 = `ReferentialIntegrity ... = 1.0`**（`= 100` は成立しない）
11. **低頻度 + 低レイテンシ = Lambda + プロビジョンドコンカレンシー**

### D4(18%)で最頻出の判断ポイント
1. **特定の人だけが使えるキー = SSE-KMS + キーポリシー**（SSE-S3 は不可、CloudHSM は過剰）
2. **EMR のセキュリティ設定は1つにまとめ、クラスター作成時に渡す**
3. **列の見え方を人ごとに変える = 動的データマスキング + データベースロール**
4. **多数オブジェクトの権限 = スキーマ単位 GRANT**
5. **AWS 環境内に閉じる = プライベートサブネット + インターフェイス VPC エンドポイント ＋ 拡張 VPC ルーティング + Direct Connect**
6. **一時認証情報に IAM グループは効かない → セッションポリシー**
7. **ビジネス用語集 = DataZone ／ 技術メタデータ = Glue Data Catalog**

---

## ⚡ 「迷ったらこう切る」DEA共通の判断軸

1. **末尾の修飾語を先に読む**。「最もコスト効率」「最小の運用労力」「最小の開発工数」「最短の実行時間」「最も低いレイテンシ」のどれかで正解の方向が半分決まる。**選択肢は全部動く**前提で、この一語だけを見て切る。
2. **マネージドの組み込み機能 > 自作（Lambda / KCL / 自前スクリプト / 外部ジョブ）**。専用機能が存在するのに自作する選択肢は、ほぼ常に不正解。
3. **中間ステップの少ない方**（S3 を挟む、Lambda を挟む、テーブルをコピーする、クラスターを増やす、全データを変換する…は大体不正解）。
4. **「すでに設定済み」と書かれた層は原因ではない**。原因は明記されていない別レイヤー（経路 or 権限）にある。
5. **架空の機能・実行不可能な操作が混ざっていないかを先に見る**。
   - 「オプティマイザで候補を選ぶ」「命名規則で動的に権限を当てる」「セキュリティ設定を2つアタッチ」「Firehose で KDS へ送る」「Glue に EBS を増設」「一致率 `= 100`」はすべて成立しない。
6. **2つ選択は "別の層" から1つずつ**。監視＋対策、パーティション＋ファイル形式、到達制御＋経路保護。同じ層の対策が2つ並んでいたら疑う。
7. **症状の言葉をそのまま原因にしない**。「クエリプランが遅い」と「スキャン量が多い」は別問題。「遅延が出ている場所」と「遅延の原因」も別。
8. **垂直スケーリング（ワーカー拡大・ノード追加・EC2 増強）は逃げの選択肢**。偏り・小ファイル・帯域・経路の問題は解決しないうえ、コスト要件で必ず負ける。
9. **要件文が指定した格納先・宛先を勝手に読み替えない**。「データウェアハウスへロード」と書いてあるのに S3 + Athena を選ぶのは要件違反。
10. **頻度・閾値・サイズの数字が選択肢の差になっていないか確認する**。「日次1回のコンパクション vs 短い間隔」「`= 1.0` vs `= 100`」「ファイルを結合 vs 分割のまま」はいずれも数字だけが違う。

---

**頑張ってください 🍀**
