# DOP-C02 キーワード網羅辞書(マーク60問から抽出)

> **本資料の狙い**: 「試験直前 凝縮まとめ」の補完として、**マーク済60問の解説から抽出したキーワードを17カテゴリに整理した辞書**。
>
> - 用途: 「解いていて迷ったとき / 覚えきれていないキーワードを引く」ときの辞書
> - 各見出しに **出現した問題番号** を記載しているので、原問題の解説に戻れる
> - 重要度でフィルタせず、**判断の分かれ目になった論点はすべて掲載**（頻出限定版は「凝縮まとめ」を参照）
> - 🚨 マークは **実際に選択肢の正誤を分けた決定打**

## 📚 目次

- [1. CI/CDパイプライン(CodePipeline・CodeBuild・CodeCommit)](#1-cicdパイプライン)
- [2. デプロイメント(CodeDeploy・デプロイ戦略)](#2-デプロイメント)
- [3. アーティファクト・レジストリ(ECR・CodeArtifact)](#3-アーティファクトレジストリ)
- [4. CloudFormation](#4-cloudformation)
- [5. AWS SAM・AWS CDK](#5-aws-samaws-cdk)
- [6. AWS Systems Manager](#6-aws-systems-manager)
- [7. EC2・Auto Scaling・AMI](#7-ec2auto-scalingami)
- [8. コンテナ(ECS・EKS)](#8-コンテナecseks)
- [9. サーバーレス(Lambda・API Gateway)](#9-サーバーレスlambdaapi-gateway)
- [10. ストレージ(S3・FSx・EFS・EBS)](#10-ストレージ)
- [11. データベース・DR(Aurora・Route 53・ARC)](#11-データベースdr)
- [12. メッセージング・イベント(SQS・SNS・EventBridge)](#12-メッセージングイベント)
- [13. 監視・ロギング(CloudWatch・AMP・フローログ)](#13-監視ロギング)
- [14. IAM・認証](#14-iam認証)
- [15. マルチアカウント統制(Organizations・Control Tower)](#15-マルチアカウント統制)
- [16. セキュリティ検出・コンプライアンス(Config・Security Hub)](#16-セキュリティ検出コンプライアンス)
- [17. ネットワーク(PrivateLink・VPCエンドポイント・ELB)](#17-ネットワーク)

---

## 1. CI/CDパイプライン

### 🔑 CodePipeline のステージ構成とパイプライン分割

> 出現: 演習2-問題13 / 演習3-問題29 / 演習3-問題1

・パイプラインはソース・ビルド・デプロイ等のステージで構成され、ステージ内のアクションは `runOrder` が同じものが**並列実行**される。同じ `runOrder` を複数のデプロイアクションに設定することで、複数環境への同時デプロイを1本のパイプラインで実現できる。

・「環境ごとに独立したロールバックが必要」という要件は、**パイプラインやアプリケーションを分ける理由にはならない**。CodeDeployのデプロイメントグループ単位で完結する。

・パイプラインの途中に **Lambdaアクション** を挟むことで、SDKのエクスポート・S3アップロード・CloudFrontのキャッシュ無効化のような後処理を「デプロイの一部」として確実に実行できる（手動作業の排除・成否の可視化）。

・CodeBuildだけで完結させる案は、ステージ間の連携・承認プロセス・実行状況の可視化が欠けるため、複雑なCI/CDでは不足する。

### 🔑 環境分離はブランチ、リポジトリではない 🚨

> 出現: 演習2-問題13

・**単一リポジトリ + 環境別ブランチ（test / main など）+ 環境別パイプライン** が標準構成。各パイプラインを対応するブランチにリンクする。

・環境ごとにリポジトリを分けると、コードの同期・マージ作業・バグ修正の二重対応が発生し、開発効率が落ちる。**同一コードベースから環境別デプロイ**を行うのが原則。

・本番パイプラインには **手動承認アクション** を入れ、自動デプロイはテスト環境のみに限定する。

### 🔑 CodePipeline のクロスアカウントデプロイ 🚨

> 出現: 演習3-問題36

・ビルドアカウントのパイプラインから開発/本番アカウントへデプロイする場合、アクセス拒否の原因は **2つの権限レイヤー** に分かれる。

1. **KMSレイヤー**: アーティファクトが暗号化されている場合、他アカウントのCloudFormation実行ロールが**復号(`kms:Decrypt`)**できる必要がある。**AWSマネージドキーはキーポリシーを編集できない**ため、**カスタマーマネージドキー(CMK)** を作成してポリシーで許可する。
2. **S3レイヤー**: IAMロールに `s3:GetObject` があっても、**アーティファクトS3バケットのバケットポリシー**でクロスアカウントを明示的に許可しないと拒否される。

・原則: **クロスアカウントはアイデンティティベースポリシーとリソースベースポリシーの両方が必要**。片方だけでは通らない。

### 🔑 CodeBuild の buildspec.yml とフェーズ

> 出現: 演習1-問題70 / 演習3-問題58

・`install` / `pre_build` / `build` / `post_build` のフェーズを持つ。**認証や事前準備は `pre_build`**、ビルドとpushは `build` に置くのが定石（認証エラーを早期に検出できる）。

・`env` セクションで `git-credential-helper: yes` を設定すると、**CodeBuildのサービスロールの認証情報でGit操作（clone / push）** が可能になる。

・`CODEBUILD_BUILD_SUCCEEDING` 環境変数で、**テスト成功時のみタグ付けする**といった条件分岐が書ける。

### 🔑 CodeCommit APIの制約とネイティブGit 🚨

> 出現: 演習1-問題70

・**CodeCommit APIには「Gitタグを作成する」エンドポイントが存在しない**。タグ付けが要件なら、**ネイティブGitコマンド（`git clone` → `git tag` → `git push`）** を使う以外にない。

・AWS CLIでソースを「コピー」する方法は実質ファイルのダウンロードであり、**Git履歴やメタデータが失われる**。Gitリポジトリの操作はGitで統一するのが安全。

### 🔑 AWS IAM Roles Anywhere

> 出現: 演習2-問題62

・**オンプレミス環境のワークロードにIAMロールを使わせる**ための仕組み。X.509証明書ベースの認証で、長期のアクセスキーを配布せずに一時認証情報を取得できる。

・構成要素: **トラストアンカー**（信頼するCA）+ **プロファイル** + **IAMロール**。

・「オンプレのCI/CDパイプラインからCodeArtifactやその他AWSサービスにアクセスしたい」というシナリオの定番解。

---

## 2. デプロイメント

### 🔑 CodeDeploy のデプロイメントタイプ

> 出現: 演習1-問題35

・**インプレースデプロイメント**: 既存インスタンス上でアプリを更新。各インスタンスは一時的にトラフィックから外れる。新しいインスタンス群のプロビジョニングは発生しない。

・**ブルー/グリーンデプロイメント**: 新しいインスタンス群（グリーン環境）を作成してデプロイ・テストし、トラフィックを段階的に移行。移行完了後に元の環境を削除または維持できる。
→ **「新しいリビジョンごとに新しいインスタンス群を自動でプロビジョニング」** という要件があれば、**必ずブルー/グリーン**。

・ブルー/グリーンでは **「Auto Scalingグループの自動コピー」** オプションで、元のASG設定をそのままグリーン環境に複製できる（手動セットアップ不要という要件に対応）。

### 🔑 CodeDeploy のデプロイメント設定（構成） 🚨

> 出現: 演習1-問題35 / 演習1-問題62 / 演習3-問題69

・**標準（事前定義）構成**:
- `CodeDeployDefault.OneAtATime`: 1つずつ更新
- `CodeDeployDefault.HalfAtATime`: **半数を同時に更新**
- `CodeDeployDefault.AllAtOnce`: すべて同時

・**カスタム構成**: 最小正常ホスト数を数値または割合で指定できる。ただし **標準構成で表現できる要件にカスタムを作るのは「不要な複雑化」で不正解になりやすい**。
→ 「新インスタンスの半数を同時にターゲットにする」= `HalfAtATime` を選ぶ。

・ECS向け: `CodeDeployDefault.ECSLinear10PercentEvery1Minutes`, `ECSCanary10Percent5Minutes`, `ECSAllAtOnce` など。
・Lambda向け: `LambdaCanary10Percent10Minutes`, `LambdaLinear10PercentEvery1Minute`, `LambdaAllAtOnce` など。

### 🔑 CodeDeploy ライフサイクルイベント（EC2/オンプレミス）🚨

> 出現: 演習1-問題35 / 演習2-問題73

・**インプレースの実行順**:
`ApplicationStop` → `DownloadBundle` → `BeforeInstall` → `Install` → `AfterInstall` → `ApplicationStart` → `ValidateService`
（ロードバランサー使用時は前後に `BeforeBlockTraffic` → `BlockTraffic` → `AfterBlockTraffic`、`BeforeAllowTraffic` → `AllowTraffic` → `AfterAllowTraffic`）

・**ブルー/グリーンの内訳**:
- **ブルー環境（元）**: `BeforeBlockTraffic` / `BlockTraffic` / `AfterBlockTraffic`
- **グリーン環境（新）**: `BeforeInstall` / `Install` / `AfterInstall` / `ApplicationStart` / `ValidateService` / `BeforeAllowTraffic` / `AllowTraffic` / `AfterAllowTraffic`

・**`Install` / `BlockTraffic` / `AllowTraffic` はCodeDeployの予約イベント** で、**ユーザースクリプトを実行できない**。選択肢にこれらへのフック指定があれば即除外。

### 🔑 BeforeInstall フック 🚨

> 出現: 演習2-問題73

・**新しいアプリケーションリビジョンのインストール前**に実行される。ディレクトリ作成、依存関係のインストール、**前提ファイル（ライセンスファイル等）の配置**に使う。

・`Install` イベントで実際のファイル配置が行われるが、これは予約イベントでスクリプトを実行できないため、**この段階で必要なファイルはすでに存在している必要がある**。

・判断の目安: 「アプリの起動時に読み込まれるファイル」は **`ApplicationStart` より前**、つまり `BeforeInstall` で配置する。

### 🔑 BeforeAllowTraffic フック 🚨

> 出現: 演習1-問題35 / 演習3-問題37

・**新環境（グリーン）がトラフィックを受け取る直前**に実行される。**最終的な前提条件の確認**に最適。

・典型的な用途:
- 一時ファイル・キャッシュの削除
- **データベースのスキーマ変更やデータ移行の完了待ち**（ポーリング + タイムアウト設定）
- 依存サービスへのヘルスチェック

・`BeforeBlockTraffic` は**元の環境からトラフィックをブロックする前**の処理であり、新環境の準備には使えない（頻出の引っかけ）。

### 🔑 Lambda デプロイメントのフックは2つだけ 🚨

> 出現: 演習3-問題37

・**CodeDeploy for Lambda で利用できるフックは `BeforeAllowTraffic` と `AfterAllowTraffic` のみ**。実行順は `BeforeAllowTraffic` → トラフィック切り替え → `AfterAllowTraffic`。

・`BeforeInstall` / `ValidateService` などは **EC2/オンプレミス等の他プラットフォーム用** であり、Lambdaデプロイの選択肢に出てきたら不正解。

・フックに指定したLambda関数は、検証結果を **`PutLifecycleEventHookExecutionStatus` API** でCodeDeployに通知し、デプロイ継続の可否を動的に決められる。失敗時は自動ロールバック。

・トラフィック制御は **エイリアスの重み付きルーティング** で実現され、CloudWatchアラームと統合して自動ロールバックできる。

### 🔑 appspec.yml

> 出現: 演習2-問題73 / 演習3-問題73

・デプロイの内容（ファイル配置、フックのスクリプト、ECS/Lambdaのターゲット定義）を記述するファイル。**リポジトリのルート**に置く。

・appspec.ymlの欠如はデプロイ失敗の原因になるが、その場合は **「スキップ」ではなく明確なエラー** として報告される（症状の切り分けに使う）。

### 🔑 CodeDeploy「スキップ」ステータスの診断 🚨

> 出現: 演習3-問題73

・すべてのライフサイクルイベントが「スキップされた」= **CodeDeployは指示を出したが、対象インスタンス側のエージェントが処理できていない**状態。

・主な原因は2つ:
1. **ネットワーク到達性の欠如**: プライベートサブネットにNATゲートウェイがない / パブリックサブネットにインターネットゲートウェイがない → エージェントが**CodeDeployのサービスエンドポイントに到達できない**（VPCエンドポイントで代替も可能）
2. **インスタンスプロファイルの権限不足**: S3からリビジョンを取得したり、状況を報告するIAM権限がない

・切り分けのポイント: 権限エラーや設定ファイル欠如は**エラーメッセージ**として現れる。「スキップ」は**通信・エージェント起因**を疑う。

### 🔑 デプロイメントグループ

> 出現: 演習3-問題29 / 演習3-問題73

・デプロイ対象（EC2タグ / ASG / ECSサービス / Lambdaエイリアス）とデプロイ設定・ロールバック設定をまとめた単位。

・**1つのCodeDeployアプリケーションに複数のデプロイメントグループ**を作ることで、デプロイスクリプト・IAMロール・通知設定を一元管理しつつ、**グループ単位で独立した並列デプロイとロールバック**ができる。
→ 「複数のALB+ASGの組に並列デプロイし、問題があった環境だけロールバックしたい」の最小構成解。

### 🔑 AWS::CodeDeployBlueGreen トランスフォーム 🚨

> 出現: 演習1-問題62

・**CloudFormationテンプレートからECSのブルー/グリーンデプロイを実行する**ための仕組み。

・必要な要素:
- `Transform: AWS::CodeDeployBlueGreen` の宣言
- `Hooks: AWS::CodeDeploy::BlueGreen` フックパラメータ（ライフサイクルのカスタマイズ）
- **`TrafficRoutingConfig`**: `TimeBasedLinear`（`StepPercentage` / `BakeTimeMins`）または `TimeBasedCanary`
→ 「1分ごとに10%ずつ」= `TimeBasedLinear` で `StepPercentage: 10`, `BakeTimeMins: 1`（CodeDeployの `ECSLinear10PercentEvery1Minutes` 相当）

・**問われているのが「CloudFormationテンプレートに何を含めるか」なら、AppSpecのデプロイ構成名を答える選択肢は的外れ**。

---

## 3. アーティファクト・レジストリ

### 🔑 Amazon ECR の認証（get-login-password）🚨

> 出現: 演習3-問題58

・S3へのアクセスは AWS SDK/CLI がIAM認証情報で署名するだけで完結するが、**DockerクライアントからECRを操作する場合は、IAM権限に加えて「認証トークンを取得してDockerレジストリにログインする」手順が必須**。

・実装:
```
aws ecr get-login-password --region <region> | docker login --username AWS --password-stdin <account>.dkr.ecr.<region>.amazonaws.com
```
・**IAMロールの権限更新とbuildspec.ymlの更新だけではアクセスに失敗する**（S3→ECR移行時の定番の落とし穴）。認証トークンには有効期限（12時間）がある。

### 🔑 ECR リポジトリポリシー（クロスアカウント）🚨

> 出現: 演習2-問題50 / 演習1-問題65

・イメージを**別アカウントから取得**させるには、**イメージが置かれているアカウント側のリポジトリにリポジトリポリシー**を設定し、取得側のプリンシパル（ECSタスク実行ロール等）を許可する。

・取得側にはIAM権限（`ecr:GetAuthorizationToken`, `ecr:BatchGetImage`, `ecr:GetDownloadUrlForLayer` 等）も必要 = **両側許可**。

・**新しいアカウントにリポジトリを作り直す案は不正解**になりやすい。既存のCI/CDパイプラインとの整合性が崩れ、レプリケーション等の追加設定が増えるため。

### 🔑 ECRのプライベート接続には2種類のエンドポイントが必要 🚨

> 出現: 演習2-問題50

・ECRからのイメージ取得は **①ECR APIでのメタデータ取得** と **②S3からのイメージレイヤーのダウンロード** の2フェーズに分かれる。

・したがって完全にプライベート化するには **ECR用のインターフェイスVPCエンドポイント（`ecr.api` / `ecr.dkr`）** と **S3用のゲートウェイエンドポイント** の**両方**が必要。

### 🔑 ECR イメージスキャンとスキャン前後の分離

> 出現: 演習1-問題65

・**プッシュ時スキャン（scan on push）** を有効にすると、イメージ登録時に自動でスキャンされる。

・「スキャン済みのみデプロイ可能」「スキャン前後を分離」という要件は、**共有サービスアカウントにリポジトリを2つ**用意して実現する:
- **スキャン前リポジトリ**: 組織全体に**書き込み**許可 + プッシュ時スキャン有効
- **スキャン後リポジトリ**: 組織全体に**読み取り**許可

・移動の自動化は **EventBridgeでECRのスキャン完了イベントを検知 → Lambdaで結果を評価 → 問題なければスキャン後リポジトリへ移動**。リポジトリごとにパイプラインを作る案は管理オーバーヘッドが大きく不正解。

### 🔑 AWS CodeArtifact の外部接続とアップストリーム 🚨

> 出現: 演習2-問題62

・**外部接続（external connection）**: npm / PyPI / Maven Central などのパブリックリポジトリへの接続。**1つのリポジトリに設定できる外部接続は1つだけ**。
→ 「複数の外部接続を持つリポジトリを1つ作る」という選択肢は**技術的に誤り**。

・**アップストリームリポジトリ**: 参照先の連鎖。開発者が使うリポジトリの**アップストリーム**としてパブリック接続用リポジトリを設定すると、パッケージが自動でプロキシ・キャッシュされる（「ダウンストリーム」ではない）。

・オンプレのCI/CDから利用するなら **IAM Roles Anywhere** で認証する。

---

## 4. CloudFormation

### 🔑 cfn-init（AWS::CloudFormation::Init）🚨

> 出現: 演習2-問題24 / 演習3-問題67

・テンプレートの **`Metadata` 内の `AWS::CloudFormation::Init`** に書かれた設定（パッケージ、ファイル、サービス、コマンド）を読み取り、インスタンスに適用するヘルパースクリプト。UserDataから起動時に1回実行する。

・**構成ファイルの中身をテンプレート内に埋め込める**ため、「構成ファイルはすべてソース管理に置く」というポリシー要件を、テンプレートのバージョン管理だけで満たせる。

・UserDataにベタ書きしたシェルスクリプトとの違いは、**宣言的に書けること**と、**cfn-hupと組み合わせて再適用できること**。

### 🔑 cfn-hup 🚨

> 出現: 演習2-問題24 / 演習3-問題67

・インスタンス上で常駐し、**スタックのメタデータ変更を定期的に検知して cfn-init を再実行**するデーモン。

・これがあるおかげで、**インスタンスを置き換えずに**スタック更新の内容を実行中のインスタンスへ反映できる。設定ファイル（`/etc/cfn/cfn-hup.conf`, `cfn-auto-reloader.conf`）で更新間隔とフックを定義する。

・「UserDataを変更してスタックを更新したのにアプリが更新されない」問題の解決策の1つ（もう1つは SSM State Manager）。

### 🔑 cfn-signal と WaitOnResourceSignals 🚨

> 出現: 演習3-問題70

・**cfn-signal** は、UserDataでの初期化が**成功したか失敗したか**をCloudFormationに通知するヘルパースクリプト。

・ASGの `UpdatePolicy: AutoScalingRollingUpdate` に **`WaitOnResourceSignals: true`** と `PauseTime` を設定すると、CloudFormationは**新インスタンスからのシグナルを受け取るまで更新完了とみなさない**。時間内に来ない/失敗シグナルなら**スタック更新を失敗させ、ロールバック**する。

・**症状の見分け方**: 「スタックは `UPDATE_COMPLETE` なのに、ALBのヘルスチェックが全部Unhealthy」= **EC2が起動しただけで成功と判定されている** = cfn-signalが無い。
→ CloudWatchアラームやライフサイクルフックは、この「スタックの状態制御」という本質を解決しない。

### 🔑 CloudFormation サービスロールと iam:PassRole 🚨

> 出現: 演習1-問題45

・「インフラ変更はCloudFormation経由のみ」を強制する構成は3点セット:

1. **開発者ロール**: `AdministratorAccess` を外し、**`ReadOnlyAccess` + CloudFormationスタック操作権限（`cloudformation:CreateStack` / `UpdateStack` / `DeleteStack` 等）+ `iam:PassRole`** を付与
2. **デプロイ用ロール（サービスロール）の信頼ポリシー**: プリンシパルに **`cloudformation.amazonaws.com`** を指定して `sts:AssumeRole` を許可
3. **デプロイ用ロールのアクセス許可**: リソース作成権限 + `cloudformation:*` + **`iam:PassRole`（`iam:PassedToService` = `cloudformation.amazonaws.com` の条件付き）**

・**`iam:PassRole`** は「サービスにロールを渡す」権限。これがないと、開発者はスタックにサービスロールを指定できない。**`iam:PassedToService` 条件**で渡し先サービスを限定するのがベストプラクティス。

・信頼ポリシーで**開発者ロールを信頼させる案は不正解**（開発者が直接そのロールを引き受けて手動操作できてしまう）。

### 🔑 CloudFormation ドリフト検出 🚨

> 出現: 演習2-問題21

・スタックのリソースがテンプレート定義から乖離していないかを検出する機能。API（`DetectStackDrift`）でも実行できるが、**「検出結果を通知する」仕組みは標準では持たない**。

・推奨解: **AWS Config のマネージドルール `cloudformation-stack-drift-detection-check` を有効化** → **EventBridgeでコンプライアンス変更イベントを捕捉** → **SNSに通知**。
→ Lambdaを2つ作って結果をポーリングする案は、マネージド機能で置き換えられるため「運用効率」の観点で不正解。

### 🔑 CloudFormation IaCジェネレーター 🚨

> 出現: 演習3-問題57

・**CloudFormation管理外の既存リソースをスキャンして、テンプレートまたはCDKアプリを自動生成**する機能（2024年2月リリース）。500種類以上のリソースタイプに対応。

・**部分スキャン**でリソースタイプを絞り込める（例: Lambdaのみ）。

・**`cdk migrate` と連携して、CloudFormationテンプレートを経由せずCDKアプリケーション（Python等）を直接生成できる**のがポイント。「テンプレート生成 → 手動でプロパティ編集 → CDKに変換」という3ステップが不要になるため、「最も実装工数が少ない」要件を満たす。

・AWS Configにはコード生成機能がないため、リソース一覧をCSVで出して手書きする案は最も工数が多い。

### 🔑 SSMパラメータ型のテンプレートパラメータ / resolve:ssm 🚨

> 出現: 演習3-問題53 / 演習2-問題8

・CloudFormationのパラメータ型に **`AWS::SSM::Parameter::Value<AWS::EC2::Image::Id>`** などを指定すると、**Parameter Storeから値を動的に取得**できる。テンプレートを書き換えずに常に最新のAMI IDを使える。

・動的参照 **`{{resolve:ssm:parameter-name:version}}`** も利用可能。

・起動テンプレートの `ImageId` にも **`resolve:ssm:` 形式**が指定でき、パラメータ名・バージョン付き・ラベル付き・**他アカウントから共有されたパラメータのARN**まで対応する。

・階層型のパラメータ名（`/ami/prod/linux` 等）で環境別・リージョン別・プロジェクト別の管理ができ、IAMで参照権限を制御できる。

### 🔑 AWS::SSM::Association リソース

> 出現: 演習2-問題35

・CloudFormationから **State Manager アソシエーション** を作成するリソース。**タグでターゲットを指定**すれば、起動テンプレートでそのタグを付けた**すべての新規インスタンスに自動適用**される。

・`AWS::SSM::Document` は「ドキュメントそのもの」を作るリソースであり、**インスタンスへの関連付けは行わない**。AWSマネージドランブックを使う場面では新規ドキュメント作成は不要。

### 🔑 起動時のUserDataでのリソース参照

> 出現: 演習2-問題67

・`AWS::AutoScaling::LaunchConfiguration`（および起動テンプレート）の **`UserData`** に **`!Ref` / `Fn::Sub`** でCloudFormation内の他リソース（ECSクラスタ名等）を埋め込むことで、起動時に正しい設定を注入できる。

・ECS on EC2 の場合、UserDataで `/etc/ecs/ecs.config` に `ECS_CLUSTER=<cluster-name>` を書き込む。これが無いと**インスタンスはdefaultクラスタに登録され、「誤ったクラスタにリンクされる」症状**になる。

### 🔑 CloudFormation StackSets（選択肢としての位置づけ）

> 出現: 演習2-問題49 / 演習1-問題59

・複数アカウント/リージョンへ同一テンプレートを展開する機能。自動デプロイを有効にすると、OUに追加された**新規アカウントにも自動展開**される。

・ただし **「Control Towerからリクエスト可能なカスタムオプション（ブループリント）を提供する」** という要件では、Service Catalog製品として登録するのが正しく、StackSetsは要件を満たさない。

・「予防的コントロールも必要」な場面では、StackSets単独では不足（SCPが伴わない）。

---

## 5. AWS SAM・AWS CDK

### 🔑 SAMの CodeUri と「no changes to deploy」🚨

> 出現: 演習1-問題30

・`AWS::Serverless::Function` の **`CodeUri` にS3パスを直接書くと、SAMはそのパスを参照するだけでコードの中身の変更を検知しない**。テンプレートに差分がなければ「no changes to deploy」となる。

・解決策は **`CodeUri` をローカルのコードディレクトリに変更する**こと。すると:
- `sam deploy`（内部でbuild/packageを行う）
- または `aws cloudformation package` でS3へアップロード → `aws cloudformation deploy`

のいずれかで、**新しいS3キーが生成されテンプレート差分が発生する** → 正しくデプロイされる。

### 🔑 AutoPublishAlias と DeploymentPreference 🚨

> 出現: 演習3-問題69

・**`AutoPublishAlias`**: SAMがデプロイのたびに新しいバージョンを発行し、指定エイリアスを更新する。**カナリア/リニアデプロイの前提**。

・**`DeploymentPreference`**: デプロイ方式を宣言する。
```yaml
DeploymentPreference:
  Type: LambdaCanary10Percent10Minutes
  Alarms: [ !Ref ErrorAlarm ]
  Hooks:
    PreTraffic: !Ref PreTrafficHookFunction
```
・**`Alarms` にCloudWatchアラームを指定すると、ALARM状態で自動ロールバック**される。
→ アラームは **関数ごと・バージョン（次元）ごとに個別作成**する。複合アラームは粒度が粗く、カナリアの異常検知には不適。

・CloudFormationの汎用機能で同じことをやろうとすると記述量が激増するため、**サーバーレスのカナリアはSAMが正解**。

### 🔑 CDKのLambdaコード指定方法 🚨

> 出現: 演習3-問題57

・**`Code.from_asset(path)`**: ローカルのディレクトリ/zipを指定。デプロイ時にCDKが自動でzip化してS3へアップロード。**既存Lambdaを取り込んでCDK管理する場合の推奨**。

・**`Code.from_inline(code)`**: 文字列としてテンプレートに埋め込み。**4,096バイト上限**。

・**`Code.from_bucket(bucket, key)`**: 既存S3上のzipを参照。S3側を更新してもLambdaは自動更新されないため、継続管理には不向き。

・**`Code.from_ecr_image()`**: コンテナイメージ形式のLambda専用。

・CloudFormation側の対応: `Code.S3Bucket + S3Key`（標準）/ `Code.ZipFile`（4,096バイト上限）/ `Code.ImageUri`（コンテナ専用、**zip形式のLambdaには使用不可**）。

---

## 6. AWS Systems Manager

### 🔑 SSMドキュメントの種類

> 出現: 演習1-問題3 / 演習1-問題5 / 演習2-問題35

・**Command document**: Run Command / State Manager で実行するアクション定義
・**Automation document（ランブック）**: 複数ステップの自動化ワークフロー
・**Policy document**: State Managerで使う設定ポリシー

・構造: `schemaVersion` / `description` / `parameters` / `mainSteps`。

### 🔑 aws:downloadContent プラグイン 🚨

> 出現: 演習1-問題3

・**外部ソースからコンテンツをマネージドノードにダウンロード**するプラグイン。

・主なパラメータ:
- **`sourceType`**: `GitHub` / `S3` / `HTTP` / `SSMDocument`
- **`sourceInfo`**: リポジトリ情報（`owner`, `repository`, `path`, `getOptions`, 認証トークン等）
- `destinationPath`: 保存先

・**GitHub上のスクリプトを使うなら、S3を中間ステージにする必要はない**。「GitHub → S3 → downloadContent」という選択肢は**不要に複雑**で不正解。

・比較:
- **`aws:configurePackage`**: SSM Distributor のパッケージをインストール/アンインストールする用途。**Gitリポジトリは対象外**。
- **`aws:softwareInventory`**: インベントリ収集専用。**ダウンロードや実行はできない**。
- **`aws:runShellScript` / `aws:runPowerShellScript`**: ダウンロードしたスクリプトの実行に使う。

### 🔑 ハイブリッドアクティベーション

> 出現: 演習1-問題3

・AWS環境外の物理デバイス/VMをSSMのマネージドノードとして登録する仕組み。

・手順: **①アクティベーション作成（IDとコードを取得） → ②対象にSSM Agentをインストール → ③IDとコードで登録 → ④マネージドインスタンスとして表示**。

・登録後は、EC2と同じようにRun Command / State Manager / Patch Managerの対象にできる。

### 🔑 State Manager アソシエーション 🚨

> 出現: 演習1-問題5 / 演習2-問題24

・**「望ましい状態」を定義し、継続的に適用し続ける**機能。定義した設定からずれた場合も再適用される。

・特徴:
- **ターゲット指定が柔軟**: タグ、リソースグループ、`*`（全マネージドノード）
- **新しく追加されたインスタンスにも自動的に適用**される
- スケジュール実行（cron/rate）が可能

・「すべてのインスタンスに一貫してソフトウェアをインストールし、追加インスタンスにも自動適用」= **State Manager**。
→ AWS Config / Inspector / CloudTrail+EventBridge は**検出はできてもインストールはできない**。

・CloudFormationの更新を既存インスタンスへ反映する手段としても使える（cfn-hupの代替）。

### 🔑 SSM Automation ランブック（AWSマネージド）

> 出現: 演習2-問題35 / 演習3-問題18

・AWSが保守する標準ランブックを使うと、自前でドキュメントを作る必要がなく保守性が高い。

・頻出:
- **`AWS-JoinDirectoryServiceDomain`**: EC2をAWS Managed Microsoft ADに参加させる。必要な管理ポリシーは **`AmazonSSMManagedInstanceCore` + `AmazonSSMDirectoryServiceAccess`**。
- **`AWS-EnableS3BucketEncryption`**: S3バケットのデフォルト暗号化を有効化（Configの修復アクションとして使う）。

・**Config ルール + Automation ランブック** = 検出と自動修復のセット。

### 🔑 Parameter Store の使いどころ

> 出現: 演習1-問題31 / 演習3-問題53 / 演習2-問題8

・**動的に変わる値の「単一の参照点」** として使う。

・典型パターン:
- **DBエンドポイントの保存**: フェイルオーバー時にLambdaが値を更新し、アプリは接続エラー時に**Parameter Storeから再取得**する（アプリの再デプロイ不要）
- **最新AMI IDの配布**: Image Builderが書き込み、CloudFormation/起動テンプレートが参照
- 階層型の命名 + IAMによる参照制御

---

## 7. EC2・Auto Scaling・AMI

### 🔑 EC2 Image Builder の配布設定 🚨

> 出現: 演習2-問題8

・**配布設定（Distribution Settings）** は、AMI作成後の後処理を一元管理する機能:
- 指定リージョンへのAMI配布
- **起動テンプレートの更新（新しいバージョンを自動作成、デフォルトバージョン変更も可）**
- AMIの他アカウント共有、タグ付与、**SSMパラメータへのAMI ID書き込み**

・**EventBridge + Lambdaで起動テンプレートを更新する自作案は「標準機能の迂回」で不正解**になる。

・注意: **パイプライン実行結果のSNS通知は配布設定ではなく「インフラストラクチャ設定」** で指定する。

・パイプラインの実行契機: 手動 / cron式 / EventBridgeスケジュール。「ベースイメージやコンポーネントに更新がある場合のみ実行」も選べる。

### 🔑 起動テンプレートのバージョン指定 🚨

> 出現: 演習2-問題8

・Auto Scalingグループが参照できるバージョンは3種類:
- **特定バージョン番号**: 固定
- **`$Default`**: 明示的にデフォルト指定されたバージョン
- **`$Latest`**: 常に最新

・**`$Latest` は検証前のバージョンが即座に本番へ反映されるリスク**がある。`SetDefaultVersion` と組み合わせた **`$Default`** の方が変更を制御しやすい。

### 🔑 インスタンスリフレッシュ

> 出現: 演習1-問題33

・ASG内の**既存インスタンスを段階的に置き換える**機能。最小正常率（`MinHealthyPercentage`）、ウォームアップ時間などで制御する。

・「起動テンプレートを変更したので、既存インスタンスにも新しい設定を適用したい」場合の標準手段。

### 🔑 Auto Scaling ウォームプール 🚨

> 出現: 演習3-問題45

・**事前初期化済みインスタンスをプールしておく**機能。状態は **Stopped / Running / Hibernated** から選べる。

・**Stopped 状態**: 起動時間を大幅短縮しつつ、課金は **EBSストレージ料金のみ**（コンピュート課金なし）。**「起動時間短縮とコスト効率の両立」要件の定番解**。

・**Standby 状態との違い**: Standbyはロードバランサーからは外れるが**Running料金が発生**する。メンテナンス用途であり、コスト削減目的には不適。

・「起動時にS3から大量データをダウンロードして処理するため起動が遅い」→ 事前にダウンロード済みの状態でプールしておく。

### 🔑 Auto Scaling ライフサイクルフック

> 出現: 演習3-問題45

・インスタンスの**起動時（`autoscaling:EC2_INSTANCE_LAUNCHING`）/ 終了時（`EC2_INSTANCE_TERMINATING`）** に処理を差し込む仕組み。**最大48時間**まで待機できる。

・起動フックでは、インスタンスが **InService になる前**に初期化（差分データ取得、ヘルスチェック準備）を行い、完了後に **`CompleteLifecycleAction`（CONTINUE / ABANDON）** で制御を返す。

・ウォームプールと組み合わせると、「プールから出したインスタンスが最新データに追いついてからトラフィックを受ける」制御ができる。

---

## 8. コンテナ(ECS・EKS)

### 🔑 ECS Task State Change イベント 🚨

> 出現: 演習1-問題63

・EventBridgeで捕捉できるECSのイベント:
- **`ECS Task State Change`**: タスクの状態変化。**終了理由（`stoppedReason`）、終了コード、タスクARN**を含む
- `ECS Container Instance State Change`: コンテナインスタンスの状態変化
- `ECS Deployment State Change`: サービスのデプロイ状態変化

・イベントパターン例:
```json
{ "source": ["aws.ecs"], "detail-type": ["ECS Task State Change"], "detail": { "lastStatus": ["STOPPED"] } }
```
・**「終了したタスクをすべて記録し、タスクレベルの詳細で調査したい」= EventBridge → CloudWatch Logs → Logs Insights**。
→ EC2インスタンスのログをCloudWatch Logsに送る案では、**タスクレベルの終了理由が取得できない**。

・タスクの状態: `PROVISIONING` → `PENDING` → `RUNNING` → `DEPROVISIONING` → `STOPPED`。

### 🔑 ECSクラスタへのインスタンス登録

> 出現: 演習2-問題67

・ECSエージェントは **`/etc/ecs/ecs.config` の `ECS_CLUSTER`** または環境変数からクラスタ名を読む。指定がなければ **default クラスタ**に登録される。

・CloudFormationでは起動設定/起動テンプレートの **UserData** にクラスタ名を書き込む。ECS-optimized AMI と **`ecsInstanceRole`（`AmazonEC2ContainerServiceforEC2Role`）** も前提。

### 🔑 EKS の aws-auth ConfigMap 🚨

> 出現: 演習3-問題20

・**IAMの認証（Authentication）が通っても、Kubernetes側の認可（Authorization）は別**。EKSクラスタの **`aws-auth` ConfigMap** でIAMロール/ユーザーをKubernetesのユーザー・グループにマッピングする必要がある。

・**クロスアカウントでCodeBuildからEKSを操作する手順**:
1. アプリケーションアカウントにデプロイ用IAMロールを作り、**信頼ポリシーで中央DevOpsアカウントを信頼**
2. そのロールにEKS操作権限（`eks:DescribeCluster` 等）を付与
3. **`aws-auth` ConfigMap にそのロールをマッピング**（例: `system:masters` や専用グループ）
4. CodeBuildのサービスロールから `sts:AssumeRole`

・**`Unauthorized` エラーが出たら、まず aws-auth を疑う**（IAM権限だけ足しても解決しない）。

---

## 9. サーバーレス(Lambda・API Gateway)

### 🔑 Lambda イベントソースマッピングと ReportBatchItemFailures 🚨

> 出現: 演習2-問題9

・イベントソースマッピングは、Lambdaとイベントソース（SQS / Kinesis / DynamoDB Streams）の連携設定。**バッチサイズ（SQSは最大10,000）、バッチウィンドウ、並行実行数**を制御する。

・**`FunctionResponseTypes: [ReportBatchItemFailures]`** を設定すると、Lambdaはレスポンスに **`batchItemFailures`（失敗したメッセージID）** を返せる。
→ **成功したメッセージだけキューから削除され、失敗分のみ再配信**される。

・従来はバッチ内の1件が失敗するとバッチ全体が再処理され、**処理済みメッセージの重複処理**が発生していた。

・副次効果として、**大きなバッチサイズを安全に使える**ようになり、Lambda実行回数・リクエスト料金・DynamoDBへの書き込み効率が改善する。

・SQS標準キューは **At-least-once配信**（重複あり・順序保証なし）。可視性タイムアウトのデフォルトは30秒、最大12時間。

### 🔑 Lambda 非同期呼び出しの再試行と Destinations 🚨

> 出現: 演習2-問題10

・SNS → Lambda は**非同期呼び出し**。**SNSは「呼び出しに成功した時点で責務完了」**。

・その後、**Lambda関数のコード内の処理が失敗**した場合、再試行は**Lambda自身の非同期呼び出し機構**が管理し、**既定で2回再試行（合計3回）**。使い切ると **DestinationsやDLQの設定がない限りイベントは破棄**される。

・**保護対象が違う2つの仕組みを混同しない**:
- **SNSサブスクリプションのDLQ**: 「**SNSからLambdaの呼び出し自体が失敗**した場合」のみ保護。**コード内の失敗は対象外** 🚨
- **Lambdaの失敗時Destination（SQS等）**: **コード内の処理失敗を事後的に捕捉**できる
- **SNS → SQS → Lambda（ポーリング）**: 下流が停止していても**メッセージがキューに残り続ける**ため、そもそも消失しない（最も堅い）

・EventBridgeへの置き換えも配信は非同期であり、ルールレベルのDLQは呼び出し失敗しか守らないため、同じ問題は解決しない。

### 🔑 API Gateway の IAM認証と SigV4 署名 🚨

> 出現: 演習1-問題44

・**`User: anonymous is not authorized`** = API GatewayがリクエストのIAMプリンシパルを識別できていない = **リクエストがSigV4で署名されていない**。

・解決には**2つセット**が必要:
1. **すべてのAPIメソッドで認証方式を `AWS_IAM` に設定**
2. **クライアントがAWS認証情報で Signature Version 4 署名**を行う

・アクセス範囲の制御は **API Gatewayのリソースポリシー**（`aws:PrincipalOrgPaths` でOU限定、`aws:SourceVpce` でVPCエンドポイント限定など）と組み合わせる。

### 🔑 API Gateway の相互TLS（mTLS）🚨

> 出現: 演習2-問題30

・**カスタムドメイン**に対して有効化する機能。**トラストストア（信頼するCA証明書のバンドル）をS3に配置し、そのURIを参照**する。

・内部専用アプリなら **ACM Private CA（AWS Private CA）** でプライベートCAを構成し、そのCAでクライアント証明書を発行する。

・**信頼基点（トラストストア）に指定するのはルート/中間CA証明書**であり、**個別のクライアント証明書や秘密鍵を指定することは暗号学的に誤り**（選択肢の切り分けポイント）。

・運用上の注意: CA証明書のS3アクセス権限、証明書の更新プロセス、失効時の対応手順。

### 🔑 API Gateway の SDK エクスポート

> 出現: 演習3-問題1

・API GatewayはデプロイされたステージからクライアントSDK（JavaScript / Android / iOS 等）を生成・エクスポートできる（`GetSdk` API）。

・CI/CDに組み込む場合は、**APIデプロイ直後のパイプラインアクションでLambdaを実行**し、SDK取得 → S3アップロード → **CloudFrontのキャッシュ無効化**まで一貫させる。

---

## 10. ストレージ

### 🔑 S3 レプリケーションの基本と「既存オブジェクト」問題 🚨

> 出現: 演習1-問題6 / 演習3-問題8 / 演習1-問題29

・**レプリケーションルールを設定しても、対象は設定後に作成された新規オブジェクトのみ**。**既存オブジェクトは複製されない**。

・**既存オブジェクトの複製 = S3 Batch Operations（バッチレプリケーション）**。マニフェスト（S3インベントリ）またはバッチレプリケーションジョブで一括処理する。

・前提条件: **ソース/ターゲット両方でバージョニングが有効**。メタデータ・ACL・タグは保持される。レプリケーションは**非同期**（結果整合性）。

・両リージョンで読み書きするなら **双方向レプリケーション**（片方向のルールだけでは要件を満たさない）。

### 🔑 クロスアカウントCRRの設定分担 🚨

> 出現: 演習3-問題8

・**レプリケーション処理は、S3サービスが「ソースアカウントのIAMロール」を使って実行する**。したがって制御の主体はソース側:

1. **ソースアカウント**: レプリケーション用IAMロールを作成（信頼: `s3.amazonaws.com`）
2. **ソースバケット**: レプリケーションルールを定義して有効化
3. **ターゲットバケット**: **バケットポリシー**でそのIAMロールに `s3:ReplicateObject` / `s3:ReplicateDelete` / `s3:ReplicateTags` を許可

・**ソースバケットポリシーの追加設定は不要**。ターゲットアカウント側でIAMロールを作る必要もない。

・SSE-KMS暗号化を使う場合は、KMSキーの復号/暗号化権限も必要。

### 🔑 S3 レプリケーションタイムコントロール（S3 RTC）🚨

> 出現: 演習1-問題29

・**オブジェクトの99.99%が15分以内にレプリケートされることをSLAで保証**する機能。

・レプリケーション遅延のモニタリングとCloudWatchメトリクス（`ReplicationLatency`, `OperationsPendingReplication` 等）、超過時のイベント通知を提供。

・**「〇分以内に複製されなければならない」という時間要件があればS3 RTC**。標準CRRより追加コストが発生する。

### 🔑 S3 マルチリージョンアクセスポイント（MRAP）🚨

> 出現: 演習1-問題29

・複数リージョンのS3バケットに対する**単一のグローバルエンドポイント**を提供。**アクティブ-アクティブ / アクティブ-パッシブ**構成に対応。

・**アクティブ-パッシブ**: 通常はプライマリから提供し、障害時にフェイルオーバーコントロールでセカンダリへ切り替え（**通常2分以内**に反映）。**アプリはエンドポイントを変更しなくてよい**。

・**`SubmitMultiRegionAccessPointRoutes` API**: 各リージョンへのトラフィックの重み（ルーティング）を更新する。**障害検知と組み合わせてAPI呼び出しを自動化することで、自動フェイルオーバーを実現**する。

・**S3 Transfer Acceleration は転送高速化の機能**であり、レプリケーション時間やフェイルオーバーには関与しない（引っかけ選択肢）。

### 🔑 S3イベント通知とレプリケーション失敗イベント 🚨

> 出現: 演習1-問題12

・**`s3:Replication:OperationFailedReplication`** はレプリケーション失敗を通知するイベントタイプ。

・**重要**: このイベントタイプは **EventBridge経由では配信されない**。**バケット通知設定から SQS / SNS / Lambda のいずれかへ直接配信**する必要がある 🚨

・大規模データ環境での再試行は、**個別に `PutObject` で再アップロードするのではなく、Lambdaから S3 Batch Operations ジョブを作成**してまとめて処理する（API呼び出し削減・処理時間短縮・エラーハンドリングの一元化）。

・その他のイベントタイプ: `s3:ObjectCreated:*`, `s3:ObjectRemoved:*`, `s3:Replication:OperationMissedThreshold`（RTCの閾値超過）など。

### 🔑 S3アップロードの完全性検証（Content-MD5 と ETag）🚨

> 出現: 演習3-問題10

・**`Content-MD5` ヘッダー**: アップロード時にMD5ダイジェストを送ると、**S3側が受信データと突き合わせて検証**する。不一致ならエラーになるので、**戻りステータスの確認**とセットで使う。

・**`ETag`**: レスポンスに含まれるオブジェクトのハッシュ。**シングルパートアップロードならMD5と一致する**ため、ローカルで計算したMD5と比較して事後検証できる。
→ **マルチパートアップロードではETagはMD5と一致しない**（パートごとのMD5を連結して再ハッシュした値 + `-パート数`）ので注意。

・タグ付けフィールドやクエリパラメータにチェックサムを付ける方法は検証機能として機能しない。

・新しいオプションとして `x-amz-checksum-*`（CRC32/CRC32C/SHA-1/SHA-256）もある。

### 🔑 Amazon FSx for NetApp ONTAP 🚨

> 出現: 演習1-問題33

・**SMB（Windows）と NFS（Linux）の両方を同時にサポート**する唯一のマネージドファイルシステム。マルチAZ構成で複数AZからのアクセスとミリ秒レイテンシを提供。

・比較:
- **Amazon EFS**: **NFSのみ**（Windows SMBは非対応）
- **EBS gp3**: **1度に1インスタンスにしかアタッチできない**（Multi-Attachはio1/io2の限定用途）
- **FSx for Windows File Server**: SMBのみ

・ASGと組み合わせる場合は、**新規インスタンスは起動テンプレートのUserDataでマウント**、**既存インスタンスはインスタンスリフレッシュ**で適用する。

### 🔑 S3 のデフォルト暗号化（2023年以降）

> 出現: 演習3-問題18

・**2023年1月以降、S3はすべての新規オブジェクトをデフォルトで SSE-S3 により暗号化**し、すべてのバケットにデフォルト暗号化が自動適用される。

・そのため現在の試験文脈では、「暗号化がまったく設定されていないバケット」ではなく、**組織が指定する暗号化方式（SSE-KMS等）の強制**や、**Configルール + 自動修復による継続的な準拠性確保**の仕組みを問う問題として読む。

---

## 11. データベース・DR

### 🔑 RPO と RTO 🚨

> 出現: 演習1-問題22

・**RPO（目標復旧時点）**: 障害時に許容できる**データ損失の最大量**。小さいほど頻繁なバックアップ or 継続的レプリケーションが必要。

・**RTO（目標復旧時間）**: 障害発生から**復旧までの最大許容時間**。小さいほど迅速なフェイルオーバーと高度な自動化が必要。

・**RPO≦2時間 / RTO≦10分 / リージョン障害対応** の組み合わせは、**DB層とアプリ層の両方**の手当てが必要（片方だけの選択肢は不十分）。

### 🔑 Aurora グローバルデータベース 🚨

> 出現: 演習1-問題22

・プライマリリージョン + 1つ以上のセカンダリリージョンで構成。**ストレージレベルのレプリケーションで、典型的な遅延は1秒未満**。

・**セカンダリを新しいプライマリに昇格させる操作は通常数分**。→ RPO/RTOの厳しい要件を満たせる。

・「複数リージョンにAuroraクラスターを置き、NLBでリージョン間のDBリクエストを分散」といった選択肢は**技術的に成立しない**（NLBはリージョナル）。

### 🔑 クロスリージョンリードレプリカの昇格自動化

> 出現: 演習1-問題31

・自動化の構成要素:
1. **障害検知**: **EventBridge**（リアルタイム性が必要。CloudTrailベースは遅い）
2. **昇格処理**: **Lambda**（`PromoteReadReplica` / `FailoverGlobalCluster`）
3. **エンドポイント管理**: **Parameter Store** に保存し、Lambdaが更新
4. **アプリ側**: **接続エラー時にParameter Storeからエンドポイントをリロード**

・これにより、アプリの再デプロイやDNS変更を待たずに接続先を切り替えられる。

### 🔑 Route 53 フェイルオーバールーティング

> 出現: 演習1-問題22

・プライマリ/セカンダリのリソースを指定し、**ヘルスチェックに基づいて自動的にルーティングを切り替える**。

・DNS変更の伝播には数秒〜数分かかり、**TTL設定で調整**する。RTO要件が非常に厳しい場合はTTLを短くしておく。

・マルチリージョンのアプリ層HAは「各リージョンにALB + ASG + ヘルスチェック + Route 53フェイルオーバー」が定番。

### 🔑 Route 53 ARC（Application Recovery Controller）とゾーナルシフト 🚨

> 出現: 演習1-問題26

・**ゾーナルシフト**: 特定AZ内のリソースへのトラフィックルーティングを**一時的に停止**する機能。障害の疑いがあるAZを意図的に迂回させる。

・**リソースセット**: 監視・回復アクションの対象となるリソースのグループ。
・**準備チェック（readiness check）**: リソースがトラフィックを処理する準備ができているかの確認（`ElbV2TargetGroupsCanServeTraffic` 等）。**トラフィック制御そのものではない**。

・**ALBのDNSホスト名はロードバランサー全体を指すため、特定AZを識別できない**（この選択肢は不正解になる）。

・2024年に「Amazon Application Recovery Controller」へ名称変更。

---

## 12. メッセージング・イベント

### 🔑 SQS 標準キューと FIFOキュー

> 出現: 演習2-問題9

・**標準キュー**: At-least-once配信（**重複あり**）、順序保証なし、高スループット。**アプリ側で冪等性の確保**が必要。

・**FIFOキュー**: Exactly-once処理と順序保証。重複除去IDとメッセージグループIDを使用。スループット制限があり料金は高い。

・可視性タイムアウト: 0秒〜12時間（デフォルト30秒）。**Lambdaのタイムアウトより長く**設定する。

### 🔑 EventBridge ルールとイベントパターン

> 出現: 演習1-問題63 / 演習2-問題21 / 演習1-問題65 / 演習2-問題31

・`source` / `detail-type` / `detail` でイベントを絞り込む。ターゲットにLambda / SNS / SQS / Step Functions / 他アカウントのイベントバス等を指定できる。

・頻出パターン:
- ECSタスク終了: `"source": ["aws.ecs"], "detail-type": ["ECS Task State Change"]`
- ECRスキャン完了: `"source": ["aws.ecr"], "detail-type": ["ECR Image Scan"]`
- Configのコンプライアンス変更: `"source": ["aws.config"], "detail-type": ["Config Rules Compliance Change"]`
- CloudTrail経由のAPIコール: `"detail-type": ["AWS API Call via CloudTrail"]`, `"detail": {"eventName": ["CreateAccount"]}`

### 🔑 EventBridge のクロスアカウント配信 🚨

> 出現: 演習2-問題31

・**イベントは「APIを実行したアカウント」で発生・記録される**（CreateAccountを自動化アカウントで実行すれば、CloudTrailイベントも自動化アカウントに記録される）。

・クロスアカウント配信の構成:
1. **送信側アカウント**のEventBridgeルールを作り、**ターゲットに受信側アカウントのデフォルトイベントバス（ARN）** を指定
2. **受信側アカウントのデフォルトイベントバスのリソースベースポリシー**で、送信側アカウントからの `events:PutEvents` を許可

・カスタムバスを新設せず**デフォルトバスを使う**ことで、AWS標準サービスのイベントとの統合が容易になり、設定も最小になる。

---

## 13. 監視・ロギング

### 🔑 CloudWatch Logs Insights と Contributor Insights の使い分け 🚨

> 出現: 演習1-問題63

・**Logs Insights**: クエリ言語でログを検索・集計・分析する。**個別イベントの詳細調査（終了理由、エラーコードの特定）に使う**。

・**Contributor Insights**: ログから「上位の貢献者（トップN）」を抽出してランキング表示する。**傾向分析用**であり、障害の根本原因の詳細調査には向かない。

・DOPでは「調査したい」= Logs Insights、「誰が一番多いか知りたい」= Contributor Insights と読み替える。

### 🔑 CloudWatch メトリクスフィルタとアラーム 🚨

> 出現: 演習3-問題72

・**メトリクスフィルタ**: ロググループに入ってくるログを**パターンマッチしてカスタムメトリクス化**する。追加インフラ不要で、大量ログでもリアルタイム処理できる。

・カウント値だけでなく、ログ中の数値をメトリクス値にすることもできる。

・アラーム設定例: 評価期間5分・1データポイントで発報 → SNSトピックへ通知。

・**「拒否リストのIPからのアクセスをリアルタイムで通知したい」+「低コスト」= フローログ → メトリクスフィルタ → アラーム → SNS** が最小構成（Athena/Kinesis/OpenSearchは過剰）。

### 🔑 VPC フローログ 🚨

> 出現: 演習3-問題72

・記録項目: `srcaddr` / `dstaddr` / `srcport` / `dstport` / `protocol` / **`action`（ACCEPT / REJECT）** など。**ログ形式はカスタマイズ可能**。

・**フィルタで ACCEPT / REJECT / ALL を選べる**。
→ **「拒否リストのIPが実際にアプリへ到達したか」を知りたいなら ACCEPT のみ**をキャプチャすればよく、データ量とコストを大幅に削減できる（REJECTは既にブロック済みで優先度が低い）。

・配信先: **CloudWatch Logs / S3 / Amazon Data Firehose**。後続処理とコストが変わるので要件に合わせる。

### 🔑 Amazon Managed Service for Prometheus（AMP）のアラート 🚨

> 出現: 演習2-問題70

・構成要素: **ワークスペース**（メトリクスとアラートルールの保存単位）/ **リモート書き込みエンドポイント**（メトリクス受信、SigV4認証）/ **Alert Manager**（アラートのルーティングと通知）/ **アラートルール**（PromQLで記述）。

・**SNSへ通知するための3ステップ**:
1. **アラートルールを作成**（例: `up{job="kubernetes-pods"} == 0 for 5m`）
2. **Alert Manager の設定でSNSトピックをレシーバーに指定**（`sns_configs` に `topic_arn`）
3. **SNSトピックのアクセスポリシー（リソースベース）で `aps.amazonaws.com` サービスプリンシパルに `sns:Publish` と `sns:GetTopicAttributes` を許可** 🚨

・**AMP側のIAMロールに権限を足すのは誤り**（AMPはサービスプリンシパルとして発行する）。**リモート書き込みURLはメトリクス受信用**でアラート転送には使わない。

・混乱した代理問題の対策として、`aws:SourceArn`（AMPワークスペースARN）と `aws:SourceAccount` を条件に入れるのが推奨。

### 🔑 Lambda の Errors メトリクスとアラームの次元

> 出現: 演習3-問題69

・カナリアデプロイの自動ロールバック用アラームは、**`AWS/Lambda` の `Errors` メトリクス**に対し、**関数名と Resource（バージョン/エイリアス）の次元を指定して関数ごとに個別作成**する。

・複合アラームは複数条件をまとめる用途であり、**関数単位の精密な異常検出には粒度が粗い**。

---

## 14. IAM・認証

### 🔑 信頼ポリシーとアクセス許可ポリシー（方向を間違えない）🚨

> 出現: 演習1-問題24 / 演習2-問題41 / 演習3-問題66 / 演習1-問題45

・**鉄則**:
- **信頼ポリシー（Trust Policy）は「引き受けられるロール」側に付ける**。「誰がこのロールを引き受けてよいか」を書く。
- **`sts:AssumeRole` の許可は「引き受ける側（呼び出し元）」のプリンシパルに付ける**。

・パターン別:
| シナリオ | 信頼ポリシーを置く場所 | AssumeRole許可を置く場所 |
|---|---|---|
| 管理アカウントのLambdaがメンバーのEC2情報を読む | **メンバーアカウント**のロール（信頼: 管理アカウント） | 管理アカウントのLambda実行ロール |
| オペレーションチームが各ワークロードを管理 | **各ワークロードアカウント**のロール（信頼: オペレーションアカウント） | オペレーションアカウントのIAMグループ |
| 専用アカウントのLambdaがOrganizations APIを呼ぶ | **管理アカウント**のロール（信頼: そのLambda実行ロール） | 専用アカウントのLambda実行ロール |
| CloudFormationがリソースを作る | サービスロール（信頼: `cloudformation.amazonaws.com`） | 開発者ロールに `iam:PassRole` |

・**逆方向にした選択肢が必ず紛れているので、「誰が誰を引き受けるのか」を日本語で言い直してから選ぶ**。

・ユーザー数が多い場合は、**個別ユーザーにポリシーを直付けせずIAMグループ**でまとめる（拡張性・保守性）。

### 🔑 sts:AssumeRole と一時認証情報

> 出現: 演習1-問題24 / 演習3-問題66

・実装例（Lambda内）: `boto3.client('sts').assume_role(RoleArn=..., RoleSessionName=...)` → 返却された一時認証情報で対象サービスのクライアントを生成。

・多数のメンバーアカウントを回る場合は、**並列処理とタイムアウト制御**を検討する。

・信頼ポリシーのプリンシパルは、**アカウント全体ではなく特定のロールARNに絞る**のが最小権限（例: 専用アカウントのLambda実行ロールのみ）。

### 🔑 OIDC フェデレーション（AssumeRoleWithWebIdentity）🚨

> 出現: 演習2-問題37

・OIDCのみをサポートするIdPからAWSリソースにアクセスさせる**3ステップ**:

1. **IAM OIDC IDプロバイダーを作成**: プロバイダーURL、**audience（クライアントID）**、署名（サムプリント）を登録
2. **IAMロールを作成**: 必要な権限（例: S3アクション）を付与し、**信頼ポリシーで `<provider>:aud` 条件**（`auth.company.com:aud` が特定のアプリIDに一致）を設定
3. **アプリが `AssumeRoleWithWebIdentity` を呼び、一時認証情報を取得**してAPIを呼ぶ

・**IAM Identity Center は人間のSSO（主にSAML）向け**であり、アプリケーションのAPI呼び出し用フェデレーションには不適（引っかけ選択肢）。

・条件キー `:aud`（audience）と `:sub`（subject）でアクセスを絞るのがベストプラクティス。

### 🔑 iam:PassRole と iam:PassedToService

> 出現: 演習1-問題45

・**`iam:PassRole`**: 「サービスにロールを渡す」ための権限。ユーザーがCloudFormation/CodePipeline/EC2などにロールを指定するときに必要。

・**`iam:PassedToService` 条件キー**: 渡し先のサービスを限定する（例: `cloudformation.amazonaws.com` のみ）。**これがないと任意のサービスに権限の強いロールを渡せてしまう**ため、権限昇格対策として重要。

### 🔑 aws:PrincipalAccount 条件キー 🚨

> 出現: 演習1-問題34

・**リクエスト元プリンシパルが属するアカウントID**を表す条件キー。すべてのリクエストに含まれる。

・**SCPが効かない管理アカウント内での制限**に使う。パーミッションセット（またはIAMポリシー）に、
```json
{ "Effect": "Deny", "Action": ["sso:*","sso-directory:*"], "Resource": "*",
  "Condition": { "StringEquals": { "aws:PrincipalAccount": "<管理アカウントID>" } } }
```
のような条件付きDenyを入れることで、**管理アカウントでのみ特定操作を禁止しつつ、他アカウントでは権限を維持**できる。

### 🔑 aws:SourceIp 条件キー

> 出現: 演習1-問題4

・**リクエスト元のIPアドレス**を表す条件キー。SCPやIAMポリシーで **API実行そのものをIP制限**できる。

・セキュリティ上は、**「許可する範囲を書く」より「範囲外を明示的にDenyする」** ほうが確実（許可型SCPは他の許可SCP（`FullAWSAccess`等）との組み合わせで骨抜きになり得る）。

・注意: VPCエンドポイント経由のリクエストには `aws:SourceIp` が効かないケースがあり、`aws:VpcSourceIp` / `aws:SourceVpce` を使い分ける。

### 🔑 ACM Private CA（AWS Private CA）

> 出現: 演習2-問題30

・組織内部で使う証明書を発行するマネージドCA。**外部CAへの依存とコストを排除**しつつ、証明書の発行・失効・更新を管理できる。

・mTLS用のクライアント証明書発行、内部サービス間TLSなどに使う。

### 🔑 混乱した代理問題（Confused Deputy）

> 出現: 演習2-問題70

・AWSサービスプリンシパルに権限を与える際、**`aws:SourceArn` / `aws:SourceAccount`** を条件に指定して「どのリソースからの呼び出しか」を限定する。

・クロスアカウントのロールでは **`sts:ExternalId`** を使う。

---

## 15. マルチアカウント統制

### 🔑 SCP の適用範囲 —— 管理アカウントには効かない 🚨

> 出現: 演習1-問題34 / 演習1-問題61 / 演習1-問題4

・**SCPはOrganizationsの管理アカウントには適用されない**。これはOrganizations仕様であり、**管理アカウントを新しいOUに移動しても変わらない** 🚨

・したがって「管理アカウント内で特定操作を禁止したい」場合は、**IAMポリシー / パーミッションセット側で明示的Deny**（+ `aws:PrincipalAccount` 条件）を使う。

・SCPは**許可の上限（ガードレール）**であり、それ単体では権限を与えない。実際のアクセスにはIAMポリシーとの組み合わせが必要。

### 🔑 SCP と IAMポリシー/パーミッションセットの使い分け 🚨

> 出現: 演習1-問題61 / 演習1-問題4

・**「そのアカウントでは誰も実行できないようにしたい」= SCP**。予防的制御であり、アカウント内のあらゆる経路（IAMユーザー、ロール、パーミッションセット）を一律で塞げる。

・**パーミッションセットやIAMポリシーにDenyを足す方法**は、そのセット/ポリシーを使う人にしか効かず、他の経路が残る。**「最も効率的」を問う問題ではSCPが正解**になりやすい。
→ 例外は「SCPが効かない管理アカウント」のケース。

・制御方式の階層: **予防的（SCP・IAM）> 検出（CloudTrail監視）> 事後対応（EventBridge+Lambdaで削除）**。**予防的が最も効果的で管理負荷が低い**。

### 🔑 Organizations API の実行制約 🚨

> 出現: 演習1-問題24

・**`organizations:CreateAccount` 等の主要な管理操作は、原則として管理アカウント（または委任管理者）からのみ実行可能**。

・別アカウントのLambdaから実行するには、**管理アカウントにロールを作り、そのロールをAssumeRole** する。信頼ポリシーは **専用アカウントのLambda実行ロールのみ**に絞る。

### 🔑 AWS Control Tower とコントロール（ガードレール）🚨

> 出現: 演習1-問題59 / 演習2-問題28 / 演習2-問題49 / 演習3-問題12

・**ランディングゾーン**: Organizations / IAM Identity Center / Config / CloudTrail 等を自動設定したセキュアなマルチアカウント基盤。

・**コントロール（旧ガードレール）**:
- **予防的コントロール = SCP**（アクションを防止）
- **検出的コントロール = AWS Config ルール**（違反を検出）
→ **「予防的と検出的の両方が必要」+「将来のアカウントにも自動適用」= Control Tower** 🚨
（Configコンフォーマンスパックは検出のみ、StackSetsは新規アカウントへの追従が手動）

・**Account Factory**: 標準化されたアカウント作成。**Account Factory カスタマイズ(AFC)** により、Service Catalog製品を**ブループリント**としてアカウント作成時に適用できる。

・検出的コントロールの非準拠は **EventBridgeイベント** として発行されるので、**監査アカウントのSNSトピックへ通知**するのが定番構成。

### 🔑 AWSControlTowerBlueprintAccess ロール

> 出現: 演習2-問題49

・Control Towerが**集中管理アカウント内のService Catalogリソースにアクセスする**ために必要なロール。

・**信頼ポリシー**: 管理アカウントの **`AWSControlTowerAdmin`** ロールが `sts:AssumeRole` できるようにする。
・**アクセス許可**: **`AWSServiceCatalogAdminFullAccess`**。

・カスタムオプション提供の3点セット = **CloudFormationテンプレート作成 + Service Catalog製品として登録 + このロールの作成**。

### 🔑 AWS Service Catalog

> 出現: 演習2-問題49

・**CloudFormationテンプレートを「製品」としてパッケージ化**し、ポートフォリオでグループ化してユーザー/グループに公開する。バージョン管理、アクセス制御、使用状況の追跡が可能。

・エンドユーザーはセルフサービスで承認済みリソースだけをプロビジョニングできる。

### 🔑 IAM Identity Center のパーミッションセット

> 出現: 演習1-問題34 / 演習3-問題12

・複数アカウントへのSSOとアクセス権限を一元管理する仕組み。**パーミッションセット**が各アカウントのIAMロールとして実体化される。

・**アクセス範囲の出し分け**（例: セキュリティ部門は組織全体の結果、一般ユーザーは自分のアカウントのみ）に最適。`CreateAccountAssignment` APIで割り当てを自動化できる。

・**SCPで「Security Hubを見せない」ようにする案は粗すぎる**（アカウント単位でしか効かず、部門別の閲覧範囲は表現できない）。

### 🔑 信頼されたアクセスと委任管理者 🚨

> 出現: 演習3-問題12 / 演習1-問題59

・**信頼されたアクセス（trusted access）**: Organizationsと他のAWSサービス（Security Hub / Config / Control Tower等）を統合するために、**管理アカウントで有効化**する。

・**委任管理者（delegated administrator）**: セキュリティ関連機能を**管理アカウントではなく専用アカウントに委任**する。AWSのベストプラクティス（管理アカウントは組織管理に専念）。

・**自動登録（auto-enable）**: Security Hub / GuardDuty / Config などで、**新規アカウントを自動的にメンバー登録**する機能。EventBridge+Lambdaの自作より信頼性が高く保守も容易。

---

## 16. セキュリティ検出・コンプライアンス

### 🔑 AWS Config マネージドルールと修復アクション 🚨

> 出現: 演習3-問題18 / 演習2-問題21 / 演習1-問題59

・**継続的な準拠性監視**サービス。リソース設定の変更を追跡し、ルールで評価する。

・よく出るマネージドルール:
- **`s3-bucket-server-side-encryption-enabled`**: S3のデフォルト暗号化
- **`cloudformation-stack-drift-detection-check`**: CFnスタックのドリフト
- `rds-storage-encrypted`, `encrypted-volumes` など

・**修復アクション = SSM Automation ランブック**（例: `AWS-EnableS3BucketEncryption`）を紐付けると、**非準拠検出 → 自動修復**が成立する。手動評価の実行で既存リソースも一括チェックできる。

・**「即座に適用」= 検出 + 自動修復**、**「作成そのものを防ぐ」= SCP/予防的コントロール**、と読み分ける。

### 🔑 コンフォーマンスパック

> 出現: 演習1-問題59

・Configルールと修復アクションをパッケージ化して組織全体に配布する仕組み。

・**提供するのは検出的コントロールのみ**。「予防的制御も必要」という要件では**単独では不足**し、Control Towerが正解になる。

### 🔑 AWS Security Hub 🚨

> 出現: 演習3-問題12

・複数のセキュリティサービスの検出結果を統合管理する。**CISベンチマーク**、**AWS Foundational Security Best Practices** 等の標準に対応。

・**組織運用の3点セット**:
1. **管理アカウントで信頼されたアクセスを有効化**
2. **専用のセキュリティアカウントを委任管理者に指定**（Control Towerで作成）し、そこでCISベンチマークを有効化
3. **自動登録を有効化**（新規アカウントを自動でメンバー化）

・閲覧範囲の制御は **IAM Identity Center のアクセス許可セット**で行う。

### 🔑 GuardDuty / Firewall Manager の位置づけ（引っかけ対策）

> 出現: 演習1-問題4

・**GuardDuty**: 脅威の**検出専用**。信頼できるIPリストは「検出結果から除外する」ためのものであり、**アクションのブロックはできない**。

・**Firewall Manager + Network Firewall**: **ネットワーク層**のトラフィック制御。**「AWSアカウントでアクションを実行することをブロック」= API/認可層** の要件には合わない。

・読み替えのコツ: **「アクションの実行を止める」= IAM/SCP**、**「通信を止める」= SG/NACL/Network Firewall**、**「気づく」= GuardDuty/Config/Security Hub**。

---

## 17. ネットワーク

### 🔑 AWS PrivateLink（エンドポイントサービス）🚨

> 出現: 演習3-問題38

・**サービスプロバイダー側がNLBを立て、エンドポイントサービスとして公開** → **コンシューマー側がインターフェイスVPCエンドポイントで接続**する。

・利点:
- **CIDRが重複していても接続できる**（20VPCすべてが同一CIDRでも可）
- ルートテーブル・ピアリングの管理が不要で、**既存環境への変更が最小**
- **DNS名でサービスを参照**できるため、アプリ側はエンドポイントURLの変更程度で済む
- インターネットを経由しない

・多数VPC間のマイクロサービス通信では、VPCピアリング（N×(N-1)/2の管理爆発）やTGW（CIDR重複に弱い）より優れる。

### 🔑 インターフェイス/ゲートウェイVPCエンドポイント

> 出現: 演習2-問題50 / 演習3-問題73

・**ゲートウェイ型**: S3 と DynamoDB のみ。ルートテーブルにエントリを追加する方式で**追加料金なし**。
・**インターフェイス型（PrivateLink）**: ほとんどのサービス。ENIが作られ、**セキュリティグループとDNS解決の設定が必要**。

・**ECRのプライベート利用には両方が必要**（`ecr.api` / `ecr.dkr` のインターフェイス型 + S3のゲートウェイ型）。

・CodeDeployエージェントやSSM Agentも、インターネット経路がない環境ではVPCエンドポイントが必要。

### 🔑 ALB のクロスゾーン負荷分散 🚨

> 出現: 演習1-問題26

・**ALBのクロスゾーン負荷分散はロードバランサーレベルでは常時オンに固定されており、無効化できない**。**無効化はターゲットグループレベルでのみ可能** 🚨

・**NLB / Gateway Load Balancer** は、ロードバランサーレベルとターゲットグループレベルの**両方**で設定できる。

・有効時: 各AZのノードが全AZのターゲットに分散。無効時: 各AZのノードは**同一AZのターゲットのみ**に分散 → **特定AZへのルーティングを止めれば、そのAZのインスタンスはトラフィックを受けなくなる**（ゾーナルシフトと組み合わせる理由）。

・ALBは**リージョナルサービス**であり、単一のALBで複数リージョンのターゲットは扱えない。

### 🔑 NAT / インターネットゲートウェイとエージェント通信

> 出現: 演習3-問題73

・CodeDeployエージェント・SSM Agent・ECSエージェントは、いずれも**AWSのサービスエンドポイントへのアウトバウンド通信**が前提。

・プライベートサブネットなら **NATゲートウェイ**、パブリックサブネットなら **インターネットゲートウェイ**、あるいは **VPCエンドポイント** のいずれかが必要。

・デプロイやコマンドが「開始されない/スキップされる」症状では、**まずこの到達性を疑う**。

---

## 📌 付録: 混同しやすいペア早見

| A | B | 見分け方 |
|---|---|---|
| `BeforeInstall` | `BeforeAllowTraffic` | ファイル配置=前者 / トラフィック直前の最終確認=後者 |
| `BeforeBlockTraffic` | `BeforeAllowTraffic` | 旧(ブルー)環境側=前者 / 新(グリーン)環境側=後者 |
| cfn-init | cfn-hup | 起動時に1回=前者 / 変更を検知して再適用=後者 |
| cfn-signal | ライフサイクルフック | CFnへの成否通知=前者 / ASGの状態遷移制御=後者 |
| State Manager | Run Command | 継続的に望ましい状態を維持=前者 / 単発実行=後者 |
| S3レプリケーション | S3 Batch Operations | 新規オブジェクト=前者 / 既存オブジェクト=後者 |
| S3 RTC | Transfer Acceleration | レプリケーション時間SLA=前者 / 転送高速化=後者 |
| Logs Insights | Contributor Insights | 詳細調査=前者 / 上位貢献者の傾向=後者 |
| SNSのDLQ | LambdaのDestination | 呼び出し失敗=前者 / コード内の処理失敗=後者 |
| SCP | パーミッションセットのDeny | アカウント全体を封じる=前者 / 管理アカウント内の制限=後者 |
| 予防的コントロール | 検出的コントロール | SCP=前者 / Configルール=後者（両方必要ならControl Tower） |
| Config + 修復 | Control Tower | 個別ルールの自動修復=前者 / 組織全体の統制基盤=後者 |
| ウォームプール(Stopped) | Standby | EBS課金のみ=前者 / Running課金あり=後者 |
| EFS | FSx for NetApp ONTAP | NFSのみ=前者 / SMB+NFS=後者 |
| ALBのクロスゾーン | NLBのクロスゾーン | TGレベルのみ設定可=前者 / LB・TG両方=後者 |
| Content-MD5 | ETag | 送信時にS3が検証=前者 / レスポンスで事後比較=後者 |
| 外部接続 | アップストリーム | パブリックへの接続(1つのみ)=前者 / リポジトリ間の参照連鎖=後者 |

---

**この辞書は「凝縮まとめ」で思い出せなかったキーワードを引くために使ってください 🍀**
