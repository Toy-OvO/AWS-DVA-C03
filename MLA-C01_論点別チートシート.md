# AWS Certified Machine Learning Engineer – Associate (MLA-C01)
# 論点別チートシート（教科書版）

> **想定用途**: 問題集を解く前の通し読み用。ここに書いた判断軸だけで7割前後を狙う粒度に絞っています。
> **読み方**: 各論点の「正解を選ぶ判断基準」に出てくる**太字キーワード**は、問題文の中で見つけたら即座に候補を絞るためのトリガーです。これを反射で反応できるようにするのがゴールです。

---

## 0. 全論点に共通する「地の判断軸」

この試験は、**要件文に散りばめられた制約語が正解を一意に決める**という構造をしています。まずこの対応表を暗記してください。個別論点はこの応用です。

| 問題文の言い回し | 反応すべき方向 |
|---|---|
| 最小限の運用オーバーヘッド / 運用負荷を最小 / 最小限の開発作業 | マネージド・ノーコード・組み込み機能を優先。自前実装（Lambda自作、EC2、スクラッチ学習）は原則不正解 |
| コードを書かずに / ローコード・ノーコード / プログラミングに不慣れ | Data Wrangler、Glue DataBrew、SageMaker Canvas、Autopilot |
| 最短時間で / 最速で実現 | 既存の学習済みAIサービス（Comprehend, Rekognition, Transcribe...）。「モデルを一から作る」選択肢は切る |
| 最もコスト効率が良い | ワークロードの**時間プロファイル**を見る（後述の購入オプション／推論オプション論点） |
| 既存の◯◯を活かして / 既存のスクリプトを再利用 | スクリプトモード、リフト&シフト、既存アーキテクチャを壊さない選択肢 |
| リアルタイム / ミリ秒 / 即座に応答 | 同期型（リアルタイム推論エンドポイント） |
| 大容量ペイロード / 処理に数分 | 非同期推論 |
| 定期的に一括で / 夜間バッチ / 全件まとめて | バッチ変換 |
| 断続的 / 使わない時間はゼロ | サーバーレス推論 |
| インターネットを経由しない / プライベートIPのみ | VPCエンドポイント（S3はゲートウェイ型、それ以外はインターフェース型/PrivateLink） |
| 自動的に / 自動で開始 | EventBridge、Model Monitor + Lambda、Pipelines |
| 本番ユーザーに影響を与えずに評価 | シャドーテスト |
| トラフィックの一部を新モデルへ | production variants（variant weight） |
| 監査可能 / 承認を挟む | Model Registry の承認ステータス、Lineage Tracking、CloudTrail |

**「複数の要件が並んでいる問題は、いちばん厳しい制約が選択肢を1つに絞る」** のが原則です。要件を1つずつ選択肢に当てて消していくのではなく、最も特殊な要件（ペイロード300MB、処理60分、ラベルなし、他アカウント、など）を先に見つけてください。

---

# 第I部　推論とデプロイ

## 論点1. 4つの推論オプションの選択

MLA-C01で**最頻出**。ほぼ確実に複数問出ます。

- **問われ方**
  - 「モデルをデプロイする。〜という要件を満たす最もコスト効率の良い／適切な推論オプションはどれか」
  - トラフィックの時間プロファイル（ピークと深夜、営業時間中だけ、毎晩1回）、ペイロードサイズ（3MB、50MB、100〜300MB）、応答時間（ミリ秒、数分、60分以内）が必ず書いてある
  - 「エンドポイントを選べ」ではなく「アーキテクチャを選べ」の形で、他サービス（Lambda, ECS, EC2）と混ぜて出ることも多い

- **正解を選ぶ判断基準**
  1. **まずペイロードサイズと処理時間を見る**。これが物理的な足切りになる。25MB超 or 60秒超 → リアルタイムは即消し。4MB超 or 60秒超 → サーバーレスも消し
  2. **同期で結果が要るか**を見る。「ユーザーの操作に対してその場で返す」「記事表示のたびに」→ 同期（リアルタイム or サーバーレス）
  3. **アイドル時間があるか**を見る。「深夜はほぼゼロ」「営業時間外はゼロ」「毎晩1回だけ」→ サーバーレス推論
  4. **入力データが既にS3にある一括データか**を見る。「前日分の全件」「1,000万件をまとめて」→ バッチ変換
  5. **コールドスタートを許容できないか**を見る。「一切のコールドスタートを発生させない」→ プロビジョンド同時実行、または常時稼働のリアルタイム

- **紛らわしい選択肢との切り分け**

| | リアルタイム推論 | サーバーレス推論 | 非同期推論 | バッチ変換 |
|---|---|---|---|---|
| 同期/非同期 | 同期 | 同期 | 非同期（キュー） | 非同期（ジョブ） |
| ペイロード上限 | 25 MB | 4 MB | 1 GB | S3上のデータ全体 |
| 処理時間上限 | 60秒（レスポンスストリーミング時 8分） | 60秒 | 1時間 | ジョブ単位で無制限 |
| アイドル時のコスト | 発生（常時稼働） | ゼロ | ゼロまで縮退可 | ジョブ実行中のみ課金 |
| コールドスタート | なし | あり（プロビジョンド同時実行で回避） | あり得る | 該当なし |
| 入出力 | HTTPリクエスト/レスポンス | 同左 | S3経由（結果をS3へ書き出す） | S3 → S3 |
| 典型ユースケース | 不正検知、記事レコメンド、常時アクセス | 断続トラフィック、夜間1回、営業時間だけ | 動画/大画像解析、最適化計算、長時間推論 | 夜間の全件スコアリング |

  - **サーバーレス推論 vs 非同期推論**: どちらも「アイドル時ゼロ」だが、**ペイロード4MB / 1GB** で切れる。50MBの写真 → 非同期
  - **非同期推論 vs バッチ変換**: どちらもS3経由だが、**リクエスト駆動なら非同期、スケジュールされた一括処理ならバッチ変換**。「新しい写真が来るたび」「アップロードのたび」→ 非同期。「毎晩まとめて」→ バッチ変換
  - **Lambda が選択肢にある場合**: ペイロード上限（同期6MB / 非同期1MB）、実行時間15分、メモリ10,240MB で必ず切れる。モデルホスティングの正解になることはほぼない

- **ひっかけパターン**
  - 「アイドル時ゼロにできる」と書いてある**リアルタイム推論**の選択肢。推論コンポーネント + ManagedInstanceScaling の MinInstanceCount=0 で技術的には可能だが、**復帰に数分かかりその間はエラー**、かつ設定作業が増えるため「運用負荷最小」「即応答」の要件では不正解になる
  - EC2 リザーブドインスタンス／Fargate 固定タスク数で「常時稼働」させる選択肢。コスト最小化の要件に必ず反する
  - 非同期推論を「リアルタイムに返せる」かのように書いた選択肢。**非同期エンドポイントは同期呼び出しを受け付けられない**
  - サーバーレス推論に「MaxConcurrency を大きく」と書いて、実際の要件（毎晩1回・1リクエスト）に対して過剰にする選択肢

- **派生して問われうる点**
  - サーバーレス推論の**プロビジョンド同時実行**（毎朝2時間の予測可能な負荷、コールドスタート排除）
  - 非同期推論の**スケーリングポリシー**（処理がなければ0インスタンスへ）
  - サーバーレスのレイテンシー原因切り分け → CloudWatch の **ModelSetupTime**（起動時間）を見る。ModelLatency は推論処理時間、ModelLoadingWaitTime はマルチモデルエンドポイント向け

---

## 論点2. 1エンドポイントに複数モデルを載せる方式

- **問われ方**
  - 「数百のモデルをコスト効率よくホストしたい」
  - 「4つの異なるフレームワークのモデルを1つのエンドポイントで」
  - 「GPUインスタンス上で複数モデルを、モデルごとに違うスケーリング要件で」
  - 「前処理 → 推論 → 後処理を1つの呼び出しで」

- **正解を選ぶ判断基準**
  - **同じフレームワーク × 大量（数十〜数百）× コスト最小** → マルチモデルエンドポイント
  - **異なるフレームワーク × 少数（〜15）× 個別に呼び分けたい** → マルチコンテナエンドポイント
  - **モデルごとに独立したスケーリング／GPU共有／コールドスタート回避** → 推論コンポーネント（Inference Components）
  - **複数コンテナを直列に通したい（前処理→推論）** → 推論パイプライン（Serial Inference Pipeline）

- **紛らわしい選択肢との切り分け**

| 方式 | 収容数 | フレームワーク | 呼び出し | 特徴 |
|---|---|---|---|---|
| マルチモデルエンドポイント (MME) | 数百〜数千 | 同一（共有サービングコンテナ） | TargetModel を指定 | 使うモデルを動的にロード。未ロード時に遅延 |
| マルチコンテナエンドポイント | 最大15コンテナ | バラバラでOK | TargetContainerHostname を指定 | 全コンテナ常駐 |
| 推論コンポーネント | 複数 | バラバラでOK | コンポーネント名を指定 | コンポーネント単位のオートスケーリング。最小コピー数≥1でコールドスタート回避 |
| 推論パイプライン | 最大15コンテナ | バラバラでOK | 1回の呼び出しで直列実行 | 前処理・推論・後処理の連結 |
| 単一モデルエンドポイント×N | N台 | 自由 | 別々 | 管理対象とコストがN倍。「コスト効率」要件では不正解 |

- **ひっかけパターン**
  - 「数百のモデル」に対して**マルチコンテナエンドポイント**を選ばせる → 15コンテナ上限で不可
  - 「異なるフレームワーク」に対して**マルチモデルエンドポイント**を選ばせる → MMEは共有コンテナなので不可
  - 「モデルごとに別のスケーリング要件」に対して MME を選ばせる → MMEはエンドポイント単位でしかスケールしない

- **派生して問われうる点**
  - 推論パイプラインの各ステップに Data Wrangler のフローや Feature Store の変換を組み込むケース
  - MME の「モデルロード待ち」を示すメトリクス **ModelLoadingWaitTime**

---

## 論点3. 安全なデプロイ戦略（新モデルの検証と切り替え）

- **問われ方**
  - 「本番ユーザーに影響を与えずに、ライブデータで新モデルを評価したい」
  - 「トラフィックの10%だけ新モデルへ流したい」
  - 「ダウンタイムなしでバージョンを切り替えたい。予備インスタンスは1台だけ」

- **正解を選ぶ判断基準**
  - **「エンドユーザーに影響を与えない」「レスポンスは返さない」「出力を比較する」** → シャドーテスト（シャドーバリアント）
  - **「一部のユーザーに実際に返す」「10%へルーティング」「A/Bテスト」** → production variants + variant weight
  - **「ダウンタイムなし」「予備キャパシティが限られる」** → ローリングデプロイ（バッチサイズを小さく）
  - **「即座に全切り替え、問題があれば即ロールバック」** → ブルー/グリーン（オールアットワンス）

- **紛らわしい選択肢との切り分け**

| 戦略 | ユーザーへの影響 | 目的 | キーワード |
|---|---|---|---|
| シャドーテスト | なし（応答は破棄しS3等へ記録） | 新モデルのレイテンシー・精度・安定性の検証 | 「影響を与えず」「ライブデータで」「出力を比較」 |
| production variants（A/B） | あり（一部ユーザーが新モデルの結果を受け取る） | ビジネス指標の比較 | 「10%のトラフィック」「重み」 |
| ローリング | なし（順次入れ替え） | 予備キャパシティが少ない環境での無停止更新 | 「予備インスタンス1台」「ダウンタイムなし」 |
| ブルー/グリーン | 切替時に一瞬 | 素早い切替とロールバック | 「即座に切り戻せる」 |

- **ひっかけパターン**
  - **ALB や Global Accelerator で2つのエンドポイントへ振り分ける**選択肢。SageMakerエンドポイントは ALB/NLB のターゲットに直接指定できず、エンドポイントが2つに増えてコストと監視対象が倍になるため「運用負荷最小」要件で不正解
  - アプリケーション側にルーティングロジックを実装する選択肢 → 比率変更のたびにコード変更が必要で不正解
  - 「シャドーテストでサンプリング率100%」は正しい設定（全リクエストを複製して検証できる）。100%だから本番に影響する、というのは誤り

- **派生して問われうる点**
  - シャドーテストの結果保存先（S3）と昇格の手順
  - variant weight の合計に対する比率でトラフィックが分配される点
  - デプロイ後の監視に CloudWatch の Invocations / ModelLatency を使う

---

## 論点4. エンドポイントのオートスケーリング

- **問われ方**
  - 「トラフィックが時間帯で大きく変動する。コストを抑えつつピークに応答したい」
  - 「セール期間中の可用性を維持したい」
  - 「新しいインスタンスが準備完了前に次々起動してしまい非効率」

- **正解を選ぶ判断基準**
  - **変動トラフィック + コスト + 容量維持** → SageMakerエンドポイントのオートスケーリング（Application Auto Scaling / CloudWatchメトリクスベース）
  - **「準備完了前に次が起動する」「過剰にスケールアウトする」** → **スケールアウト後のクールダウン期間を延長**する
  - **予測可能な時間帯の負荷 + フルマネージド** → サーバーレス推論のプロビジョンド同時実行
  - **コールドスタート厳禁 + モデル別スケーリング** → 推論コンポーネント（最小コピー数 ≥ 1）

- **紛らわしい選択肢との切り分け**

| 症状 | 打ち手 |
|---|---|
| スケールアウトが速すぎて無駄なインスタンスが立つ | スケールアウトのクールダウンを**延ばす** |
| ピークに追従できず遅延 | ターゲット値を下げる／最大キャパシティを上げる |
| アイドル時のコストが高い | 最小キャパシティを下げる（推論コンポーネントなら0まで） |
| 毎朝決まった時間に負荷 | スケジュールドスケーリング or プロビジョンド同時実行 |

- **ひっかけパターン**
  - 「エンドポイント作成時に **InitialInstanceCount を 0** にする」→ **これは不可能**。0まで縮小できるのは Application Auto Scaling の最小キャパシティ側だけ
  - クールダウンを「短くする」と書いた選択肢（症状を悪化させる）
  - インスタンスタイプを大きくする選択肢（スケーリングの問題を解決しない）

- **派生して問われうる点**
  - ゼロインスタンスからのスケールアウトには **NoCapacityInvocationFailures** メトリクスを使ったステップスケーリングポリシーが必要（要確認：この挙動は比較的新しい仕様のため、細部はドキュメントで確認を推奨）
  - CloudWatch メトリクス（`InvocationsPerInstance`, `CPUUtilization`）でのターゲット追跡

---

## 論点5. インスタンス選定・推論最適化

- **問われ方**
  - 「SageMakerでのホスティング経験がない。適切なインスタンスタイプの推奨がほしい」
  - 「LLMを効率的に学習し、低レイテンシで推論したい」
  - 「トレーニングのエネルギー使用量・計算リソースを削減したい（サステナビリティ）」

- **正解を選ぶ判断基準**
  - **「インスタンスタイプの推奨」「どのインスタンスが最適か分からない」** → SageMaker Inference Recommender
  - **「LLMの学習を高速化」** → SageMaker Training Compiler
  - **「専用チップ」「学習コスト/電力削減」** → AWS Trainium（学習）/ AWS Inferentia（推論）
  - **「エッジデバイス向けにモデルを最適化」** → SageMaker Neo（要確認：本問題集での出題は確認できず、一般論として）
  - **「収束しない学習を早期に止めて計算資源を節約」** → SageMaker Debugger の組み込みルール

- **紛らわしい選択肢との切り分け**

| サービス | 役割 |
|---|---|
| Inference Recommender | ロードテストを走らせて最適なインスタンスタイプ・構成を推奨 |
| Compute Optimizer | EC2 / ECS(Fargate) / Lambda / EBS の**使用率から適正サイズを推奨**。SageMakerエンドポイントは対象外 |
| Debugger | 学習中の消失勾配・GPU未使用・過学習をリアルタイム検出、ルールでアクション実行 |
| Profiler | リソース使用率のプロファイリング |
| Training Compiler | 深層学習の学習をコンパイル最適化して高速化 |

- **ひっかけパターン**
  - 「コンテナ化されたMLアプリのコスト削減」に対して Inference Recommender を選ばせる → 対象がEC2/ECS/Lambda/EBSなら **Compute Optimizer** が正解
  - Debugger を「デプロイ後のモデル品質監視」に使う選択肢 → それは Model Monitor

- **派生して問われうる点**
  - Debugger の組み込みルール（`vanishing_gradient`, `overfit`, `low_gpu_utilization`）と、検出時に停止/通知するアクション
  - サステナビリティ論点で「Trainium/Inferentia + 早期停止」の2択選択

---

# 第II部　トレーニング基盤

## 論点6. 学習の実行形態（組み込み / スクリプトモード / BYOC）

- **問われ方**
  - 「オンプレのPyTorch（TensorFlow）モデルをSageMakerへ移行したい。既存のカスタムスクリプトをできるだけ再利用したい」
  - 「処理ジョブ・学習ジョブ・パイプラインのすべてで社内ライブラリを使い、一貫した環境にしたい」
  - 「ラップトップで作ったscikit-learn/TensorFlowモデルをSageMakerに載せたい」

- **正解を選ぶ判断基準**
  - **既存スクリプトを再利用 + 標準フレームワーク（PyTorch/TF/scikit-learn/XGBoost）** → **スクリプトモード**（AWS管理のプリビルトコンテナ上で自分のスクリプトを実行）
  - **独自ライブラリ・独自ランタイムを全工程で共有** → **カスタムDockerコンテナを ECR にホストし、各ジョブでイメージを指定**（BYOC）
  - **アルゴリズム選定から任せたい / データを渡すだけ** → 組み込みアルゴリズム、または Autopilot

- **紛らわしい選択肢との切り分け**

| 方式 | 書くもの | 使う場面 | 移行コスト |
|---|---|---|---|
| 組み込みアルゴリズム | ハイパーパラメータのみ | 定番タスク（XGBoost, 画像分類, DeepAR...） | 最小だが既存コードは捨てる |
| スクリプトモード | 学習スクリプト（.py） | 既存のフレームワークコードを持ち込む | 小 |
| カスタムコンテナ（BYOC） | Dockerfile + スクリプト | 独自依存関係、特殊ランタイム、全工程で環境統一 | 中〜大 |

- **ひっかけパターン**
  - 「既存スクリプトを活かして最小限の労力」に対して **BYOC** を選ばせる → スクリプトモードで足りるのにDockerを作らせるのは過剰
  - 逆に「処理ジョブ・学習ジョブ・パイプラインすべてで社内ライブラリを統一」に対してスクリプトモードを選ばせる → ライブラリ同梱が必要なので**カスタムコンテナ + ECR**が正解
  - 「モデルを一から再実装する」系の選択肢は、移行系の問題ではほぼ常に不正解

- **派生して問われうる点**
  - ECR へのイメージ登録とジョブ定義でのイメージURI指定
  - Kubernetes への lift-and-shift → 既存Dockerイメージを **ECR** へ上げて **EKS** へ展開（コントロールプレーンを自社管理しないなら EKS、IaCにPythonなら **CDK**）

---

## 論点7. 学習コストの最適化

- **問われ方**
  - 「毎月48〜72時間かかる学習。中断されても再開できる。固定予算で計算量を最大化したい」
  - 「毎週末に90分のバッチ。今後6か月継続。最もコスト効率が良い購入オプションは」
  - 「需要が今後55週間一定。毎週35時間稼働。学習コストを削減したい」
  - 「1日に数十回の学習ジョブ。起動待ち時間を減らしたい」

- **正解を選ぶ判断基準**
  - **「中断されても再開できる」「中断を許容」「固定予算で最大化」** → **スポットインスタンス（+ 自動チェックポイント）**
  - **「需要が◯週間/◯年にわたり一定」「継続的に使う」** → **Savings Plans（SageMaker Savings Plans）**。期間・前払いの条件が付いていればそれを選ぶ
  - **「データ損失を許容しない」（EMR）** → プライマリ/コアノードはOn-Demand、**タスクノードのみSpot**
  - **「連続して実行する学習ジョブの起動時間を最小化」** → **SageMaker managed warm pools**
  - **「収束しない学習を早期に止める」** → Debugger + 早期停止

- **紛らわしい選択肢との切り分け**

| 購入オプション | 適する条件 | 割引 |
|---|---|---|
| On-Demand | 断続的・予測不能・中断不可 | なし |
| Spot | 中断許容 + チェックポイントあり | 最大約90% |
| Savings Plans / RI | 1〜3年の継続的・安定した使用量 | 最大約72% |

  - **Spot vs Savings Plans の切り分け**: 「中断されても再開できる」と書いてあれば Spot、「需要が一定で長期」なら Savings Plans。**両方書いてあることは稀**なので、どちらの語が出ているかで決める
  - **Spot の必須条件**: チェックポイントがないと中断のたびに最初からやり直しになり、割引の意味が消える。**「チェックポイントなしのSpot」は不正解パターン**

- **ひっかけパターン**
  - 「月の9割は使わない」ワークロードに Savings Plans / RI を勧める選択肢 → コミット分が無駄になる
  - EMRで**全ノードをSpot**にする選択肢 → プライマリ/コアが落ちるとデータ損失（HDFS）
  - warm pools を「コスト削減」目的とだけ説明する選択肢（本来は**起動時間の短縮**が主目的。保持中は課金される点に注意）

- **派生して問われうる点**
  - スポット中断の**2分前通知**とチェックポイントからの再開フロー
  - `MaxWaitTimeInSeconds` / Managed Spot Training の設定（要確認：本問題集では明示されていないが定番）

---

## 論点8. 学習データの入力モードとストレージ選択

- **問われ方**
  - 「300GBの学習データ（1ファイル200MB）がS3 Standardにある。追加ストレージ費用を抑えて取り込みたい」
  - 「数百万個の小さめファイル（各数MB）での学習性能を改善したい。最小限の時間で」
  - 「オンプレサーバーとSageMakerノートブックの両方からアクセス。NFS互換、ファイルロックが必要」
  - 「FSx for ONTAP に8TBの画像がある。学習ジョブから直接読ませたい」

- **正解を選ぶ判断基準**
  - **「追加ストレージを増やしたくない」「S3のまま」「大きめのファイル」** → **fast file mode**（S3から直接ストリーミング読み込み。ダウンロード完了を待たない）
  - **「数百万ファイル」「学習の I/O 性能を上げたい」** → **FSx for Lustre**（S3バケットにリンク）
  - **「NFS互換」「オンプレとクラウド両方からマウント」「複数ユーザーの同時書き込み・ファイルロック」** → **Amazon EFS**
  - **「FSx for ONTAP のデータを使う」** → 同一VPC内で **NFSマウント**して学習インスタンスから読む

- **紛らわしい選択肢との切り分け**

| 入力モード / ストレージ | 特徴 | 向くケース |
|---|---|---|
| File mode（既定） | 学習前に全データをEBSへダウンロード | データが小さい・シンプル |
| Fast file mode | S3から直接ストリーミング。ダウンロード待ちなし | 大きいファイル、S3のまま使いたい |
| Pipe mode | S3からストリーミング（旧方式） | 巨大データの逐次読み |
| FSx for Lustre | 高スループット並列FS、S3リンク可 | 数百万ファイル、繰り返し学習 |
| EFS | NFSv4、ハイブリッド、共有書き込み | オンプレ併用、共同編集 |
| S3 Express One Zone | 低レイテンシー（要確認：本問題集では未出題） | — |

- **ひっかけパターン**
  - 「S3 Standard → S3 Express / FSx へコピーする」選択肢を「追加ストレージ費用を抑える」要件に対して出す → **コピーすれば費用が増える**ので不正解
  - **Data Wrangler / Glue DataBrew が FSx for ONTAP（NFS/SMB）に直接接続できる**とする選択肢 → **できない**
    - Data Wrangler の接続先: S3, Athena, Redshift, EMR, Snowflake, Databricks, Salesforce Data Cloud など
    - Glue DataBrew の接続先: S3, Glue Data Catalog テーブル, JDBC, AppFlow, Data Exchange

- **派生して問われうる点**
  - 分散学習時のデータ配置（S3のリージョン・AZを学習インスタンスと揃える）
  - EFS と FSx for Lustre の選択（NFS互換・共有編集ならEFS、スループット重視ならLustre）

---

## 論点9. 分散学習のネットワークと暗号化

- **問われ方**
  - 「分散学習のインスタンス間通信を暗号化したい」
  - 「インスタンス間の通信オーバーヘッド・ネットワークレイテンシを最小化したい」
  - 「学習ジョブ実行時の転送中データ（data in transit）を保護したい」

- **正解を選ぶ判断基準**
  - **「インスタンス間」「ノード間」の暗号化** → **コンテナ間トラフィック暗号化（inter-container traffic encryption）を有効化**。実体はノード間通信のTLS保護
  - **「通信オーバーヘッド・レイテンシを最小化」** → **同じVPCサブネット（同一AZ）に配置し、データも同じリージョン/AZに置く**

- **紛らわしい選択肢との切り分け**

| 保護対象 | 手段 |
|---|---|
| 分散学習のノード間通信 | コンテナ間トラフィック暗号化（TLS） |
| S3 ↔ 学習ジョブ | HTTPS + VPCゲートウェイエンドポイント |
| 保存データ（S3, EBS, ノートブック） | KMS（SSE-KMS） |
| ノートブックへのアクセス経路 | IAM条件キー `aws:sourceIp`、VPN |

- **ひっかけパターン**
  - 「保管時の暗号化（KMS）」を「転送中の暗号化」の答えとして出す
  - VPCエンドポイントを「ノード間暗号化」の答えとして出す（VPCエンドポイントは経路の話で暗号化そのものではない）

- **派生して問われうる点**
  - コンテナ間暗号化を有効にすると学習時間が伸びうる点（要確認）
  - ネットワーク分離（`EnableNetworkIsolation`）との違い：分離はコンテナの外部通信を遮断する設定であり暗号化ではない

---

## 論点10. ハイパーパラメータチューニング戦略

- **問われ方**
  - 「検証損失を最小化しつつ、最小の計算時間で。どのチューニング戦略か」
  - 「数百万枚の画像で深層学習。1構成の評価に数時間〜数日かかる」

- **正解を選ぶ判断基準**
  - **「計算時間を最小化」「大規模DL」「途中で見切りを付けたい」** → **Hyperband**（早期停止で有望な構成にリソースを再配分）
  - **「試行数が限られる、効率よく探索」** → ベイズ最適化
  - **「完全に独立・並列に大量試行」** → ランダムサーチ
  - **「全組み合わせを網羅」** → グリッドサーチ（計算時間最小の要件では**必ず不正解**）

- **紛らわしい選択肢との切り分け**

| 戦略 | 中間結果の利用 | 計算効率 | 特徴 |
|---|---|---|---|
| Grid | しない | 最低 | 網羅的、組み合わせ爆発 |
| Random | しない | 中 | 並列化しやすい |
| Bayesian | 過去の試行結果を利用 | 高 | 試行数を減らせるが逐次的 |
| Hyperband | 中間結果で早期停止 | 最高（大規模DL） | エポック途中で打ち切り、リソース再配分 |

- **ひっかけパターン**
  - 「ベイズ最適化のほうが常に優れている」と思わせる選択肢。**大規模DLで計算時間最小が要件なら Hyperband**
  - 「各構成を最後まで完全に評価する」という記述はランダム/グリッドの目印であり、時間最小要件では不正解のサイン

- **派生して問われうる点**
  - 早期停止（`EarlyStoppingType`）と Hyperband の関係
  - チューニングジョブの並列実行数と探索効率のトレードオフ

---

# 第III部　データ処理・データ基盤

## 論点11. データ準備サービスの使い分け（最重要の切り分け）

- **問われ方**
  - 「データを前処理したい。最小限の運用オーバーヘッド／コードを書かずに」
  - 「5TBのデータを複数ステップで数時間かけて処理し、全体を自動化したい」
  - 「2つのソース（CSV数百万件 + Aurora）を結合して別のS3へ、毎週自動で」

- **正解を選ぶ判断基準**
  - **「コードを書かずに」「視覚的に」「GUI」「ノーコード」 + SageMaker文脈** → **SageMaker Data Wrangler**
  - **「コードを書かずに」 + Glue/データエンジニアリング文脈、レシピ、S3入出力** → **AWS Glue DataBrew**
  - **「ETL」「スキーマの自動発見」「カタログ登録」「Spark」「大規模な結合」** → **AWS Glue（クローラー + ETLジョブ）**
  - **「5TB超」「複数の連続ステップ」「数時間かかる」「NLPなど高度な処理」「全体を自動化」** → **SageMaker Pipelines（+ EventBridge で起動）**
  - **「SQLで分析したい」「その場でクエリ」「サーバーレス」** → **Amazon Athena**
  - **「Spark を自分で回したい」「既存のHadoop/Sparkジョブ」** → **Amazon EMR**

- **紛らわしい選択肢との切り分け**

| サービス | 位置づけ | 得意 | 接続先の注意 |
|---|---|---|---|
| SageMaker Data Wrangler | ML前処理のGUI | 欠損値補完、エンコーディング、外れ値検出、バランス調整、画像ノイズ付与、フロー→パイプライン化 | S3 / Athena / Redshift / EMR / Snowflake / Databricks / SaaS。**NFS/SMB不可** |
| AWS Glue DataBrew | データクレンジングのGUI | レシピによる変換、マスキング、正規化、ワンホット | S3 / Glue Catalog / JDBC / AppFlow / Data Exchange。**NFS/SMB不可** |
| AWS Glue（ETL） | Sparkベースのサーバーレス ETL | 大規模結合、フォーマット変換、Relationalize、機密データ検出 | 幅広い |
| AWS Glue クローラー | スキーマ自動検出 | Data Catalog へのメタデータ登録 | — |
| SageMaker Processing | コンテナで前処理を実行 | 自作コードでの前処理 | — |
| SageMaker Pipelines | ワークフロー全体のオーケストレーション | 複数ステップ、長時間、DAG可視化 | — |
| Amazon Athena | サーバーレスSQL | 探索的分析、CTAS | S3 |
| Amazon EMR | マネージドHadoop/Spark | 既存Sparkジョブ、超大規模 | — |

- **ひっかけパターン**
  - **Lambda を大規模データ処理に使わせる**選択肢 → 15分の実行時間上限で必ず不正解
  - **EMR** を「最小限の運用オーバーヘッド」の答えにする → クラスターの起動・終了・監視が必要なので通常は不正解
  - Data Wrangler と DataBrew のどちらでも良さそうな場合 → **問題文が SageMaker で完結する文脈なら Data Wrangler、S3を入出力とする汎用のデータ加工なら DataBrew**
  - Step Functions を「データ変換エンジン」として出す → Step Functions はオーケストレーションのみで変換機能を持たない

- **派生して問われうる点**
  - Data Wrangler の具体的な変換機能（バランスデータ操作、時系列外れ値検出、画像ノイズ付与、類似性エンコーディング、最頻値補完）
  - DataBrew のレシピジョブ、データセット作成手順（S3パスを指定 → データセット作成 → レシピジョブ実行）
  - DataBrew は**1プロジェクト1データセット**なので、複数形式が混在するフォルダは**形式ごとにフォルダを分けてプロジェクトを分ける**

---

## 論点12. ファイル形式とクエリ性能の最適化

- **問われ方**
  - 「CSVのデータ量が増えてクエリが遅い。最小の運用オーバーヘッドで性能を最適化したい」
  - 「バッチ処理とニアリアルタイム処理の両方で最小のレイテンシーを出すファイル形式は」
  - 「Canvasで複雑な構造のデータを扱う。処理時間を最小化するファイル形式は」
  - 「直近3日間のクリック傾向をAthenaで分析。30日保持後アーカイブ。取得性能を最大化したい」

- **正解を選ぶ判断基準**
  - **「クエリが遅い」「性能最適化」「列指向」** → **Apache Parquet へ変換**（Glue ETL ジョブで変換するのが定番の正解形）
  - **「日付でクエリ」「直近N日」「スキャン量を減らす」** → **パーティション分割**（日付プレフィックス）
  - **「ストレージコストも下げたい」** → Parquet + **gzip / Snappy 圧縮**
  - **「既存のS3のCSVをそのままSQLで扱いたい、テーブルを作りたい」** → Athena の **CTAS（CREATE TABLE AS SELECT）**
  - **「古いデータをアーカイブ」** → S3 ライフサイクルポリシーで Glacier へ

- **紛らわしい選択肢との切り分け**

| 形式 | 特性 | 向くケース |
|---|---|---|
| CSV | 行指向、非圧縮、スキーマなし | 単純な受け渡し |
| JSON（ネスト） | 半構造化 | 変換が必要。**Glue の Relationalize でフラット化** |
| Apache Parquet | 列指向・圧縮・スキーマ内包・述語プッシュダウン | 分析クエリ、ML学習データ、両立が必要なとき |
| ORC | 列指向 | Hive系（Parquetと迷ったら本試験ではParquetが正解になりやすい） |
| RecordIO-protobuf | SageMaker組み込みアルゴリズムの高効率形式 | 組み込みアルゴリズムでのPipe mode（要確認：本問題集では未出題） |

- **ひっかけパターン**
  - 「クエリを速くする」に対して**インスタンスを大きくする / Redshiftへ移す**という選択肢 → 運用負荷が増えるので「最小限の運用オーバーヘッド」要件では不正解
  - Athena は既定で **S3 Glacier Flexible Retrieval / Deep Archive のオブジェクトをスキップする**。アーカイブ後もクエリできるかのように書いた選択肢に注意
  - パーティションを作らずに「全件スキャンで十分」とする選択肢

- **派生して問われうる点**
  - DeepAR 向けのデータ変換（Glueで Parquet + gzip 化）
  - Glue クローラーによるパーティションの自動認識と Data Catalog 更新

---

## 論点13. データレイク・カタログ・ガバナンス

- **問われ方**
  - 「複数ソース（ログ、MySQL台帳など）を1か所に集約し、メタデータ管理とアクセス制御を効率化したい」
  - 「MLエンジニアがAthenaでデータレイクを分析。担当ブランドごとにアクセス範囲を制限したい」
  - 「大規模な顧客データの重複レコードを、氏名・電話・住所など複数属性で検出したい。最小限のコードで」

- **正解を選ぶ判断基準**
  - **「データレイクの構築」「複数ソースの統合」「ガバナンス」** → **AWS Lake Formation**
  - **「担当ごとに範囲を分ける」「属性ベースで細かく」** → Lake Formation の **LFタグ（タグベースのアクセス制御）**
  - **「スキーマの自動発見」「メタデータの保存」** → **Glue クローラー + Glue Data Catalog**
  - **「重複レコードの名寄せ」「あいまい一致」** → **Lake Formation FindMatches 変換**（ML変換）
  - **「データ品質を自動チェック」** → **AWS Glue Data Quality**（ルールでチェック）

- **紛らわしい選択肢との切り分け**

| 手段 | 制御の粒度 | 使う場面 |
|---|---|---|
| S3 バケットポリシー | バケット/プレフィックス | S3への直接アクセス制御。**Athena経由でGlue Catalogを参照する認可までは担えない** |
| IAM ポリシー | API単位、プレフィックス条件 | 単一アカウント内で部門別にS3アクセスを分けるなど、シンプルな要件ならこれが正解 |
| Lake Formation 権限 | データベース/テーブル/列/行 | データレイクの一元ガバナンス |
| Lake Formation LFタグ | タグ付けによる論理的なグルーピング | 担当者が増減する、担当替えが頻繁 |

- **ひっかけパターン**
  - 「ブランドごとにIAMポリシー内の条件を書き分ける」→ **担当替えのたびにJSON編集**が必要で運用効率が悪い（LFタグが正解）
  - 逆に「単一アカウント・S3のみ・部門別」というシンプルな要件に Lake Formation を持ち出す → **部門ごとのIAMポリシーをロールにアタッチ**で十分（過剰設計は不正解）
  - S3バケットポリシーで Athena からのクエリを制御できるとする選択肢

- **派生して問われうる点**
  - Lake Formation が Glue の S3 アクセスを承認する構成
  - Glue Data Catalog がAthenaのテーブルメタデータの実体である点（S3にファイルとしてメタデータを置いてもカタログにはならない）

---

## 論点14. ストリーミングデータの取り込み・処理

- **問われ方**
  - 「毎分5GBのユーザー行動ログを取り込み、変換し、高速クエリできるデータストアへ」
  - 「1日数億件のクリックストリームをリアルタイム取り込み・処理・可視化。SQLで処理、分析者に対話ノートブックも」
  - 「毎秒数千件のJSONを処理し異常を検出。最小限の運用オーバーヘッドで」
  - 「Firehose + OpenSearch のダッシュボードでサブ秒レイテンシーを実現したい」

- **正解を選ぶ判断基準**
  - **取り込み**: 高スループット + 順序保証 + 複数コンシューマ → **Kinesis Data Streams**。Kafka互換が要件 → **Amazon MSK**。「S3/OpenSearch等へそのまま配信するだけ」→ **Data Firehose**
  - **ストリーム処理・変換・異常検知**: **Amazon Managed Service for Apache Flink**（SQLでの処理、ノートブックでの対話分析にも対応）
  - **格納先**: 低レイテンシーのキー参照 → **DynamoDB**。全文/ログ分析とダッシュボード → **OpenSearch Service**。分析クエリ → S3 + Athena（ただしリアルタイム要件では不適）
  - **Firehoseの遅延** → **バッファ間隔をゼロ（ゼロバッファリング）に設定**

- **紛らわしい選択肢との切り分け**

| サービス | 役割 | ひとこと |
|---|---|---|
| Kinesis Data Streams | 取り込み（保持・再読可） | 高スループットのリアルタイム取り込み |
| Data Firehose | 配信（S3/Redshift/OpenSearch等へ） | バッファ間隔が遅延要因。**ゼロバッファリング可** |
| Amazon MSK | Kafka互換の取り込み | Kafkaエコシステムを使いたい |
| Managed Service for Apache Flink | ストリーム処理・変換・集約・異常検知 | SQLとノートブックの両方に対応 |
| Athena | S3上のバッチSQL | **リアルタイム処理には不適** |
| DynamoDB | 低レイテンシーKVS | リアルタイム分析の格納先 |

- **ひっかけパターン**
  - リアルタイム要件に **S3 + Athena** や **SQS + Athena** を出す → S3へ書いてからのクエリになるためバッチ的で不正解
  - Firehose の遅延に対して「バッファ間隔を60秒→120秒へ延ばす」→ **悪化する**
  - ElastiCache / RDS / S3 を「リアルタイム分析の格納先」として出す

- **派生して問われうる点**
  - Firehose の `PutRecordBatch` のバッチサイズ調整
  - Flink のノートブック（Studio）による対話分析

---

## 論点15. SageMaker Feature Store

- **問われ方**
  - 「学習時と推論時で同じ特徴量を使いたい。バッチ学習とリアルタイム推論の間で一貫性を保ちたい」
  - 「EMR上のSparkでS3のデータを処理し、Feature Storeへ取り込みたい」
  - 「生データから特徴量を生成・処理・保存する仕組みを作りたい」

- **正解を選ぶ判断基準**
  - **「学習と推論で特徴量を共有」「一貫性」「特徴量リポジトリ」** → **Feature Store**
  - **「リアルタイム推論でも使う」+「バッチ学習でも使う」** → **オンラインストアとオフラインストアの両方を有効化**
  - **「Sparkから取り込む」** → **Feature Store Spark connector**
  - **「特徴量の生成・変換パイプライン」** → **SageMaker Feature Processing** + Feature Store

- **紛らわしい選択肢との切り分け**

| ストア | 用途 | 特性 |
|---|---|---|
| オンラインストア | リアルタイム推論での低レイテンシー参照 | 最新レコードを返す |
| オフラインストア | 学習用データセットの生成 | S3に履歴を蓄積。**過去時点を指定した取り出し（time travel）が可能** |

- **典型的なワークフロー順序**（並べ替え問題で出る）
  1. フィーチャーグループを作成する
  2. 特徴量データをフィーチャーグループへ取り込む（ingest）
  3. **過去の時点を指定してオフラインストアから特徴量を取り出し、学習用テーブルへ整える**
  4. そのデータでモデルを学習する

- **ひっかけパターン**
  - 「Athenaで都度クエリしてオンラインストアを作らない」→ リアルタイム推論の低レイテンシー要件を満たさない
  - オンラインストアだけ有効化して「バッチ学習にも使える」とする選択肢

- **派生して問われうる点**
  - フィーチャーグループへのタグ付けとインデックス
  - Feature Store と Data Wrangler フローの連携（Data Wranglerの出力をFeature Storeへ）

---

# 第IV部　特徴量エンジニアリング・前処理

## 論点16. カテゴリカル変数のエンコーディング

- **問われ方**
  - 「ジャンル／都市名などのカテゴリを数値に変換したい。適切な手法は」
  - 「『合格/不合格』の2値を数値化したい。**次元数を増やさずに**」
  - 「市区町村名に表記ゆれだけが違う数千種類の値がある」

- **正解を選ぶ判断基準**
  - **「順序関係がない」「カテゴリ数が少〜中」「大小関係を学習させたくない」** → **ワンホットエンコーディング**
  - **「次元数を増やしたくない」「2値」「順序がある」** → **ラベルエンコーディング**（または**バイナリエンコーディング**）
  - **「表記ゆれ」「似た文字列」「高カーディナリティのテキストカテゴリ」** → **類似性エンコーディング（similarity encoding）**（n-gram類似度で近い値を近い数値に写像。Data Wranglerの機能）
  - **「カテゴリ数が非常に多い」** → ターゲットエンコーディング / ハッシュ化（要確認：本問題集では類似性エンコーディングが出題）

- **紛らわしい選択肢との切り分け**

| 手法 | 次元 | 順序を持ち込むか | 向くケース |
|---|---|---|---|
| ワンホット | カテゴリ数だけ増える | 持ち込まない | 順序なし・低カーディナリティ |
| ラベル（順序） | 増えない（1列） | **持ち込む** | 順序がある、または2値 |
| バイナリ | log2(N)列 | ほぼ持ち込まない | 中カーディナリティで次元を抑えたい |
| 類似性 | 抑えられる | 表記の近さを反映 | 表記ゆれの多いテキストカテゴリ |

- **ひっかけパターン**
  - 順序のないカテゴリに**ラベルエンコーディング**を勧める → モデルが実在しない大小関係を学習してしまう（これが定番の誤答）
  - 2値カテゴリに**ワンホット**を勧める → 「次元数を増やさない」要件に反する（2値なら1列で足りる）
  - 数千種類の表記ゆれにワンホットを勧める → 次元爆発

- **派生して問われうる点**
  - Glue DataBrew のワンホットエンコーディング（ノーコードで一部の列だけ変換）
  - ニューラルネットワーク入力としてのエンベディング

---

## 論点17. 数値変換・スケーリング・外れ値

- **問われ方**
  - 「販売数量／価格が大きく歪んでいる。適切な変換は」
  - 「極端に大きい・小さい商品が混ざっている。一般的な商品に対して正確に予測したい」
  - 「学習時とまったく同じ方法で本番データを正規化したい」

- **正解を選ぶ判断基準**
  - **「右に大きく歪んだ分布」「桁が大きく違う」** → **対数変換（log transformation）**
  - **「極端な外れ値がある」+「一般的なケースで精度を出したい」** → **外れ値を除去したうえで対数変換**
  - **「訓練時と推論時の前処理を一致させたい」** → **訓練データから求めた統計量（min/max、平均・標準偏差）を保存し、推論時にもその値を使う**
  - **「0〜1に収めたい」** → min-max正規化。**「外れ値に強くしたい」** → 標準化（z-score）やロバストスケーリング

- **紛らわしい選択肢との切り分け**

| 変換 | 効果 | 注意 |
|---|---|---|
| min-max正規化 | 0〜1へ線形変換 | 外れ値に弱い。**最小値・最大値を保存して再利用する** |
| 標準化（z-score） | 平均0・分散1 | 分布の形は変わらない |
| 対数変換 | 歪みを緩和、桁を圧縮 | 0や負値に注意 |
| 外れ値除去 | 極端値の影響を排除 | 「一般的な商品に対して正確に」という要件のサイン |

- **ひっかけパターン**
  - 「本番データから改めて min/max を計算して正規化する」→ **訓練時と異なるスケールになり予測が狂う**（定番の誤答）
    - 例: 訓練の min=10, max=90 なら 65 → 0.6875。推論データから min=55, max=75 を再計算すると 65 → 0.5 になってしまう
  - DataBrew のレシピを本番データにそのまま再実行する選択肢 → 同じ理由で不正解。**統計量をモデル成果物と一緒にS3へ保存して読み込む**のが正解形

- **派生して問われうる点**
  - 欠損値補完の方法選択：**カテゴリ特徴量 → 最頻値**、数値特徴量 → 平均/中央値
  - Data Wrangler の時系列外れ値検出（自動判定 + グラフ確認）

---

## 論点18. クラス不均衡への対処

- **問われ方**
  - 「少数クラスが極端に少ない。学習前にデータセットを調整したい。最小限の運用努力で」
  - 「40〜45歳の正例クラスが過剰。DPL値が +0.9。適切なサンプリング手法は」
  - 「画像分類の学習データでクラス不均衡が生じている」

- **正解を選ぶ判断基準**
  - **「少数クラスを増やす」** → オーバーサンプリング（SMOTE 等）
  - **「多数クラスを減らす」「特定の層が過剰に表現されている」** → アンダーサンプリング
  - **「最小限の運用努力」+ SageMaker文脈** → **Data Wrangler の「バランスデータ」操作**
  - **どちらとも取れる場合**: 問題文が「過剰に表現されている（over-represented）」と書いていればアンダーサンプリング、「少数クラスのサンプルが足りない」ならオーバーサンプリング

- **紛らわしい選択肢との切り分け**

| 手法 | 何をするか | 副作用 |
|---|---|---|
| オーバーサンプリング | 少数クラスを複製/生成 | 過学習リスク |
| アンダーサンプリング | 多数クラスを削減 | 情報損失 |
| SMOTE | 少数クラスの合成サンプル生成 | Data Wranglerに搭載 |
| クラス重み付け | 損失関数で少数クラスを重視 | データは変えない |
| 評価指標の変更 | Recall / F1 / AUC を使う | **不均衡そのものは解消しない** |

- **ひっかけパターン**
  - 「クラス不均衡を解消したい」に対して**評価指標を変える**選択肢 → 指標変更は測り方の話でデータ調整ではない（問題文が「データセットを調整」と言っていれば不正解）
  - 逆に「不正取引をできるだけ多く検出したい」という**指標選択**の問題に、サンプリング手法を出す

- **派生して問われうる点**
  - Clarify の事前トレーニングバイアスメトリクス **DPL（ラベル比率の差）** との関連
  - 不均衡データに強い組み込みアルゴリズム（LightGBM、XGBoost の `scale_pos_weight`）

---

# 第V部　モデリングと評価

## 論点19. SageMaker 組み込みアルゴリズムの選択

- **問われ方**
  - 「ユースケースとアルゴリズムを対応付けよ」（マッチング形式で頻出）
  - 「SageMakerが直接サポートする組み込みアルゴリズムで、〜を実現したい」

- **正解を選ぶ判断基準**（丸暗記推奨・1対1対応）

| ユースケース／キーワード | アルゴリズム |
|---|---|
| 表形式データの分類・回帰、ランキング、最も汎用 | **XGBoost** |
| クラス不均衡 + 特徴量間の相互依存 + 複雑な分類 | **LightGBM** |
| 線形の分類・回帰、**説明可能性が必要**、規制当局への説明 | **Linear Learner（線形学習器）** |
| 高次元スパースなユーザー×アイテム相互作用、レコメンド | **Factorization Machines** |
| 時系列データの**異常検知**、センサーの普段と違う振る舞い | **Random Cut Forest (RCF)** |
| 時系列**予測**（複数系列の需要予測） | **DeepAR** |
| 画像の**どこに何があるか**（バウンディングボックス、位置情報） | **オブジェクト検出（Object Detection）** |
| 画像の**ピクセル単位の領域判別** | **セマンティックセグメンテーション** |
| 画像に1つのラベル | **画像分類** |
| テキスト**要約**、翻訳、系列変換 | **Sequence-to-Sequence (Seq2Seq)** |
| 単語埋め込み・テキスト分類 | **BlazingText** |
| トピック抽出 | **LDA / Neural Topic Model (NTM)** |
| 次元削減 | **PCA** |
| クラスタリング | **k-means** |
| IPアドレスの異常な利用パターン | **IP Insights** |
| オブジェクト同士の関係の埋め込み | **Object2Vec** |

- **紛らわしい選択肢との切り分け**
  - **オブジェクト検出 vs セマンティックセグメンテーション**: 「位置情報を提供」「素材がどこにあるか」→ どちらもあり得るが、**「バウンディングボックスで物体を特定」なら検出、「ピクセル単位で領域を塗り分ける」ならセグメンテーション**。問題文が「識別した素材の位置情報」程度なら**オブジェクト検出**
  - **RCF vs DeepAR**: **異常検知 = RCF、将来予測 = DeepAR**
  - **XGBoost vs Linear Learner**: 「説明可能性・監督当局への説明」が要件なら **Linear Learner**（係数で説明できる）。純粋に精度・ランキングなら XGBoost
  - **Factorization Machines vs XGBoost**: 「高次元スパース」「ユーザー×アイテム」ならFM

- **ひっかけパターン**
  - 「説明可能性が必要」なのに XGBoost を選ばせる（Clarify/SHAPで説明できるとはいえ、**組み込みアルゴリズム自体の解釈性**を問う設問では線形モデルが正解）
  - 「二値分類」に対してロジスティック回帰が選択肢にある場合、SageMakerの組み込みでは **Linear Learner の binary_classifier**

- **派生して問われうる点**
  - Linear Learner の `binary_classifier_model_selection_criteria`、`target_precision`、`target_recall`
  - XGBoost の `max_depth`（下げると過学習抑制）、`eta`、`subsample`

---

## 論点20. 過学習・未学習と学習曲線の読み方

- **問われ方**
  - 「訓練99% / 検証82%。デプロイ前に解決したい」
  - 「検証損失が改善した後に劣化へ転じる。軽減策を2つ選べ」
  - 「訓練損失と検証損失が振動している」
  - 「損失関数が収束しない。学習率とミニバッチサイズの適切な組み合わせは」

- **正解を選ぶ判断基準**
  - **訓練は良いが検証が悪い（乖離）** → **過学習**。対策：正則化（L1/L2）、ドロップアウト率を上げる、早期停止、交差検証、データ拡張、モデルを単純化（XGBoostなら `max_depth` を**小さく**）
  - **損失が振動する / 収束しない** → **学習率が高すぎる**。対策：**学習率を下げる**、**ミニバッチサイズを大きくする**（勾配推定が安定する）
  - **不要な特徴量を落としたい + 過学習** → **L1正則化（Lasso）**（係数を厳密にゼロにして特徴量選択になる）
  - **係数を縮小するが残したい** → **L2正則化（Ridge）**

- **紛らわしい選択肢との切り分け**

| 正則化 | 効果 | 選ぶ条件 |
|---|---|---|
| L1（Lasso） | 係数をゼロにする → **特徴量選択** | 「不要な特徴量を削除したい」「冗長な予測変数を削除」「多項式項を削除」 |
| L2（Ridge） | 係数を縮小するがゼロにしない | 「係数を縮小しつつゼロにはしない」 |
| Elastic Net | L1+L2 | 両方の性質が欲しい |
| ドロップアウト | ニューロンをランダムに無効化 | ニューラルネットワーク |
| 早期停止 | 検証性能が悪化したら打ち切り | エポックを重ねると劣化するケース |
| 交差検証 | 汎化性能の推定 | 評価の信頼性を上げる |

- **ひっかけパターン**
  - 過学習に対して「学習率を上げる」「エポックを増やす」「モデルを複雑にする」
  - 振動に対して「ミニバッチサイズを小さくする」（さらに不安定になる）
  - 「L1とL2を逆に説明する」選択肢（**ゼロにする＝L1**を確実に覚える）
  - 「過学習だからデータを減らす」（正しくは増やす／拡張する）

- **派生して問われうる点**
  - SageMaker Experiments で異なるモデルバージョンのメトリクスを追跡・比較する組み合わせ
  - Debugger の `overfit` ルールによる自動検出

---

## 論点21. 評価指標の選択

- **問われ方**
  - 「できるだけ多くの不正取引を特定したい。適切な評価指標は」
  - 「偽陰性のコストが偽陽性よりはるかに高い」
  - 「偽陽性を最小化したい」
  - 「正例と負例の両方の正しい予測を最大化したい」
  - 「回帰／時系列予測モデルの品質評価指標は」

- **正解を選ぶ判断基準**（**このマッピングは丸暗記**）

| 問題文の言い回し | 指標 |
|---|---|
| 見逃しを減らしたい / できるだけ多く検出 / **偽陰性のコストが高い** | **Recall（再現率）** = TP ÷ (TP + FN) |
| 誤検知を減らしたい / **偽陽性を最小化** / アラート疲れを避けたい | **Precision（適合率）** = TP ÷ (TP + FP) |
| 適合率と再現率のバランス / 不均衡データでの総合評価 | **F1スコア** |
| **正例と負例の両方**の正しい予測を最大化 / クラスがバランスしている | **Accuracy（正解率）** |
| しきい値に依存しない識別能力 | **AUC-ROC** |
| 回帰で、**元の単位で平均的なずれ**を知りたい / 外れ値の影響を抑えたい | **MAE（平均絶対誤差）** |
| 回帰で、**大きな誤差を重く**罰したい | **RMSE / MSE** |
| **時系列予測**の品質 | **wQL（重み付き分位損失）** および **RMSE** |
| 説明された分散の割合 | **R²（決定係数）** |

- **紛らわしい選択肢との切り分け**
  - **Recall vs Precision**: 「見逃したときの損失が大きい」（病気、不正、故障）→ Recall。「誤検知したときの損失が大きい」（正常品を不良と判定、スパム誤判定）→ Precision
  - **Accuracy vs F1**: クラスが不均衡なら Accuracy は無意味（全部を多数クラスと予測しても高くなる）。**問題文がクラス不均衡に触れていれば F1 / Recall**
  - **MAE vs RMSE**: 「元の単位でずれを把握」→ MAE。「大きな誤差を強く罰する」→ RMSE

- **ひっかけパターン**
  - トレードオフの方向を逆にする選択肢：「Recallを上げるとPrecisionも上がる」（通常は片方を上げるともう片方が下がる）
  - 「検出範囲を狭めたのに再現率が上がる」といった定義矛盾（例: QuickSight異常検出で Direction を All → Lower than expected に絞ると、**検出件数も再現率も下がる**）
  - Linear Learner で偽陽性を減らすのに `target_recall` を上げる選択肢 → 逆効果。正解は **`target_precision` を上げる**（`binary_classifier_model_selection_criteria` を `recall_at_target_precision` に設定）

- **派生して問われうる点**
  - Model Monitor のモデル品質監視が算出する指標群（混同行列、Precision、Recall、Accuracy、TPR/FPR、ROC、PR曲線、AUC、F0.5/F1/F2）
  - QuickSight 異常検出の Severity（感度）と Direction（方向）の違い：**件数を増やしたいなら Severity を "Low and above" に、方向の絞り込みは件数を減らす**

---

# 第VI部　MLOps・自動化

## 論点22. パイプラインとオーケストレーション

- **問われ方**
  - 「S3に新しいデータが来たらパイプラインを自動実行したい。最小限の運用オーバーヘッドで」
  - 「既存のGlueワークフローの出力をSageMaker Pipelinesで使いたい」
  - 「MLワークフローをステップ単位で細かく制御し、DAGとして可視化したい」

- **正解を選ぶ判断基準**
  - **「S3アップロードを契機に」「イベント駆動」「自動で開始」** → **EventBridge ルール**（ターゲットに SageMaker パイプライン）
  - **「MLワークフローをDAGで管理」「SageMakerネイティブ」** → **SageMaker Pipelines**
  - **「外部の処理（Glueワークフロー等）の完了を待ってから進む」** → **Pipelines の Callback step**
  - **「スケジュール実行」** → EventBridge Scheduler / Pipelines のスケジュール
  - **「複数AWSサービスにまたがる汎用ワークフロー」** → Step Functions（ただしデータ変換エンジンは持たない）

- **紛らわしい選択肢との切り分け**

| サービス | 得意 | ML文脈での位置づけ |
|---|---|---|
| SageMaker Pipelines | ML専用のDAG、ステップ再利用、Model Registry連携 | 第一候補 |
| Step Functions | 汎用のステートマシン | ML以外も混在するとき。変換処理そのものは持たない |
| EventBridge | イベント検知・トリガー | 起点を作る |
| Glue Workflows | Glueジョブ/クローラーの連鎖 | データ側のオーケストレーション |
| MWAA (Airflow) | 既存Airflow資産 | 運用負荷は高め |
| Lambda | 軽量な糊付け | 15分制限に注意 |

- **ひっかけパターン**
  - S3イベント検知に「Lambdaを定期実行してバケットをポーリングする」→ 運用負荷が高く不正解
  - Glueワークフローを「SageMaker Pipelinesの中に作り直す」→ 「既存の機能を活かす」要件に反する（Callback step が正解）
  - Pipelines の各ステップを Lambda で実装する → 15分制限

- **派生して問われうる点**
  - Pipelines の主要ステップ（Processing / Training / Model / RegisterModel / Condition / Callback / ClarifyCheck / QualityCheck）
  - CI/CD パイプラインの正しい順序：**データ取り込み → 前処理 → 学習 → 評価 → Model Registry へ登録 → 承認 → デプロイ**

---

## 論点23. モデル管理・バージョニング・実験管理

- **問われ方**
  - 「増え続けるモデルバージョンを一元管理し、承認済みだけを本番へ」
  - 「複数AWSアカウントに分散したモデルを中央カタログで一覧したい」
  - 「既存のモデルグループ構造を保ったまま、3カテゴリに整理したい」
  - 「数百回の学習の条件と結果を記録したい。最小の実装工数で」

- **正解を選ぶ判断基準**
  - **「バージョン管理」「承認」「カタログ化」「CI/CD連携」** → **SageMaker Model Registry**（モデルグループ = model package group、その配下にバージョンが自動採番される）
  - **「既存のグループ構造を壊さずに分類・整理」「発見可能性」** → **Model Registry collection**（コレクションにモデルグループを移す）
  - **「クロスアカウント」** → 中央アカウントに Model Registry を置き、**モデルグループにクロスアカウントリソースポリシー**をアタッチ
  - **「実験の条件と結果を記録・比較」** → **SageMaker Experiments**
  - **「データの出所・変換履歴を追跡」「監査」** → **SageMaker ML Lineage Tracking**
  - **「登録後に人が承認してから本番へ」** → Model Registry の**承認ステータス**を Pipelines / SDK で更新し、承認をデプロイの引き金にする

- **紛らわしい選択肢との切り分け**

| 機能 | 何を管理するか |
|---|---|
| Model Registry | モデルのバージョン、承認ステータス、デプロイ連携 |
| Model Registry collection | モデルグループの論理的なグルーピング（整理・発見可能性） |
| Experiments | 学習の試行（ハイパーパラメータと結果メトリクス） |
| ML Lineage Tracking | データ・コード・成果物の系譜。**事後の監査向けで、デプロイ制御はできない** |
| Model Cards | モデルのドキュメント化（要確認：本問題集では未出題） |

- **ひっかけパターン**
  - 「承認済みモデルだけを本番へ」に Lineage Tracking を出す → **監査はできてもデプロイ制御はできない**
  - モデルの一元管理に **ECR** を出す → ECRはコンテナイメージのレジストリでモデルのバージョン/承認管理はしない
  - 「既存のモデルグループを作り直す」選択肢 → 「既存構造を保つ」要件に反する

- **派生して問われうる点**
  - Canvas ユーザーへの共有：**Model Registry に登録してモデルグループで共有 + S3の読み取り権限付与**
  - CloudFormation でのリソースタイプ：モデル = **`AWS::SageMaker::Model`**、エンドポイント = `AWS::SageMaker::Endpoint`、設定 = `AWS::SageMaker::EndpointConfig`

---

## 論点24. モデル監視（Model Monitor / ドリフト）

**MLA-C01で最頻出のもう1つの柱。**

- **問われ方**
  - 「本番のセンサーデータの品質問題を検出しアラートを受け取りたい」
  - 「F1スコアが大幅に低下した。原因は何か」
  - 「データドリフトを検出したら自動で再学習ジョブを開始したい」
  - 「バイアスドリフトをオンデマンドで点検したい」
  - 「`baseline_drift_check` 違反でパイプラインが失敗する」

- **正解を選ぶ判断基準**
  - **監視対象で4種を選び分ける**（これが判断の中核）

| 監視の種類 | 監視対象 | 選ぶキーワード |
|---|---|---|
| **データ品質モニタリング** | 入力データの統計量（平均・標準偏差・欠損率）とベースラインの比較 | 「入力特徴量の分布」「データドリフト」「センサーデータの品質」 |
| **モデル品質モニタリング** | 予測 vs 正解ラベル（Accuracy, F1, AUC） | 「予測の正しさ」「精度劣化」。**正解ラベルが必要** |
| **バイアスドリフト（Clarify）** | 本番データ・予測のバイアスメトリクス | 「バイアスドリフト」「公平性」 |
| **特徴量アトリビューションドリフト（Clarify）** | 特徴量の寄与度の変化 | 「特徴量アトリビューションのドリフト」「寄与の変化で再学習判断」 |

  - **「検出 → 通知」**: Model Monitor → CloudWatch メトリクス → CloudWatch アラーム → SNS
  - **「検出 → 自動再学習」**: Model Monitor の通知を **Lambda** が受けて再学習ジョブを開始
  - **「オンデマンドで実行」** → Lambda から Clarify ジョブを都度起動
  - **「ベースライン違反が続く／モデル更新後に品質問題」** → **最新データで新しいベースラインを作り直す**

- **ドリフトの用語の切り分け（頻出）**

| 用語 | 定義 | 症状の書かれ方 |
|---|---|---|
| データドリフト（共変量シフト） | **入力特徴量の分布**が変わる | 「入力データの統計が学習時とずれた」 |
| コンセプトドリフト | **特徴量と目的変数の関係そのもの**が変わる | 「ベースライン分析後に時間が経ってF1が大幅低下」「市場環境が変わった」 |
| ラベルドリフト | 目的変数の分布が変わる | — |

- **ひっかけパターン**
  - 「入力分布の変化を監視したい」に**モデル品質モニタリング**を出す → 正解ラベルが必要で、入力分布のずれを早期に捉える用途に合わない
  - CloudWatch の**標準メトリクス**（Invocations、ModelLatency）で分布変化を監視するとする選択肢 → **リクエストの中身は見ていない**
  - CloudTrail でドリフトを検出するとする選択肢 → CloudTrail は**API呼び出しの監査**であって統計量は算出しない
  - Model Monitor を「**学習前**のデータバイアス検出」に使う選択肢 → それは Clarify（事前トレーニングバイアス）
  - 突然の性能低下の原因として「学習データのラベルミス」「アルゴリズムの選択ミス」を出す → **数か月正常だった後の急落は本番データ分布のドリフト**

- **派生して問われうる点**
  - 制約ファイルの `emit_metrics` を有効にすると特徴量ごとの統計がCloudWatchへ出力される
  - Model Monitor はバッチ変換の入力データ品質監視にも使える（非同期の一括推論 + 品質監視の組み合わせ問題）
  - Clarify を「デプロイ済みモデルの個々の予測の説明」に使う（コンプライアンス部門への説明要件）

---

## 論点25. CI/CD とインフラのコード化

- **問われ方**
  - 「CloudFormationでモデルを作りエンドポイントでホストしたい。宣言すべきリソースタイプは」
  - 「Kubernetesでモデルをホスト。コントロールプレーンは自社管理しない。IaCはPythonで書きたい」
  - 「オンプレのKubernetesからEKSへリフト&シフト」

- **正解を選ぶ判断基準**
  - **「IaCをPythonで書きたい」** → **AWS CDK**（CloudFormationはYAML/JSON）
  - **「Kubernetesのコントロールプレーンを管理しない」** → **Amazon EKS**
  - **「既存Dockerイメージをそのまま」** → **ECR にアップロード → EKS/ECS へ展開**（再ビルド不要が「リフト&シフト」「最小の運用オーバーヘッド」の答え）
  - **CloudFormationのリソースタイプ**: モデル本体 = `AWS::SageMaker::Model`

- **紛らわしい選択肢との切り分け**

| 手段 | 言語 | 特徴 |
|---|---|---|
| CloudFormation | YAML / JSON | 宣言的、AWSネイティブ |
| CDK | Python / TS など | プログラム的に定義し、CFnテンプレートを生成 |
| Terraform | HCL | マルチクラウド |
| SageMaker Projects | — | MLOpsのテンプレート化されたCI/CD |

- **ひっかけパターン**
  - 「Pythonで定義」の要件に CloudFormation を出す
  - 「コントロールプレーンを自社管理しない」の要件に **EC2上に自前Kubernetes** を出す
  - 「リフト&シフト」の要件に「アプリケーションを書き換えてSageMakerネイティブへ移行」を出す

- **派生して問われうる点**
  - EKS への SageMaker Operator 導入
  - ECR のイメージスキャン、ライフサイクルポリシー（要確認：本問題集では未出題）

---

## 論点26. ノートブック環境の運用

- **問われ方**
  - 「新規ノートブックインスタンスにカスタムスクリプトを自動導入したい。週十数台作る」
  - 「1時間アイドルで自動停止させたい」
  - 「rootアクセスを許可したノートブックのデプロイを事前に防ぎたい」
  - 「チームの権限管理を一元化したい」

- **正解を選ぶ判断基準**
  - **「起動時に自動で何かをする」「全ノートブックに共通の初期設定」** → **ライフサイクル設定（Lifecycle Configuration）スクリプト**
  - **「アイドル自動停止」** → ライフサイクル設定の **Start Notebook** セクションに auto-stop-idle スクリプトを配置
  - **「〜させない」「事前に防ぐ」「予防的」** → **IAMポリシーの条件キー**で作成時のパラメータを制限（例: rootアクセス無効の設定のみ許可）
  - **「権限管理を一元化」「追加作業を1箇所で」** → **単一のIAMロールを作り全ノートブックにアタッチ**

- **紛らわしい選択肢との切り分け**

| 目的 | 手段 | 性質 |
|---|---|---|
| 望まない設定を作らせない | IAMポリシーの条件キー / SCP | **予防的**（preventive） |
| 望まない設定を見つけて直す | AWS Config ルール + 修復 | **発見的**（detective） |
| 起動時の環境準備 | ライフサイクル設定 | 自動化 |

- **ひっかけパターン**
  - 「事前に防ぐ（予防的）」の要件に **AWS Config で検出して後から修正**する選択肢 → 検出は事後なので不正解
  - ライフサイクル設定の代わりに Lambda を作る選択肢 → 実装量が増えて「運用負荷最小」に反する
  - **ライフサイクル設定スクリプトは実行が5分を超えると失敗する**制約を無視した重い処理の選択肢

- **派生して問われうる点**
  - SageMaker Studio ドメイン内のユーザープロファイルへのタグ付け + **AWS Budgets** でのコストアラート
  - IAM Identity Center の権限セットはノートブックの**実行ロールを置き換えない**（混同注意）

---
# 第VII部　セキュリティ・ガバナンス

## 論点27. ネットワーク分離とプライベート接続

- **問われ方**
  - 「プライベートサブネットのEC2からBedrockへ、IGW/NATを使わずに接続したい」
  - 「学習ジョブをインターネットから分離しつつS3へアクセスしたい」
  - 「特定のIPアドレスからのSageMakerドメインへのトラフィックを遮断したい」
  - 「クロスアカウントでS3へ、パブリックIPv4なしでアクセスしたい」

- **正解を選ぶ判断基準**（**S3/DynamoDB か、それ以外か**で機械的に決まる）
  - **S3 または DynamoDB へのプライベート接続** → **ゲートウェイVPCエンドポイント**（ルートテーブルにルートを追加）
  - **それ以外のAWSサービス（Bedrock、SageMaker API/Runtime、Redshift 等）** → **インターフェースVPCエンドポイント（AWS PrivateLink）**
  - **特定IPの遮断** → **ネットワークACL のインバウンド拒否ルール**をサブネットに割り当て
  - **特定IPだけ許可（署名付きURL悪用の防止など）** → **IAMポリシーの条件キー `aws:sourceIp`**

- **紛らわしい選択肢との切り分け**

| 手段 | 対象 | 特徴 |
|---|---|---|
| ゲートウェイVPCエンドポイント | S3 / DynamoDB のみ | 無料、ルートテーブル経由、エンドポイントポリシーで絞れる |
| インターフェースVPCエンドポイント（PrivateLink） | 多くのAWSサービス、他VPCのサービス | ENIを作る、有料 |
| Transit Gateway | VPC間 / オンプレ間の接続 | **S3へのプライベートアクセスにはならない** |
| セキュリティグループ | ENI単位、**ステートフル**、許可ルールのみ | インスタンス/エンドポイント単位の制御 |
| ネットワークACL | サブネット単位、**ステートレス**、**拒否ルールが書ける** | 特定IPのブロックはこれ |
| NATゲートウェイ | プライベートサブネットからのアウトバウンド | インターネット経由になるので「経由しない」要件では不正解 |

- **ひっかけパターン**
  - S3へのプライベート接続に**インターフェースエンドポイント/PrivateLink**を出す（S3はゲートウェイ型が定番の正解。※S3にもインターフェース型は存在するが、試験では「ゲートウェイエンドポイント」が期待解になりやすい）
  - **Transit Gateway** を S3 プライベートアクセスの答えにする
  - 「特定IPをブロック」に**セキュリティグループ**を出す → SGには**拒否ルールが書けない**
  - 署名付きURLの流出対策に `aws:sourceVpc` を出す → **VPCエンドポイント経由のリクエストにしか付かない条件キー**で、VPNクライアントの直接アクセスでは機能しない

- **派生して問われうる点**
  - エンドポイントポリシーで特定バケットのみ許可する
  - 学習ジョブのVPC設定（プライベートサブネット + S3ゲートウェイエンドポイント）
  - `aws:RequestTag` / `aws:PrincipalTag` は**タグベース**の条件キーであり「どこから来たか」は判定できない

---

## 論点28. 暗号化とシークレット管理

- **問われ方**
  - 「S3バケットの暗号化をSSE-S3からSSE-KMSに変えたら学習ジョブがAccessDeniedで失敗する」
  - 「顧客の属性データを暗号化し、どのアプリがキーにアクセスできるか制御したい」
  - 「APIトークンを3か月ごとに自動ローテーションしたい」

- **正解を選ぶ判断基準**
  - **「どのアプリがキーにアクセスできるかを制御」「キー管理」** → **AWS KMS のカスタマーマネージドキー**
  - **SSE-KMS 変更後の AccessDenied** → **実行ロールのIAMポリシーに `kms:Decrypt` / `kms:Encrypt` を追加**
  - **「トークン/認証情報を安全に保存 + 自動ローテーション」** → **AWS Secrets Manager**（ローテーション用Lambdaと組み合わせ）
  - **「設定値・パラメータの保存（ローテーション不要）」** → Systems Manager Parameter Store

- **紛らわしい選択肢との切り分け**

| 用途 | サービス |
|---|---|
| 暗号鍵の作成・管理・アクセス制御 | AWS KMS |
| シークレットの保存 + **自動ローテーション** | Secrets Manager |
| 設定パラメータの保存（SecureStringも可、自動ローテーションなし） | Parameter Store |
| 転送中の暗号化（分散学習ノード間） | コンテナ間トラフィック暗号化（TLS） |
| S3のサーバー側暗号化 | SSE-S3（AWS管理）/ SSE-KMS（キー制御可）/ SSE-C |

- **ひっかけパターン**
  - 「自動ローテーション」に対して **EventBridge + Lambda を自作**する選択肢 → Secrets Manager のローテーション機能があるので作り込みは不正解
  - AccessDenied の原因を「バケットポリシー」だけに求める選択肢 → **KMSキーの権限**が要る
  - 「保管時の暗号化」と「転送中の暗号化」の取り違え

- **派生して問われうる点**
  - KMSキーポリシーとIAMポリシーの両方が必要な点
  - AWS Encryption CLI でKMSキーを参照してファイルを暗号化する

---

## 論点29. アクセス制御（IAM）とクロスアカウント

- **問われ方**
  - 「MLエンジニアが自部門のデータにだけアクセスできるようにしたい。単一アカウント、S3」
  - 「アカウントAのS3からアカウントBのSageMakerへデータを渡して学習したい」
  - 「アカウントAのComprehendカスタムモデルをアカウントBで使いたい」
  - 「署名付きURLの悪用を防ぎ、社内VPNの出口IPだけに限定したい」

- **正解を選ぶ判断基準**
  - **単一アカウント + 部門別のS3アクセス** → **部門ごとのIAMポリシーを作りロール/ユーザーにアタッチ**（シンプルな要件にLake Formationを持ち出さない）
  - **クロスアカウントでのリソース共有** → **リソースベースポリシー**（S3バケットポリシー、Model Registryのリソースポリシー、Comprehendのリソースベースポリシー）
  - **Comprehendカスタムモデルの共有** → ソースモデルにリソースベースIAMポリシーを付与し、**ImportModel API** でインポート
  - **接続元IPでの制限** → IAMポリシーの条件キー **`aws:sourceIp`**

- **紛らわしい選択肢との切り分け**

| 条件キー | 判定するもの | 使える場面 |
|---|---|---|
| `aws:sourceIp` | リクエストの送信元IPアドレス | 社内VPN出口IPに限定 |
| `aws:sourceVpc` | 経由したVPCエンドポイントのVPC ID | **VPCエンドポイント経由のときだけ**リクエストコンテキストに載る |
| `aws:RequestTag` | リクエストで渡されたタグ | ABAC（属性ベース） |
| `aws:PrincipalTag` | リクエスト元プリンシパルのタグ | ABAC |

- **ひっかけパターン**
  - 「単一アカウント・S3のみ」というシンプルな要件に **Lake Formation** や **S3 Access Points** を出す（過剰設計）
  - `aws:sourceVpc` を「送信元IPの制限」の答えにする
  - IAM Identity Center の権限セットを「ノートブックの実行ロールの代替」として出す

- **派生して問われうる点**
  - SageMaker 署名付きURLに対するIAM認可は、URL発行時だけでなく**利用時の各HTTPリクエスト/WebSocketフレームにも適用**される
  - クロスアカウントでのS3ゲートウェイエンドポイント + バケットポリシーでの相手アカウント許可

---

## 論点30. 機密データの検出とマスキング

- **問われ方**
  - 「S3内の機密データを自動識別して削除したい。最小限の運用オーバーヘッドで」
  - 「レビュー本文からPIIを除去して学習に渡したい。週数十万件」
  - 「ケア記録のPIIとPHIの両方を保護したい」
  - 「日次CSVのクレジットカード番号をマスクしたい」
  - 「Redshiftのデータをデータサイエンティストに見せるが、元データは変換しない」

- **正解を選ぶ判断基準**（**どこにあるデータか**で決まる）

| データの所在／要件 | 正解サービス |
|---|---|
| **S3内の機密データを発見（インベントリ）** | **Amazon Macie**（発見のみ。削除処理は Lambda 等と組み合わせ） |
| **テキストからPIIを検出・マスキング** | **Amazon Comprehend**（`DetectPiiEntities`） |
| **医療テキストからPHIを検出** | **Amazon Comprehend Medical** |
| **Glueジョブの中でマスキング（Spark、大量CSV）** | **AWS Glue の機密データ検出（Detect PII transform）** |
| **ノーコードでマスキング、順序を保持** | **AWS Glue DataBrew** |
| **Redshiftのクエリ結果だけを難読化し、元データは変えない** | **動的データマスキング（Dynamic Data Masking）ポリシー** |

- **紛らわしい選択肢との切り分け**
  - **Macie vs Comprehend PII**: Macie は**S3オブジェクトのスキャンと検出**が仕事。テキスト中のPIIを**その場で置換**したいなら Comprehend
  - **Comprehend vs Comprehend Medical**: PII（氏名・住所・カード番号）は Comprehend、PHI（症状・診断名・投薬歴などの医療エンティティ）は Comprehend Medical。**両方要求されたら両方使う選択肢が正解**
  - **動的データマスキング vs 匿名化テーブルの作成**: 「元のデータを変換しない」「匿名化済みデータを保存しない」→ 動的データマスキング。UNLOAD してGlueで処理する案は実装・運用負担が最大で不正解

- **ひっかけパターン**
  - 「PIIとPHIの両方」に対して Comprehend だけ／Comprehend Medical だけの選択肢
  - 匿名化したコピーを別テーブルに作る選択肢（「保存しない」要件に反する）
  - Macie に「削除まで自動でやる」機能があるかのように書いた選択肢

- **派生して問われうる点**
  - Glue の Detect PII transform の出力オプション（検出列の追加／指定文字列への置換／部分マスク／SHA-256ハッシュ化）
  - Comprehend の処理単位：`DetectSentiment`（単一文書）、`BatchDetectSentiment`（最大25文書）、`StartSentimentDetectionJob`（S3上の大量文書を非同期）

---

## 論点31. コンプライアンス（メタデータ収集のオプトアウト）

- **問われ方**
  - 「学習中にSageMakerが使用状況とエラーのメタデータを収集しないようにしたい」
  - 「集約メタデータの共有を禁止するコンプライアンスルールがある。学習ジョブはAPI呼び出しで作成される」

- **正解を選ぶ判断基準**
  - **メタデータ収集の停止 = 環境変数 `OPT_OUT_TRACKING` を `1` に設定する**。これ一択
  - 設定場所は**ジョブ作成時のAPI呼び出し**（`create-training-job --environment OPT_OUT_TRACKING=1`、Boto3/SDKの環境変数）。**学習スクリプトの中では指定できない**
  - 自動プロセスで作成する場合は、各ジョブの `Environment` に含める

- **ひっかけパターン**
  - CloudTrail で記録する → **事後の監査**であって収集の防止ではない
  - IAMでCloudWatchへの書き込みを拒否する → メタデータ収集はCloudWatchとは別の仕組みで動くので止まらず、ログ・メトリクスまで失う
  - プライベートサブネットで実行する / KMSで暗号化する → ネットワーク経路や保管とは独立なので止まらない

- **派生して問われうる点**
  - コンソールから作成した学習ジョブでは**既定でメタデータ収集が無効**
  - 「プライベートIPのみ + メタデータ非共有」の複合問題では、**VPCゲートウェイエンドポイント + `OPT_OUT_TRACKING=1`** の組み合わせが正解になる

---
# 第VIII部　AIサービスと生成AI

## 論点32. AIマネージドサービスの1対1マッピング

「**最短時間で / 最小限の開発工数で**」という語が出たら、**自前でモデルを作る選択肢は全部切って**この表から選びます。

- **問われ方**
  - 「〜を実現する最短のソリューションは」
  - 「独自モデルの学習を伴わず、最小限の開発工数で」

- **正解を選ぶ判断基準（暗記表）**

| やりたいこと | サービス |
|---|---|
| 音声 → テキスト（文字起こし） | **Amazon Transcribe** |
| 言語 → 言語（翻訳） | **Amazon Translate** |
| テキスト → 音声 | **Amazon Polly** |
| 感情分析、キーフレーズ抽出、エンティティ抽出、言語検出、トピック | **Amazon Comprehend** |
| PDF/画像からのテキスト・帳票・表の抽出（OCR） | **Amazon Textract** |
| 医療テキストの症状・診断名・投薬歴の抽出、PHI | **Amazon Comprehend Medical** |
| 画像・動画の物体/顔/不適切コンテンツ/視線検出 | **Amazon Rekognition** |
| ドキュメントの**セマンティック検索**（社内規程、FAQ） | **Amazon Kendra** |
| レコメンデーション（マネージド） | **Amazon Personalize** |
| 時系列予測（マネージド） | **Amazon Forecast** |
| 会話ボット（意図・スロット） | **Amazon Lex** |
| 人間によるレビューの組み込み（低信頼度だけ人に回す） | **Amazon A2I（Augmented AI）** |
| データラベリング | **SageMaker Ground Truth** |
| 社内向け生成AIアシスタント（企業データ横断） | **Amazon Q Business** |

- **紛らわしい選択肢との切り分け**
  - **Comprehend vs Comprehend Medical**: 医療用語（症状・診断名・薬剤名）が出たら Medical
  - **Textract vs Comprehend**: 「PDFからテキストを取り出す」が主目的なら Textract、「テキストからエンティティ/感情を取り出す」なら Comprehend。※Comprehend は**PDFを直接入力してエンティティ抽出**もできるため、「事前のテキスト抽出工程を挟まない」と書かれていたら Comprehend が正解になり得る
  - **Kendra vs OpenSearch**: 「セマンティック検索」「自然文で聞く」→ Kendra。キーワード/ログ分析 → OpenSearch
  - **Rekognition vs SageMaker で自作**: 「最小限の運用負荷」「最短時間」なら Rekognition
  - **Personalize vs Factorization Machines**: マネージドで済ませたいなら Personalize、SageMaker組み込みアルゴリズムを指定されていれば FM

- **ひっかけパターン**
  - 「最短時間で」という要件に対して**ナイーブベイズ／RNN／seq2seq を一から構築**する選択肢（データ整備・特徴量設計・学習・評価・運用が全部必要になるため必ず不正解）
  - Transcribe Call Analytics の入力は**S3上の音声ファイルまたは音声ストリーム**であり、**テキストを直接入力できない**
  - 「音声＋動画＋テキストが混在」「外国語」といった複合要件に対し、1サービスだけの選択肢（正解は Transcribe + Translate + Bedrock のような**組み合わせ**）

- **派生して問われうる点**
  - Rekognition + A2I（低信頼度だけ人間レビュー）でラベリング時間を短縮するパターン
  - Comprehend のキーフレーズ抽出 + カスタムエンティティ認識の組み合わせ
  - Textract の非同期分析クォータ（PDF最大50MB・100ページ、1ジョブ最大500ファイル）

---

## 論点33. 生成AI・Bedrock・RAG

- **問われ方**
  - 「チャットボットの回答のばらつきを減らしたい」
  - 「S3の.csvと.docxをデータソースにRAGを実装したい。運用負荷を最小に」
  - 「オープンソースLLMをデプロイして社内文書に答えさせたい」
  - 「Amazon Q Business の応答に競合他社名を出したくない」
  - 「各ユースケースに適したFMEvalの評価タスクを割り当てよ」

- **正解を選ぶ判断基準**
  - **「回答の一貫性を高めたい／ばらつきを減らしたい」** → **temperature を下げる、top_k（top_p）を下げる**（両方下げる）
  - **「多様性・創造性がほしい」** → 逆に上げる
  - **「S3のドキュメントをRAGのデータソースに、運用負荷最小」** → **Amazon Bedrock ナレッジベース（Knowledge Bases）**（インデックス作成と検索が自動化される）
  - **「オープンソースLLMを手早くデプロイ」** → **SageMaker JumpStart**
  - **「サーバーレスな対話フロントエンド」** → **API Gateway（WebSocket API）+ Lambda**
  - **「Q Business の応答から特定語を除外」** → **ブロックフレーズ（blocked phrases）を登録**

- **用語の1対1対応（生成AI）**

| 用語 | 意味 |
|---|---|
| 埋め込み（Embedding） | テキストを意味を保った数値ベクトルへ変換したもの |
| RAG（検索拡張生成） | 回答生成前に外部ナレッジベースを検索し、見つけた内容を質問に添えてモデルへ渡す手法 |
| temperature | 出力のランダム性。**低い=一貫性が高い** |
| top_k / top_p | 候補トークンの絞り込み。**低い=一貫性が高い** |
| プロンプトエンジニアリング | 指示文の工夫で出力を制御する |
| ファインチューニング | モデルの重み自体を追加学習で更新する |
| ガードレール | 望ましくない入出力をブロックする仕組み |
| エージェント | ツール/APIを呼び出して多段のタスクを実行させる仕組み |

- **FMEval の評価タスクの割り当て（マッチング頻出）**

| ユースケース | 評価タスク |
|---|---|
| ラベルを付与する（スパム判定、カテゴリ分け） | **分類（Classification）** |
| 制約の少ない自由文の生成 | **オープンエンド生成（Open-ended generation）** |
| 根拠を添えて問いに端的に答える | **質問応答（Question answering）** |
| 長文を短くまとめる | **テキスト要約（Summarization）** |

- **ひっかけパターン**
  - 「一貫性を高める」のに **top_k を上げる**選択肢（候補が増えてランダム性が上がる）
  - temperature を下げつつ top_k を上げる組み合わせ（効果を打ち消す）
  - RAGの実装を「Lambdaでベクトル検索を自作」させる選択肢 → 運用負荷最小の要件に反する（ただし、選択肢にナレッジベースがなく「S3に埋め込みを保存してLambdaが検索、Bedrockが生成」という**サーバーレス構成の組み合わせ**が正解になる出題もあるため、選択肢の顔ぶれで判断する）
  - 「ファインチューニング」と「RAG」の取り違え：**最新の社内情報を参照させたい → RAG**、**口調やタスク特性を学ばせたい → ファインチューニング**

- **派生して問われうる点**
  - Bedrock へのプライベート接続（インターフェースVPCエンドポイント / PrivateLink）
  - Bedrock のモデル呼び出し API（`InvokeModel`）と Lambda 経由のアーキテクチャ

---

## 論点34. データラベリング（Ground Truth / A2I）

- **問われ方**
  - 「大量のラベルなし画像にラベルを付けたい。社内の担当者だけに限定して、可能な限り高精度で」
  - 「前回は全ビデオフレームのラベリングに6か月かかった。大幅に短縮したい」

- **正解を選ぶ判断基準**
  - **「社内の担当者だけ」「機密性が高い」** → **プライベート（内部）ワークフォース**でワークチームを作る
  - **「専門知識を持つベンダーに委託」** → ベンダーワークフォース（AWS Marketplace）
  - **「大量・低機密・低コスト」** → パブリックワークフォース（Mechanical Turk）
  - **「ラベリング時間を大幅に短縮」** → **AIサービスで自動ラベリングし、低信頼度だけ A2I で人間レビュー**（例: Rekognition + A2I）
  - **「Ground Truth の自動ラベリング」** → アクティブラーニングによる自動ラベリング（要確認：本問題集では Rekognition + A2I が正解として出題）

- **紛らわしい選択肢との切り分け**

| ワークフォース | 誰が作業するか | 機密データ |
|---|---|---|
| プライベート（内部） | 自社の従業員・登録した専門家 | ○ |
| ベンダー | Marketplaceのラベリング事業者 | △（契約次第） |
| パブリック（MTurk） | 不特定多数 | **×** |

- **ひっかけパターン**
  - 機密画像に**パブリックワークフォース**を出す
  - 「自社で育成した専門家を登録したい」に**ベンダーワークフォース**を出す
  - A2I だけでラベリングを完結させる選択肢（A2Iは**人間レビューの組み込み**であって、ラベリングジョブ管理そのものは Ground Truth）

- **派生して問われうる点**
  - Ground Truth のラベリングジョブ作成手順（タスクの種類、判定基準、ワークチームの割り当て）
  - ラベル品質向上のための複数人によるアノテーション統合（要確認）

---

## 論点35. ローコード／ノーコードML（Canvas / Autopilot / QuickSight）

- **問われ方**
  - 「プログラミングに不慣れな担当者がLLMをファインチューニングしたい」
  - 「ターゲット変数を予測できるか判断したい。最小限の開発作業で」
  - 「QuickSightの異常検出でDirection/Severityを変えたときの影響は」

- **正解を選ぶ判断基準**
  - **「ローコード・ノーコード（LCNC）」「プログラミングに不慣れ」+ SageMaker** → **SageMaker Canvas**（基盤モデルのファインチューニングもCanvas内で可能）
  - **「予測できるか試したい」「自動でモデルを作って評価指標を見たい」** → **SageMaker Autopilot**
  - **「ビジネスユーザー向けのダッシュボードと異常検出」** → **Amazon QuickSight**（ML Insights / 異常検出は内部で RCF を使用）

- **QuickSight 異常検出のパラメータ（1対1で覚える）**

| パラメータ | 選択肢 | 意味 |
|---|---|---|
| Severity（重大度・感度） | Low and above / **Medium and above（既定）** / High and above | 「Low and above」が最も多く検出、「High and above」が最も少ない |
| Direction（方向） | All / Higher than expected / Lower than expected | 方向を絞ると**検出件数も再現率も下がる** |

- **紛らわしい選択肢との切り分け**

| サービス | 誰向け | できること |
|---|---|---|
| Canvas | 業務担当者 | GUIでモデル構築・予測・基盤モデルのファインチューニング |
| Autopilot | データサイエンティスト/エンジニア | AutoMLで候補モデルを自動生成し、リーダーボードで比較 |
| Data Wrangler | データ準備担当 | 前処理のGUI |
| QuickSight | BIユーザー | 可視化と ML Insights（異常検出・予測） |

- **ひっかけパターン**
  - 「Canvasユーザーと外部モデルを共有」に対して**S3の権限だけ**／**Model Registry登録だけ**の片方のみの選択肢 → **両方必要**
  - QuickSight で「検出件数を増やしたい」に対して Direction を変える選択肢 → 件数を増やすのは **Severity（感度）** 側

- **派生して問われうる点**
  - Canvas での基盤モデルのファインチューニングと再学習の運用
  - Autopilot が出力する評価指標で「予測できるか」を報告するフロー

---
# 第IX部　コスト管理と可観測性

## 論点36. コストの監視・最適化サービス

- **問われ方**
  - 「SageMaker Studioのコンピューティングコストをプロジェクト単位で追跡し、しきい値でアラートを受けたい」
  - 「コンテナ化されたMLアプリの過剰プロビジョニングを特定したい。最小限の開発作業で」
  - 「Lambda関数のメトリクスが閾値を超えたらメールで通知したい」

- **正解を選ぶ判断基準**
  - **「予算のしきい値でアラート」** → **AWS Budgets**（+ リソースタグでプロジェクト単位に分ける）
  - **「過剰プロビジョニング/アイドルリソースの特定と適正サイズ推奨」** → **AWS Compute Optimizer**（対象: EC2 / ECS on Fargate / Lambda / EBS）
  - **「メトリクス監視 + 通知」** → **CloudWatch メトリクス + CloudWatch アラーム + SNS**（メール/SMS）
  - **「API呼び出しをすべて記録」** → **CloudTrail**（記録）+ **CloudWatch**（監視・アラーム）

- **紛らわしい選択肢との切り分け**

| サービス | 役割 |
|---|---|
| AWS Budgets | 予算しきい値の設定と通知 |
| Cost Explorer | コストの可視化・分析 |
| Compute Optimizer | 使用率に基づく適正サイズの推奨 |
| Trusted Advisor | ベストプラクティスのチェック |
| CloudWatch | メトリクス・ログ・アラーム |
| CloudTrail | API呼び出しの監査ログ |

- **ひっかけパターン**
  - 「API呼び出しイベントの件数がしきい値を超えたら通知」→ **CloudTrail だけ**では通知できない。**CloudTrail で記録 + CloudWatch のダッシュボード/アラームで監視・通知**が正解
  - Compute Optimizer に SageMaker エンドポイントの推奨機能があるかのように書いた選択肢（インスタンスタイプ推奨は **Inference Recommender**）
  - コスト追跡に「ユーザーごとに別ドメインを作る」→ 要件が「単一ドメイン内」のときは不正解。**ユーザープロファイルにタグを付ける**

- **派生して問われうる点**
  - SageMaker Studio のユーザープロファイル / スペースへのタグ付けとコスト配分タグ
  - Savings Plans / Spot / On-Demand の選択（論点7と共通）

---

# 第X部　バイアスと説明可能性

## 論点37. SageMaker Clarify

- **問われ方**
  - 「学習前にデータのバイアスを検出したい。適切な事前トレーニングバイアスメトリクスは」
  - 「人口統計グループ間の性能差を公平性メトリクスで数値化したい」
  - 「個々の予測に対する説明をコンプライアンス部門に示したい」
  - 「時間の経過とともに性能が落ちる。長期的・自動化された対策は」

- **正解を選ぶ判断基準**
  - **「学習前」「データのバイアス」「ラベルの偏り」** → **Clarify の事前トレーニングバイアスメトリクス**。ラベルの比率の差は **DPL（Difference in Proportions of Labels）**
  - **「学習後」「グループ間の性能差」「公平性メトリクス」** → Clarify の事後トレーニングバイアスメトリクス
  - **「個々の予測の説明」「各特徴量の寄与」** → **Clarify の説明可能性（SHAP に基づく特徴量アトリビューション）**
  - **「時間経過での性能低下を継続監視して再学習判断」** → **Clarify の特徴量アトリビューションドリフト監視（ModelExplainabilityMonitor）**
  - **「バイアスをオンデマンドで点検」** → Lambda から Clarify ジョブを起動
  - **「バイアスメトリクスをダッシュボードに出す」** → Clarify の結果を **CloudWatch メトリクス**から取得

- **紛らわしい選択肢との切り分け**

| 機能 | タイミング | 見るもの |
|---|---|---|
| 事前トレーニングバイアス（DPL など） | 学習前のデータ | 属性ごとのラベル比率の偏り |
| 事後トレーニングバイアス | 学習後 | グループ間の予測性能差 |
| 説明可能性（SHAP） | 推論時 | 各特徴量が予測に与えた寄与 |
| 特徴量アトリビューションドリフト | 本番運用中 | 寄与度の分布の変化 |
| Model Monitor（データ品質） | 本番運用中 | 入力データの統計量 |

- **DPL の読み方**: 値が大きいほど（例: +0.9）そのグループで正例が過剰に表現されている → **アンダーサンプリング**で是正するのが定番の正解

- **ひっかけパターン**
  - 「学習前のデータバイアス検出」に **Model Monitor** を出す
  - 「データ品質のルール整備」を Clarify に、「バイアス検出」を Data Wrangler に割り当てる逆パターン（正しくは**データ品質 = Data Wrangler、バイアス検出 = Clarify**）
  - 「個々の予測の説明」に Debugger を出す

- **派生して問われうる点**
  - Clarify + Data Wrangler の役割分担
  - Clarify のバイアスレポートの出力先（S3、SageMaker Studio上のUI）

---

# 付録A　数値で覚えるべき事項

## 推論オプション

- リアルタイム推論: ペイロード **最大25MB**、処理時間 **最大60秒**（レスポンスストリーミング時は **最大8分**）
- サーバーレス推論: ペイロード **最大4MB**、処理時間 **最大60秒**、メモリ **1,024MB〜6,144MB（1GB刻み）**、1エンドポイントあたり**最大同時実行数200**
- 非同期推論: ペイロード **最大1GB**、処理時間 **最大1時間**、インスタンス数を**0まで縮退可能**
- マルチコンテナエンドポイント / 推論パイプライン: **1エンドポイントあたり最大15コンテナ**
- エンドポイント作成時の `InitialInstanceCount` は **0にできない**（0への縮退はオートスケーリング側）

## AWS Lambda

- 実行時間: **最大15分（900秒）**
- メモリ: **最大10,240MB**
- 呼び出しペイロード: **同期6MB / 非同期1MB**

## 購入オプション

- スポットインスタンス: オンデマンド比 **最大約90%割引**、中断の **2分前**に通知
- Savings Plans / リザーブドインスタンス: **1年 or 3年**のコミット、**最大約72%割引**

## その他

- SageMaker ノートブックのライフサイクル設定スクリプト: 実行が **5分**を超えると失敗
- Amazon Comprehend: リアルタイム分析は1ドキュメント **UTF-8で最大100KB**、`BatchDetectSentiment` は **最大25文書**
- Amazon Textract: 同期分析は **最大10MB・1ページ**、非同期分析は PDF **最大50MB・最大100ページ**、1ジョブ **最大500ファイル**
- Amazon Data Firehose: バッファ間隔は **0秒（ゼロバッファリング）に設定可能**。既定のバッファ間隔（例: 60秒）が遅延の原因になる
- QuickSight 異常検出の Severity 既定値: **Medium and above**
- 推論コンポーネントをホストするエンドポイント: `ManagedInstanceScaling` の `MinInstanceCount` を **0** に設定可能。ただしゼロからの復帰に **数分**かかり、その間の呼び出しはエラー

---

# 付録B　用語1対1対応表（暗記用）

## SageMaker 機能名 → 一言の役割

| 機能 | 役割 |
|---|---|
| SageMaker Data Wrangler | GUIでのML向けデータ前処理（欠損値・エンコード・バランス調整・外れ値・画像変換） |
| SageMaker Processing | コンテナで前処理/後処理ジョブを実行 |
| SageMaker Feature Store | 特徴量の一元管理（オンライン/オフラインストア） |
| SageMaker Feature Processing | 特徴量の生成・変換パイプライン |
| SageMaker Experiments | 学習試行の条件と結果の記録・比較 |
| SageMaker Debugger | 学習中の異常（消失勾配・GPU未使用・過学習）をリアルタイム検出 |
| SageMaker Training Compiler | 深層学習の学習を高速化 |
| SageMaker managed warm pools | 学習リソースを保持して次のジョブの起動時間を短縮 |
| SageMaker Model Registry | モデルのバージョン管理・承認・カタログ化 |
| SageMaker Model Registry collection | モデルグループの論理的なグルーピング |
| SageMaker Pipelines | MLワークフローのDAGオーケストレーション |
| SageMaker ML Lineage Tracking | データ・コード・成果物の系譜追跡（監査用） |
| SageMaker Model Monitor | 本番モデルの継続監視（データ品質/モデル品質/バイアス/説明可能性） |
| SageMaker Clarify | バイアス検出（学習前・学習後）と説明可能性（SHAP） |
| SageMaker Inference Recommender | 推論インスタンスタイプ・構成の推奨 |
| SageMaker JumpStart | 事前学習済みモデル/OSS LLMのワンクリックデプロイ |
| SageMaker Canvas | ノーコードのモデル構築・予測・FMファインチューニング |
| SageMaker Autopilot | AutoMLによる自動モデル生成と比較 |
| SageMaker Ground Truth | データラベリングとワークフォース管理 |
| SageMaker Studio | 統合開発環境（ノートブック、パイプライン可視化） |
| SageMaker ライフサイクル設定 | ノートブック起動/作成時の自動スクリプト実行 |

## 監視・ログ・監査

| サービス | 役割 |
|---|---|
| CloudWatch | メトリクス・ログ・ダッシュボード・アラーム |
| CloudTrail | API呼び出しの監査ログ（**記録**であって通知ではない） |
| SNS | 通知の配信（メール、SMS） |
| EventBridge | イベント検知とターゲット起動 |
| AWS Config | 設定変更の記録とコンプライアンス評価（**発見的**） |

## 主要CloudWatchメトリクス（SageMaker）

| メトリクス | 意味 |
|---|---|
| `Invocations` | 呼び出し回数 |
| `ModelLatency` | モデルの**推論処理時間** |
| `OverheadLatency` | SageMaker側のオーバーヘッド |
| `ModelSetupTime` | **サーバーレスエンドポイントの起動時間**（コールドスタートの切り分けに使う） |
| `ModelLoadingWaitTime` | **マルチモデルエンドポイント**でのモデルロード待ち時間 |
| `CPUUtilization` / `MemoryUtilization` | リソース使用率（オートスケーリングの指標） |

---

# 付録C　「要確認」リスト（本問題集の記述だけでは断定できない点）

以下は本チートシートの中で確信度が相対的に低い、または問題集の解説にのみ依拠している項目です。試験直前に公式ドキュメントで裏取りすることを推奨します。

1. **推論コンポーネントのゼロスケール仕様**: `ManagedInstanceScaling` の `MinInstanceCount=0` と、ゼロからのスケールアウトに `NoCapacityInvocationFailures` メトリクスによるステップスケーリングが必要という点。比較的新しい機能のため、仕様が変わっている可能性があります。
2. **S3 へのインターフェースVPCエンドポイント**: S3にもインターフェース型エンドポイントは存在しますが、本試験では「S3 = ゲートウェイエンドポイント」が期待解になる傾向です。問題文が「他VPC/オンプレからのアクセス」を含む場合はインターフェース型が正解になり得ます。
3. **Ground Truth の自動ラベリング（アクティブラーニング）**: 本問題集では「Rekognition + A2I」が正解として出題されていますが、Ground Truth 自体の自動ラベリング機能を答えさせる出題も想定されます。選択肢の顔ぶれで判断してください。
4. **RAG のアーキテクチャ**: 「Bedrock ナレッジベース」が正解になる出題と、「S3に埋め込みを保存 + Lambda + Bedrock」というサーバーレス構成が正解になる出題の両方が確認できました。**選択肢にナレッジベースがあればそちらを優先**し、なければ構成の組み合わせで判断してください。
5. **コンテナ間トラフィック暗号化のオーバーヘッド**: 有効化すると学習時間が延びる可能性がある点は一般論であり、本問題集では明示されていません。
6. **SageMaker Neo / RecordIO-protobuf / S3 Express One Zone / Model Cards**: 本問題集では出題が確認できませんでした。試験範囲には含まれうるため、余裕があれば別途確認してください。
7. **オブジェクト検出 vs セマンティックセグメンテーション**: 「位置情報を提供する」という表現だけでは切り分けが微妙になる場合があります。「バウンディングボックス」「ピクセル単位」といった語の有無を最優先で見てください。

---

# 付録D　試験直前の20秒チェックリスト

問題を読んだら、この順で確認してください。

1. **制約語を探す** — 「最小限の運用オーバーヘッド」「最短時間」「最もコスト効率」「コードを書かずに」のどれか
2. **数値を探す** — ペイロードサイズ、処理時間、データ量、頻度。これが推論オプション/購入オプションを一意に決める
3. **時間プロファイルを探す** — 常時 / 断続的 / 夜間1回 / 予測可能な時間帯
4. **「既存の◯◯」を探す** — 既存資産を壊す選択肢は切る
5. **「影響を与えずに」を探す** — シャドーテストのサイン
6. **監視対象を確認する** — 入力データ / 予測の正しさ / バイアス / 寄与度 のどれか
7. **データの所在を確認する** — S3 / Redshift / テキスト / 医療テキスト。機密データ検出サービスがこれで決まる
8. **自前実装の選択肢を切る** — Lambda で大規模処理、モデルを一から構築、EC2 で自前運用、はほぼ常に不正解
